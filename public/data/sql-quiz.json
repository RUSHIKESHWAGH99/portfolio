{"version":4,"topicLabels":{"basics":"SQL fundamentals & clauses","joins":"JOINs & relational combinations","aggregates":"GROUP BY, aggregates & HAVING","subqueries":"Subqueries & EXISTS / IN","window":"Window functions & OVER","cte":"CTEs & WITH","optimization":"Query tuning & patterns","indexes":"Indexes & access paths","nulls":"NULL handling & functions","dml_ddl":"DML / DDL basics","set_ops":"UNION / INTERSECT / EXCEPT"},"questions":[{"id":1,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE user_id = NULL;","options":["Nothing; it returns rows where user_id is NULL","NULL cannot appear after an equality operator","The engine treats NULL as 0 for comparisons","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE"],"answer":3},{"id":2,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT order_id, region, COUNT(*) FROM users GROUP BY order_id;","options":["Two non-aggregate columns cannot precede COUNT","COUNT(*) is not allowed alongside GROUP BY","region is not in GROUP BY and not functionally dependent on order_id","GROUP BY must list every column in the table"],"answer":2},{"id":3,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(amount) > 10, where should that condition go?\nSELECT sku, AVG(amount) FROM events GROUP BY sku","options":["WHERE AVG(amount) > 10 — before GROUP BY","LIMIT with a sort by AVG replaces the filter","HAVING AVG(amount) > 10 — after GROUP BY","It cannot be expressed without a subquery"],"answer":2},{"id":4,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.status = b.status;","options":["Every row from skus appears; unmatched accounts columns are NULL","Unmatched accounts rows are kept; matched skus rows are dropped","A Cartesian product is produced","Only rows matching on both sides are returned"],"answer":0},{"id":5,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["An error unless an ON clause is provided","Duplicate-free union of both tables","Only rows with equal primary keys","Every row of sessions paired with every row of campaigns — Cartesian product"],"answer":3},{"id":6,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT region FROM payments WHERE tier > 0 GROUP BY region HAVING COUNT(*) > 1 ORDER BY region;","options":["FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","WHERE → FROM → GROUP BY → HAVING","SELECT → FROM → WHERE → GROUP BY","ORDER BY is applied before GROUP BY"],"answer":0},{"id":7,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT sku FROM subs) UNION ALL (SELECT sku FROM visitors);","options":["UNION ALL sorts by sku; UNION does not","UNION ALL keeps duplicate rows; UNION removes them","UNION ALL removes duplicates; UNION keeps them","UNION ALL requires identical column names"],"answer":1},{"id":8,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT score, ROW_NUMBER() OVER (PARTITION BY price) rn FROM tickets;","options":["Runs, but row order within each partition is non-deterministic","Behaves like RANK() with gaps","Always numbers rows by physical storage order","Syntax error — ORDER BY is mandatory inside OVER"],"answer":0},{"id":9,"type":"theoretical","topic":"window","q":"What does LAG(flag, 1) return for each row?\nSELECT kind, LAG(flag, 1) OVER (ORDER BY kind) prev FROM shipments;","options":["The value from the next row ahead","The first flag in the partition for every row","A running sum of all prior flag values","The value of flag from the previous row in ORDER BY sequence"],"answer":3},{"id":10,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.tier = o.tier);","options":["When the subquery returns at least one row for that outer row","When the subquery returns exactly one column","When COUNT(*) inside the subquery exceeds zero","When IN would return FALSE"],"answer":0},{"id":11,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(qty) and COUNT(*)?\nSELECT COUNT(qty), COUNT(*) FROM clicks;","options":["They are always identical","COUNT(qty) skips NULLs; COUNT(*) counts every row","COUNT(qty) counts only distinct non-NULL values","COUNT(*) skips NULLs; COUNT(qty) counts every row"],"answer":1},{"id":12,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["TRUE","UNKNOWN (a value distinct from NULL in the SQL standard)","FALSE","NULL"],"answer":0},{"id":13,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT kind FROM accounts),\n     b AS (SELECT * FROM a WHERE kind > 0)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","Only if both are declared RECURSIVE","No — only base table accounts may be referenced","Yes — b may reference a defined earlier in the same WITH"],"answer":3},{"id":14,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(code) = 'X';","options":["It forces the optimizer to use a sort-merge join","UPPER() always improves index efficiency","Applying UPPER() to code prevents a plain index seek on code","Functions are required for case-insensitive searches"],"answer":2},{"id":15,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["They are identical in all databases","TRUNCATE removes one row at a time","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","DELETE cannot use a WHERE clause"],"answer":2},{"id":16,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.user_id = b.user_id;","options":["This syntax is illegal in SQL:1999+","Cannot represent outer joins","Forgetting the WHERE condition produces a silent Cartesian product","Old-style joins are always faster than explicit JOIN"],"answer":2},{"id":17,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Only valid when two or more tables appear in FROM","HAVING without GROUP BY is the same as WHERE","Always a syntax error in standard SQL","Often yes — the whole table is treated as one implicit group"],"answer":3},{"id":18,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","DENSE_RANK leaves gaps; RANK does not","They behave identically for all inputs","Only RANK supports PARTITION BY"],"answer":0},{"id":19,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT status FROM products) AS x;","options":["When the outer query has no FROM clause","When the subquery returns more than one row","When the subquery returns zero rows","When the subquery contains GROUP BY"],"answer":1},{"id":20,"type":"theoretical","topic":"indexes","q":"Composite index on (created_at, flag) — which filter uses the index for a seek?","options":["WHERE created_at = ? — satisfies the leftmost-prefix rule","WHERE flag LIKE '%x%' alone","ORDER BY flag without any filter on created_at","WHERE flag = ? alone — skips the leading column"],"answer":0},{"id":21,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT region, tier FROM invoices;","options":["Only the first column region","Primary-key rows only","Each column independently in two passes","The combined row (region, tier) — the pair must be unique"],"answer":3},{"id":22,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(sku) FROM coupons WHERE qty IS NULL; -- assume 0 rows match","options":["NULL — aggregating an empty set returns NULL","Zero rows are returned","An error — MAX on empty set is undefined","0 (zero)"],"answer":0},{"id":23,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["INNER JOIN always returns at least one row","No rows satisfy the ON predicate (including when NULLs prevent matches)","INNER JOIN ignores the WHERE clause","NULL in ON always evaluates to TRUE"],"answer":1},{"id":24,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT flag FROM metrics) EXCEPT (SELECT flag FROM tickets);","options":["Rows in metrics not present in tickets","Rows in tickets not present in metrics","All rows from both queries combined","Rows present in both metrics and tickets"],"answer":0},{"id":25,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT tier, NTILE(4) OVER (ORDER BY code) q FROM flags;","options":["Returns fractional percentiles 0.25 … 1.0","Keeps only the top 4 rows","Requires PARTITION BY tier or it errors","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)"],"answer":3},{"id":26,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE qty = NULL;","options":["NULL cannot appear after an equality operator","Nothing; it returns rows where qty is NULL","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","The engine treats NULL as 0 for comparisons"],"answer":2},{"id":27,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT price, user_id, COUNT(*) FROM users GROUP BY price;","options":["COUNT(*) is not allowed alongside GROUP BY","GROUP BY must list every column in the table","user_id is not in GROUP BY and not functionally dependent on price","Two non-aggregate columns cannot precede COUNT"],"answer":2},{"id":28,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(kind) > 10, where should that condition go?\nSELECT order_id, AVG(kind) FROM events GROUP BY order_id","options":["It cannot be expressed without a subquery","LIMIT with a sort by AVG replaces the filter","HAVING AVG(kind) > 10 — after GROUP BY","WHERE AVG(kind) > 10 — before GROUP BY"],"answer":2},{"id":29,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.code = b.code;","options":["Only rows matching on both sides are returned","Unmatched accounts rows are kept; matched skus rows are dropped","A Cartesian product is produced","Every row from skus appears; unmatched accounts columns are NULL"],"answer":3},{"id":30,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Every row of sessions paired with every row of campaigns — Cartesian product","An error unless an ON clause is provided","Only rows with equal primary keys","Duplicate-free union of both tables"],"answer":0},{"id":31,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT user_id FROM payments WHERE created_at > 0 GROUP BY user_id HAVING COUNT(*) > 1 ORDER BY user_id;","options":["ORDER BY is applied before GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","SELECT → FROM → WHERE → GROUP BY","WHERE → FROM → GROUP BY → HAVING"],"answer":1},{"id":32,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT order_id FROM subs) UNION ALL (SELECT order_id FROM visitors);","options":["UNION ALL keeps duplicate rows; UNION removes them","UNION ALL requires identical column names","UNION ALL sorts by order_id; UNION does not","UNION ALL removes duplicates; UNION keeps them"],"answer":0},{"id":33,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT amount, ROW_NUMBER() OVER (PARTITION BY sku) rn FROM tickets;","options":["Always numbers rows by physical storage order","Behaves like RANK() with gaps","Syntax error — ORDER BY is mandatory inside OVER","Runs, but row order within each partition is non-deterministic"],"answer":3},{"id":34,"type":"theoretical","topic":"window","q":"What does LAG(status, 1) return for each row?\nSELECT score, LAG(status, 1) OVER (ORDER BY score) prev FROM shipments;","options":["The value of status from the previous row in ORDER BY sequence","The value from the next row ahead","A running sum of all prior status values","The first status in the partition for every row"],"answer":0},{"id":35,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.created_at = o.created_at);","options":["When the subquery returns exactly one column","When the subquery returns at least one row for that outer row","When IN would return FALSE","When COUNT(*) inside the subquery exceeds zero"],"answer":1},{"id":36,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(region) and COUNT(*)?\nSELECT COUNT(region), COUNT(*) FROM clicks;","options":["COUNT(region) skips NULLs; COUNT(*) counts every row","COUNT(region) counts only distinct non-NULL values","COUNT(*) skips NULLs; COUNT(region) counts every row","They are always identical"],"answer":0},{"id":37,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["NULL","UNKNOWN (a value distinct from NULL in the SQL standard)","FALSE","TRUE"],"answer":3},{"id":38,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT score FROM accounts),\n     b AS (SELECT * FROM a WHERE score > 0)\nSELECT * FROM b;","options":["Only if both are declared RECURSIVE","No — CTEs cannot reference each other","Yes — b may reference a defined earlier in the same WITH","No — only base table accounts may be referenced"],"answer":2},{"id":39,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(flag) = 'X';","options":["UPPER() always improves index efficiency","Functions are required for case-insensitive searches","Applying UPPER() to flag prevents a plain index seek on flag","It forces the optimizer to use a sort-merge join"],"answer":2},{"id":40,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["DELETE cannot use a WHERE clause","TRUNCATE removes one row at a time","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","They are identical in all databases"],"answer":2},{"id":41,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.qty = b.qty;","options":["Old-style joins are always faster than explicit JOIN","This syntax is illegal in SQL:1999+","Cannot represent outer joins","Forgetting the WHERE condition produces a silent Cartesian product"],"answer":3},{"id":42,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Often yes — the whole table is treated as one implicit group","Only valid when two or more tables appear in FROM","Always a syntax error in standard SQL","HAVING without GROUP BY is the same as WHERE"],"answer":0},{"id":43,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["Only RANK supports PARTITION BY","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","They behave identically for all inputs","DENSE_RANK leaves gaps; RANK does not"],"answer":1},{"id":44,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT code FROM products) AS x;","options":["When the subquery returns more than one row","When the subquery contains GROUP BY","When the outer query has no FROM clause","When the subquery returns zero rows"],"answer":0},{"id":45,"type":"theoretical","topic":"indexes","q":"Composite index on (name, status) — which filter uses the index for a seek?","options":["WHERE status LIKE '%x%' alone","ORDER BY status without any filter on name","WHERE status = ? alone — skips the leading column","WHERE name = ? — satisfies the leftmost-prefix rule"],"answer":3},{"id":46,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["The combined row (user_id, created_at) — the pair must be unique","Each column independently in two passes","Only the first column user_id","Primary-key rows only"],"answer":0},{"id":47,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(order_id) FROM coupons WHERE region IS NULL; -- assume 0 rows match","options":["Zero rows are returned","NULL — aggregating an empty set returns NULL","0 (zero)","An error — MAX on empty set is undefined"],"answer":1},{"id":48,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["No rows satisfy the ON predicate (including when NULLs prevent matches)","INNER JOIN ignores the WHERE clause","NULL in ON always evaluates to TRUE","INNER JOIN always returns at least one row"],"answer":0},{"id":49,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT status FROM metrics) EXCEPT (SELECT status FROM tickets);","options":["Rows present in both metrics and tickets","Rows in tickets not present in metrics","All rows from both queries combined","Rows in metrics not present in tickets"],"answer":3},{"id":50,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) q FROM flags;","options":["Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Returns fractional percentiles 0.25 … 1.0","Keeps only the top 4 rows","Requires PARTITION BY created_at or it errors"],"answer":0},{"id":51,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE region = NULL;","options":["Nothing; it returns rows where region is NULL","The engine treats NULL as 0 for comparisons","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","NULL cannot appear after an equality operator"],"answer":2},{"id":52,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT sku, qty, COUNT(*) FROM users GROUP BY sku;","options":["Two non-aggregate columns cannot precede COUNT","GROUP BY must list every column in the table","qty is not in GROUP BY and not functionally dependent on sku","COUNT(*) is not allowed alongside GROUP BY"],"answer":2},{"id":53,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(score) > 10, where should that condition go?\nSELECT price, AVG(score) FROM events GROUP BY price","options":["WHERE AVG(score) > 10 — before GROUP BY","It cannot be expressed without a subquery","LIMIT with a sort by AVG replaces the filter","HAVING AVG(score) > 10 — after GROUP BY"],"answer":3},{"id":54,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.flag = b.flag;","options":["Unmatched accounts rows are kept; matched skus rows are dropped","Only rows matching on both sides are returned","Every row from skus appears; unmatched accounts columns are NULL","A Cartesian product is produced"],"answer":2},{"id":55,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Duplicate-free union of both tables","Every row of sessions paired with every row of campaigns — Cartesian product","Only rows with equal primary keys","An error unless an ON clause is provided"],"answer":1},{"id":56,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT qty FROM payments WHERE name > 0 GROUP BY qty HAVING COUNT(*) > 1 ORDER BY qty;","options":["FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","WHERE → FROM → GROUP BY → HAVING","ORDER BY is applied before GROUP BY","SELECT → FROM → WHERE → GROUP BY"],"answer":0},{"id":57,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT price FROM subs) UNION ALL (SELECT price FROM visitors);","options":["UNION ALL requires identical column names","UNION ALL sorts by price; UNION does not","UNION ALL removes duplicates; UNION keeps them","UNION ALL keeps duplicate rows; UNION removes them"],"answer":3},{"id":58,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT kind, ROW_NUMBER() OVER (PARTITION BY order_id) rn FROM tickets;","options":["Runs, but row order within each partition is non-deterministic","Always numbers rows by physical storage order","Syntax error — ORDER BY is mandatory inside OVER","Behaves like RANK() with gaps"],"answer":0},{"id":59,"type":"theoretical","topic":"window","q":"What does LAG(code, 1) return for each row?\nSELECT amount, LAG(code, 1) OVER (ORDER BY amount) prev FROM shipments;","options":["The value from the next row ahead","The value of code from the previous row in ORDER BY sequence","The first code in the partition for every row","A running sum of all prior code values"],"answer":1},{"id":60,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.name = o.name);","options":["When the subquery returns at least one row for that outer row","When IN would return FALSE","When COUNT(*) inside the subquery exceeds zero","When the subquery returns exactly one column"],"answer":0},{"id":61,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(user_id) and COUNT(*)?\nSELECT COUNT(user_id), COUNT(*) FROM clicks;","options":["They are always identical","COUNT(user_id) counts only distinct non-NULL values","COUNT(*) skips NULLs; COUNT(user_id) counts every row","COUNT(user_id) skips NULLs; COUNT(*) counts every row"],"answer":3},{"id":62,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["TRUE","NULL","FALSE","UNKNOWN (a value distinct from NULL in the SQL standard)"],"answer":0},{"id":63,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT amount FROM accounts),\n     b AS (SELECT * FROM a WHERE amount > 0)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","Yes — b may reference a defined earlier in the same WITH","No — only base table accounts may be referenced","Only if both are declared RECURSIVE"],"answer":1},{"id":64,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(status) = 'X';","options":["It forces the optimizer to use a sort-merge join","Functions are required for case-insensitive searches","Applying UPPER() to status prevents a plain index seek on status","UPPER() always improves index efficiency"],"answer":2},{"id":65,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["They are identical in all databases","DELETE cannot use a WHERE clause","TRUNCATE removes one row at a time","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE"],"answer":3},{"id":66,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.region = b.region;","options":["This syntax is illegal in SQL:1999+","Old-style joins are always faster than explicit JOIN","Forgetting the WHERE condition produces a silent Cartesian product","Cannot represent outer joins"],"answer":2},{"id":67,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Always a syntax error in standard SQL","HAVING without GROUP BY is the same as WHERE","Often yes — the whole table is treated as one implicit group","Only valid when two or more tables appear in FROM"],"answer":2},{"id":68,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","DENSE_RANK leaves gaps; RANK does not","Only RANK supports PARTITION BY","They behave identically for all inputs"],"answer":0},{"id":69,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT flag FROM products) AS x;","options":["When the subquery contains GROUP BY","When the outer query has no FROM clause","When the subquery returns zero rows","When the subquery returns more than one row"],"answer":3},{"id":70,"type":"theoretical","topic":"indexes","q":"Composite index on (tier, code) — which filter uses the index for a seek?","options":["WHERE tier = ? — satisfies the leftmost-prefix rule","WHERE code LIKE '%x%' alone","WHERE code = ? alone — skips the leading column","ORDER BY code without any filter on tier"],"answer":0},{"id":71,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT qty, name FROM invoices;","options":["Primary-key rows only","The combined row (qty, name) — the pair must be unique","Only the first column qty","Each column independently in two passes"],"answer":1},{"id":72,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(price) FROM coupons WHERE user_id IS NULL; -- assume 0 rows match","options":["NULL — aggregating an empty set returns NULL","0 (zero)","An error — MAX on empty set is undefined","Zero rows are returned"],"answer":0},{"id":73,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["INNER JOIN always returns at least one row","INNER JOIN ignores the WHERE clause","NULL in ON always evaluates to TRUE","No rows satisfy the ON predicate (including when NULLs prevent matches)"],"answer":3},{"id":74,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT code FROM metrics) EXCEPT (SELECT code FROM tickets);","options":["Rows in metrics not present in tickets","Rows present in both metrics and tickets","All rows from both queries combined","Rows in tickets not present in metrics"],"answer":0},{"id":75,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT name, NTILE(4) OVER (ORDER BY status) q FROM flags;","options":["Returns fractional percentiles 0.25 … 1.0","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Requires PARTITION BY name or it errors","Keeps only the top 4 rows"],"answer":1},{"id":76,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE user_id = NULL;","options":["NULL cannot appear after an equality operator","The engine treats NULL as 0 for comparisons","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","Nothing; it returns rows where user_id is NULL"],"answer":2},{"id":77,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT order_id, region, COUNT(*) FROM users GROUP BY order_id;","options":["COUNT(*) is not allowed alongside GROUP BY","Two non-aggregate columns cannot precede COUNT","GROUP BY must list every column in the table","region is not in GROUP BY and not functionally dependent on order_id"],"answer":3},{"id":78,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(amount) > 10, where should that condition go?\nSELECT sku, AVG(amount) FROM events GROUP BY sku","options":["It cannot be expressed without a subquery","WHERE AVG(amount) > 10 — before GROUP BY","HAVING AVG(amount) > 10 — after GROUP BY","LIMIT with a sort by AVG replaces the filter"],"answer":2},{"id":79,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.status = b.status;","options":["Only rows matching on both sides are returned","A Cartesian product is produced","Every row from skus appears; unmatched accounts columns are NULL","Unmatched accounts rows are kept; matched skus rows are dropped"],"answer":2},{"id":80,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["An error unless an ON clause is provided","Duplicate-free union of both tables","Every row of sessions paired with every row of campaigns — Cartesian product","Only rows with equal primary keys"],"answer":2},{"id":81,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT region FROM payments WHERE tier > 0 GROUP BY region HAVING COUNT(*) > 1 ORDER BY region;","options":["WHERE → FROM → GROUP BY → HAVING","ORDER BY is applied before GROUP BY","SELECT → FROM → WHERE → GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY"],"answer":3},{"id":82,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT sku FROM subs) UNION ALL (SELECT sku FROM visitors);","options":["UNION ALL keeps duplicate rows; UNION removes them","UNION ALL requires identical column names","UNION ALL removes duplicates; UNION keeps them","UNION ALL sorts by sku; UNION does not"],"answer":0},{"id":83,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT score, ROW_NUMBER() OVER (PARTITION BY price) rn FROM tickets;","options":["Behaves like RANK() with gaps","Runs, but row order within each partition is non-deterministic","Syntax error — ORDER BY is mandatory inside OVER","Always numbers rows by physical storage order"],"answer":1},{"id":84,"type":"theoretical","topic":"window","q":"What does LAG(flag, 1) return for each row?\nSELECT kind, LAG(flag, 1) OVER (ORDER BY kind) prev FROM shipments;","options":["The value of flag from the previous row in ORDER BY sequence","A running sum of all prior flag values","The first flag in the partition for every row","The value from the next row ahead"],"answer":0},{"id":85,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.tier = o.tier);","options":["When the subquery returns exactly one column","When IN would return FALSE","When COUNT(*) inside the subquery exceeds zero","When the subquery returns at least one row for that outer row"],"answer":3},{"id":86,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(qty) and COUNT(*)?\nSELECT COUNT(qty), COUNT(*) FROM clicks;","options":["COUNT(qty) skips NULLs; COUNT(*) counts every row","They are always identical","COUNT(*) skips NULLs; COUNT(qty) counts every row","COUNT(qty) counts only distinct non-NULL values"],"answer":0},{"id":87,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["NULL","TRUE","UNKNOWN (a value distinct from NULL in the SQL standard)","FALSE"],"answer":1},{"id":88,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT kind FROM accounts),\n     b AS (SELECT * FROM a WHERE kind > 0)\nSELECT * FROM b;","options":["Yes — b may reference a defined earlier in the same WITH","No — only base table accounts may be referenced","Only if both are declared RECURSIVE","No — CTEs cannot reference each other"],"answer":0},{"id":89,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(code) = 'X';","options":["UPPER() always improves index efficiency","It forces the optimizer to use a sort-merge join","Functions are required for case-insensitive searches","Applying UPPER() to code prevents a plain index seek on code"],"answer":3},{"id":90,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["DELETE cannot use a WHERE clause","They are identical in all databases","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","TRUNCATE removes one row at a time"],"answer":2},{"id":91,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.user_id = b.user_id;","options":["Old-style joins are always faster than explicit JOIN","Cannot represent outer joins","Forgetting the WHERE condition produces a silent Cartesian product","This syntax is illegal in SQL:1999+"],"answer":2},{"id":92,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Only valid when two or more tables appear in FROM","HAVING without GROUP BY is the same as WHERE","Often yes — the whole table is treated as one implicit group","Always a syntax error in standard SQL"],"answer":2},{"id":93,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["DENSE_RANK leaves gaps; RANK does not","Only RANK supports PARTITION BY","They behave identically for all inputs","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers"],"answer":3},{"id":94,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT status FROM products) AS x;","options":["When the subquery returns more than one row","When the subquery contains GROUP BY","When the subquery returns zero rows","When the outer query has no FROM clause"],"answer":0},{"id":95,"type":"theoretical","topic":"indexes","q":"Composite index on (created_at, flag) — which filter uses the index for a seek?","options":["ORDER BY flag without any filter on created_at","WHERE created_at = ? — satisfies the leftmost-prefix rule","WHERE flag = ? alone — skips the leading column","WHERE flag LIKE '%x%' alone"],"answer":1},{"id":96,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT region, tier FROM invoices;","options":["The combined row (region, tier) — the pair must be unique","Each column independently in two passes","Primary-key rows only","Only the first column region"],"answer":0},{"id":97,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(sku) FROM coupons WHERE qty IS NULL; -- assume 0 rows match","options":["An error — MAX on empty set is undefined","0 (zero)","Zero rows are returned","NULL — aggregating an empty set returns NULL"],"answer":3},{"id":98,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["No rows satisfy the ON predicate (including when NULLs prevent matches)","INNER JOIN always returns at least one row","NULL in ON always evaluates to TRUE","INNER JOIN ignores the WHERE clause"],"answer":0},{"id":99,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT flag FROM metrics) EXCEPT (SELECT flag FROM tickets);","options":["Rows present in both metrics and tickets","Rows in metrics not present in tickets","Rows in tickets not present in metrics","All rows from both queries combined"],"answer":1},{"id":100,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT tier, NTILE(4) OVER (ORDER BY code) q FROM flags;","options":["Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Requires PARTITION BY tier or it errors","Keeps only the top 4 rows","Returns fractional percentiles 0.25 … 1.0"],"answer":0},{"id":101,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE qty = NULL;","options":["Nothing; it returns rows where qty is NULL","The engine treats NULL as 0 for comparisons","NULL cannot appear after an equality operator","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE"],"answer":3},{"id":102,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT price, user_id, COUNT(*) FROM users GROUP BY price;","options":["Two non-aggregate columns cannot precede COUNT","COUNT(*) is not allowed alongside GROUP BY","user_id is not in GROUP BY and not functionally dependent on price","GROUP BY must list every column in the table"],"answer":2},{"id":103,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(kind) > 10, where should that condition go?\nSELECT order_id, AVG(kind) FROM events GROUP BY order_id","options":["WHERE AVG(kind) > 10 — before GROUP BY","LIMIT with a sort by AVG replaces the filter","HAVING AVG(kind) > 10 — after GROUP BY","It cannot be expressed without a subquery"],"answer":2},{"id":104,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.code = b.code;","options":["Unmatched accounts rows are kept; matched skus rows are dropped","A Cartesian product is produced","Every row from skus appears; unmatched accounts columns are NULL","Only rows matching on both sides are returned"],"answer":2},{"id":105,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Only rows with equal primary keys","An error unless an ON clause is provided","Duplicate-free union of both tables","Every row of sessions paired with every row of campaigns — Cartesian product"],"answer":3},{"id":106,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT user_id FROM payments WHERE created_at > 0 GROUP BY user_id HAVING COUNT(*) > 1 ORDER BY user_id;","options":["FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","WHERE → FROM → GROUP BY → HAVING","SELECT → FROM → WHERE → GROUP BY","ORDER BY is applied before GROUP BY"],"answer":0},{"id":107,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT order_id FROM subs) UNION ALL (SELECT order_id FROM visitors);","options":["UNION ALL sorts by order_id; UNION does not","UNION ALL keeps duplicate rows; UNION removes them","UNION ALL removes duplicates; UNION keeps them","UNION ALL requires identical column names"],"answer":1},{"id":108,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT amount, ROW_NUMBER() OVER (PARTITION BY sku) rn FROM tickets;","options":["Runs, but row order within each partition is non-deterministic","Always numbers rows by physical storage order","Behaves like RANK() with gaps","Syntax error — ORDER BY is mandatory inside OVER"],"answer":0},{"id":109,"type":"theoretical","topic":"window","q":"What does LAG(status, 1) return for each row?\nSELECT score, LAG(status, 1) OVER (ORDER BY score) prev FROM shipments;","options":["A running sum of all prior status values","The first status in the partition for every row","The value from the next row ahead","The value of status from the previous row in ORDER BY sequence"],"answer":3},{"id":110,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.created_at = o.created_at);","options":["When the subquery returns at least one row for that outer row","When COUNT(*) inside the subquery exceeds zero","When the subquery returns exactly one column","When IN would return FALSE"],"answer":0},{"id":111,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(region) and COUNT(*)?\nSELECT COUNT(region), COUNT(*) FROM clicks;","options":["They are always identical","COUNT(region) skips NULLs; COUNT(*) counts every row","COUNT(region) counts only distinct non-NULL values","COUNT(*) skips NULLs; COUNT(region) counts every row"],"answer":1},{"id":112,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["TRUE","UNKNOWN (a value distinct from NULL in the SQL standard)","FALSE","NULL"],"answer":0},{"id":113,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT score FROM accounts),\n     b AS (SELECT * FROM a WHERE score > 0)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","No — only base table accounts may be referenced","Only if both are declared RECURSIVE","Yes — b may reference a defined earlier in the same WITH"],"answer":3},{"id":114,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(flag) = 'X';","options":["Applying UPPER() to flag prevents a plain index seek on flag","UPPER() always improves index efficiency","It forces the optimizer to use a sort-merge join","Functions are required for case-insensitive searches"],"answer":0},{"id":115,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["They are identical in all databases","TRUNCATE removes one row at a time","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","DELETE cannot use a WHERE clause"],"answer":2},{"id":116,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.qty = b.qty;","options":["This syntax is illegal in SQL:1999+","Cannot represent outer joins","Forgetting the WHERE condition produces a silent Cartesian product","Old-style joins are always faster than explicit JOIN"],"answer":2},{"id":117,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Always a syntax error in standard SQL","Only valid when two or more tables appear in FROM","HAVING without GROUP BY is the same as WHERE","Often yes — the whole table is treated as one implicit group"],"answer":3},{"id":118,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["DENSE_RANK leaves gaps; RANK does not","They behave identically for all inputs","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","Only RANK supports PARTITION BY"],"answer":2},{"id":119,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT code FROM products) AS x;","options":["When the outer query has no FROM clause","When the subquery returns more than one row","When the subquery returns zero rows","When the subquery contains GROUP BY"],"answer":1},{"id":120,"type":"theoretical","topic":"indexes","q":"Composite index on (name, status) — which filter uses the index for a seek?","options":["WHERE name = ? — satisfies the leftmost-prefix rule","WHERE status LIKE '%x%' alone","ORDER BY status without any filter on name","WHERE status = ? alone — skips the leading column"],"answer":0},{"id":121,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["Each column independently in two passes","Primary-key rows only","Only the first column user_id","The combined row (user_id, created_at) — the pair must be unique"],"answer":3},{"id":122,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(order_id) FROM coupons WHERE region IS NULL; -- assume 0 rows match","options":["NULL — aggregating an empty set returns NULL","An error — MAX on empty set is undefined","Zero rows are returned","0 (zero)"],"answer":0},{"id":123,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["INNER JOIN always returns at least one row","No rows satisfy the ON predicate (including when NULLs prevent matches)","INNER JOIN ignores the WHERE clause","NULL in ON always evaluates to TRUE"],"answer":1},{"id":124,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT status FROM metrics) EXCEPT (SELECT status FROM tickets);","options":["Rows in metrics not present in tickets","Rows in tickets not present in metrics","All rows from both queries combined","Rows present in both metrics and tickets"],"answer":0},{"id":125,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) q FROM flags;","options":["Returns fractional percentiles 0.25 … 1.0","Requires PARTITION BY created_at or it errors","Keeps only the top 4 rows","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)"],"answer":3},{"id":126,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE region = NULL;","options":["Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","Nothing; it returns rows where region is NULL","NULL cannot appear after an equality operator","The engine treats NULL as 0 for comparisons"],"answer":0},{"id":127,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT sku, qty, COUNT(*) FROM users GROUP BY sku;","options":["COUNT(*) is not allowed alongside GROUP BY","qty is not in GROUP BY and not functionally dependent on sku","GROUP BY must list every column in the table","Two non-aggregate columns cannot precede COUNT"],"answer":1},{"id":128,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(score) > 10, where should that condition go?\nSELECT price, AVG(score) FROM events GROUP BY price","options":["It cannot be expressed without a subquery","LIMIT with a sort by AVG replaces the filter","HAVING AVG(score) > 10 — after GROUP BY","WHERE AVG(score) > 10 — before GROUP BY"],"answer":2},{"id":129,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.flag = b.flag;","options":["Only rows matching on both sides are returned","Unmatched accounts rows are kept; matched skus rows are dropped","A Cartesian product is produced","Every row from skus appears; unmatched accounts columns are NULL"],"answer":3},{"id":130,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["An error unless an ON clause is provided","Only rows with equal primary keys","Every row of sessions paired with every row of campaigns — Cartesian product","Duplicate-free union of both tables"],"answer":2},{"id":131,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT qty FROM payments WHERE name > 0 GROUP BY qty HAVING COUNT(*) > 1 ORDER BY qty;","options":["SELECT → FROM → WHERE → GROUP BY","ORDER BY is applied before GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","WHERE → FROM → GROUP BY → HAVING"],"answer":2},{"id":132,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT price FROM subs) UNION ALL (SELECT price FROM visitors);","options":["UNION ALL keeps duplicate rows; UNION removes them","UNION ALL requires identical column names","UNION ALL sorts by price; UNION does not","UNION ALL removes duplicates; UNION keeps them"],"answer":0},{"id":133,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT kind, ROW_NUMBER() OVER (PARTITION BY order_id) rn FROM tickets;","options":["Always numbers rows by physical storage order","Behaves like RANK() with gaps","Syntax error — ORDER BY is mandatory inside OVER","Runs, but row order within each partition is non-deterministic"],"answer":3},{"id":134,"type":"theoretical","topic":"window","q":"What does LAG(code, 1) return for each row?\nSELECT amount, LAG(code, 1) OVER (ORDER BY amount) prev FROM shipments;","options":["The value of code from the previous row in ORDER BY sequence","A running sum of all prior code values","The value from the next row ahead","The first code in the partition for every row"],"answer":0},{"id":135,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.name = o.name);","options":["When IN would return FALSE","When the subquery returns at least one row for that outer row","When the subquery returns exactly one column","When COUNT(*) inside the subquery exceeds zero"],"answer":1},{"id":136,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(user_id) and COUNT(*)?\nSELECT COUNT(user_id), COUNT(*) FROM clicks;","options":["COUNT(user_id) skips NULLs; COUNT(*) counts every row","COUNT(user_id) counts only distinct non-NULL values","COUNT(*) skips NULLs; COUNT(user_id) counts every row","They are always identical"],"answer":0},{"id":137,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["NULL","UNKNOWN (a value distinct from NULL in the SQL standard)","FALSE","TRUE"],"answer":3},{"id":138,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT amount FROM accounts),\n     b AS (SELECT * FROM a WHERE amount > 0)\nSELECT * FROM b;","options":["Yes — b may reference a defined earlier in the same WITH","No — CTEs cannot reference each other","Only if both are declared RECURSIVE","No — only base table accounts may be referenced"],"answer":0},{"id":139,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(status) = 'X';","options":["UPPER() always improves index efficiency","Applying UPPER() to status prevents a plain index seek on status","Functions are required for case-insensitive searches","It forces the optimizer to use a sort-merge join"],"answer":1},{"id":140,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["DELETE cannot use a WHERE clause","TRUNCATE removes one row at a time","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","They are identical in all databases"],"answer":2},{"id":141,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.region = b.region;","options":["Old-style joins are always faster than explicit JOIN","This syntax is illegal in SQL:1999+","Cannot represent outer joins","Forgetting the WHERE condition produces a silent Cartesian product"],"answer":3},{"id":142,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Only valid when two or more tables appear in FROM","Always a syntax error in standard SQL","Often yes — the whole table is treated as one implicit group","HAVING without GROUP BY is the same as WHERE"],"answer":2},{"id":143,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["They behave identically for all inputs","Only RANK supports PARTITION BY","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","DENSE_RANK leaves gaps; RANK does not"],"answer":2},{"id":144,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT flag FROM products) AS x;","options":["When the subquery contains GROUP BY","When the outer query has no FROM clause","When the subquery returns more than one row","When the subquery returns zero rows"],"answer":2},{"id":145,"type":"theoretical","topic":"indexes","q":"Composite index on (tier, code) — which filter uses the index for a seek?","options":["WHERE code LIKE '%x%' alone","ORDER BY code without any filter on tier","WHERE code = ? alone — skips the leading column","WHERE tier = ? — satisfies the leftmost-prefix rule"],"answer":3},{"id":146,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT qty, name FROM invoices;","options":["The combined row (qty, name) — the pair must be unique","Each column independently in two passes","Only the first column qty","Primary-key rows only"],"answer":0},{"id":147,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(price) FROM coupons WHERE user_id IS NULL; -- assume 0 rows match","options":["0 (zero)","NULL — aggregating an empty set returns NULL","Zero rows are returned","An error — MAX on empty set is undefined"],"answer":1},{"id":148,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["No rows satisfy the ON predicate (including when NULLs prevent matches)","NULL in ON always evaluates to TRUE","INNER JOIN ignores the WHERE clause","INNER JOIN always returns at least one row"],"answer":0},{"id":149,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT code FROM metrics) EXCEPT (SELECT code FROM tickets);","options":["Rows present in both metrics and tickets","Rows in tickets not present in metrics","All rows from both queries combined","Rows in metrics not present in tickets"],"answer":3},{"id":150,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT name, NTILE(4) OVER (ORDER BY status) q FROM flags;","options":["Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Returns fractional percentiles 0.25 … 1.0","Keeps only the top 4 rows","Requires PARTITION BY name or it errors"],"answer":0},{"id":151,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE user_id = NULL;","options":["Nothing; it returns rows where user_id is NULL","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","The engine treats NULL as 0 for comparisons","NULL cannot appear after an equality operator"],"answer":1},{"id":152,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT order_id, region, COUNT(*) FROM users GROUP BY order_id;","options":["region is not in GROUP BY and not functionally dependent on order_id","GROUP BY must list every column in the table","Two non-aggregate columns cannot precede COUNT","COUNT(*) is not allowed alongside GROUP BY"],"answer":0},{"id":153,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(amount) > 10, where should that condition go?\nSELECT sku, AVG(amount) FROM events GROUP BY sku","options":["WHERE AVG(amount) > 10 — before GROUP BY","It cannot be expressed without a subquery","LIMIT with a sort by AVG replaces the filter","HAVING AVG(amount) > 10 — after GROUP BY"],"answer":3},{"id":154,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.status = b.status;","options":["Unmatched accounts rows are kept; matched skus rows are dropped","Only rows matching on both sides are returned","Every row from skus appears; unmatched accounts columns are NULL","A Cartesian product is produced"],"answer":2},{"id":155,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Only rows with equal primary keys","Duplicate-free union of both tables","Every row of sessions paired with every row of campaigns — Cartesian product","An error unless an ON clause is provided"],"answer":2},{"id":156,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT region FROM payments WHERE tier > 0 GROUP BY region HAVING COUNT(*) > 1 ORDER BY region;","options":["WHERE → FROM → GROUP BY → HAVING","ORDER BY is applied before GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","SELECT → FROM → WHERE → GROUP BY"],"answer":2},{"id":157,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT sku FROM subs) UNION ALL (SELECT sku FROM visitors);","options":["UNION ALL requires identical column names","UNION ALL sorts by sku; UNION does not","UNION ALL removes duplicates; UNION keeps them","UNION ALL keeps duplicate rows; UNION removes them"],"answer":3},{"id":158,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT score, ROW_NUMBER() OVER (PARTITION BY price) rn FROM tickets;","options":["Runs, but row order within each partition is non-deterministic","Always numbers rows by physical storage order","Syntax error — ORDER BY is mandatory inside OVER","Behaves like RANK() with gaps"],"answer":0},{"id":159,"type":"theoretical","topic":"window","q":"What does LAG(flag, 1) return for each row?\nSELECT kind, LAG(flag, 1) OVER (ORDER BY kind) prev FROM shipments;","options":["The first flag in the partition for every row","The value of flag from the previous row in ORDER BY sequence","The value from the next row ahead","A running sum of all prior flag values"],"answer":1},{"id":160,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.tier = o.tier);","options":["When the subquery returns at least one row for that outer row","When COUNT(*) inside the subquery exceeds zero","When IN would return FALSE","When the subquery returns exactly one column"],"answer":0},{"id":161,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(qty) and COUNT(*)?\nSELECT COUNT(qty), COUNT(*) FROM clicks;","options":["COUNT(*) skips NULLs; COUNT(qty) counts every row","COUNT(qty) counts only distinct non-NULL values","They are always identical","COUNT(qty) skips NULLs; COUNT(*) counts every row"],"answer":3},{"id":162,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["TRUE","NULL","FALSE","UNKNOWN (a value distinct from NULL in the SQL standard)"],"answer":0},{"id":163,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT kind FROM accounts),\n     b AS (SELECT * FROM a WHERE kind > 0)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","Yes — b may reference a defined earlier in the same WITH","No — only base table accounts may be referenced","Only if both are declared RECURSIVE"],"answer":1},{"id":164,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(code) = 'X';","options":["Applying UPPER() to code prevents a plain index seek on code","Functions are required for case-insensitive searches","It forces the optimizer to use a sort-merge join","UPPER() always improves index efficiency"],"answer":0},{"id":165,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["They are identical in all databases","TRUNCATE removes one row at a time","DELETE cannot use a WHERE clause","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE"],"answer":3},{"id":166,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.user_id = b.user_id;","options":["This syntax is illegal in SQL:1999+","Old-style joins are always faster than explicit JOIN","Forgetting the WHERE condition produces a silent Cartesian product","Cannot represent outer joins"],"answer":2},{"id":167,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Always a syntax error in standard SQL","HAVING without GROUP BY is the same as WHERE","Often yes — the whole table is treated as one implicit group","Only valid when two or more tables appear in FROM"],"answer":2},{"id":168,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["DENSE_RANK leaves gaps; RANK does not","Only RANK supports PARTITION BY","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","They behave identically for all inputs"],"answer":2},{"id":169,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT status FROM products) AS x;","options":["When the subquery returns zero rows","When the subquery contains GROUP BY","When the outer query has no FROM clause","When the subquery returns more than one row"],"answer":3},{"id":170,"type":"theoretical","topic":"indexes","q":"Composite index on (created_at, flag) — which filter uses the index for a seek?","options":["WHERE created_at = ? — satisfies the leftmost-prefix rule","WHERE flag LIKE '%x%' alone","WHERE flag = ? alone — skips the leading column","ORDER BY flag without any filter on created_at"],"answer":0},{"id":171,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT region, tier FROM invoices;","options":["Primary-key rows only","The combined row (region, tier) — the pair must be unique","Only the first column region","Each column independently in two passes"],"answer":1},{"id":172,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(sku) FROM coupons WHERE qty IS NULL; -- assume 0 rows match","options":["NULL — aggregating an empty set returns NULL","An error — MAX on empty set is undefined","0 (zero)","Zero rows are returned"],"answer":0},{"id":173,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["NULL in ON always evaluates to TRUE","INNER JOIN ignores the WHERE clause","INNER JOIN always returns at least one row","No rows satisfy the ON predicate (including when NULLs prevent matches)"],"answer":3},{"id":174,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT flag FROM metrics) EXCEPT (SELECT flag FROM tickets);","options":["Rows in metrics not present in tickets","Rows present in both metrics and tickets","All rows from both queries combined","Rows in tickets not present in metrics"],"answer":0},{"id":175,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT tier, NTILE(4) OVER (ORDER BY code) q FROM flags;","options":["Returns fractional percentiles 0.25 … 1.0","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Requires PARTITION BY tier or it errors","Keeps only the top 4 rows"],"answer":1},{"id":176,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE qty = NULL;","options":["Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","The engine treats NULL as 0 for comparisons","NULL cannot appear after an equality operator","Nothing; it returns rows where qty is NULL"],"answer":0},{"id":177,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT price, user_id, COUNT(*) FROM users GROUP BY price;","options":["COUNT(*) is not allowed alongside GROUP BY","GROUP BY must list every column in the table","Two non-aggregate columns cannot precede COUNT","user_id is not in GROUP BY and not functionally dependent on price"],"answer":3},{"id":178,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(kind) > 10, where should that condition go?\nSELECT order_id, AVG(kind) FROM events GROUP BY order_id","options":["HAVING AVG(kind) > 10 — after GROUP BY","WHERE AVG(kind) > 10 — before GROUP BY","It cannot be expressed without a subquery","LIMIT with a sort by AVG replaces the filter"],"answer":0},{"id":179,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.code = b.code;","options":["Only rows matching on both sides are returned","A Cartesian product is produced","Every row from skus appears; unmatched accounts columns are NULL","Unmatched accounts rows are kept; matched skus rows are dropped"],"answer":2},{"id":180,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["An error unless an ON clause is provided","Duplicate-free union of both tables","Every row of sessions paired with every row of campaigns — Cartesian product","Only rows with equal primary keys"],"answer":2},{"id":181,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT user_id FROM payments WHERE created_at > 0 GROUP BY user_id HAVING COUNT(*) > 1 ORDER BY user_id;","options":["SELECT → FROM → WHERE → GROUP BY","WHERE → FROM → GROUP BY → HAVING","ORDER BY is applied before GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY"],"answer":3},{"id":182,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT order_id FROM subs) UNION ALL (SELECT order_id FROM visitors);","options":["UNION ALL requires identical column names","UNION ALL removes duplicates; UNION keeps them","UNION ALL keeps duplicate rows; UNION removes them","UNION ALL sorts by order_id; UNION does not"],"answer":2},{"id":183,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT amount, ROW_NUMBER() OVER (PARTITION BY sku) rn FROM tickets;","options":["Behaves like RANK() with gaps","Runs, but row order within each partition is non-deterministic","Syntax error — ORDER BY is mandatory inside OVER","Always numbers rows by physical storage order"],"answer":1},{"id":184,"type":"theoretical","topic":"window","q":"What does LAG(status, 1) return for each row?\nSELECT score, LAG(status, 1) OVER (ORDER BY score) prev FROM shipments;","options":["The value of status from the previous row in ORDER BY sequence","A running sum of all prior status values","The first status in the partition for every row","The value from the next row ahead"],"answer":0},{"id":185,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.created_at = o.created_at);","options":["When COUNT(*) inside the subquery exceeds zero","When IN would return FALSE","When the subquery returns exactly one column","When the subquery returns at least one row for that outer row"],"answer":3},{"id":186,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(region) and COUNT(*)?\nSELECT COUNT(region), COUNT(*) FROM clicks;","options":["COUNT(region) skips NULLs; COUNT(*) counts every row","COUNT(*) skips NULLs; COUNT(region) counts every row","They are always identical","COUNT(region) counts only distinct non-NULL values"],"answer":0},{"id":187,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["NULL","TRUE","UNKNOWN (a value distinct from NULL in the SQL standard)","FALSE"],"answer":1},{"id":188,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT score FROM accounts),\n     b AS (SELECT * FROM a WHERE score > 0)\nSELECT * FROM b;","options":["Yes — b may reference a defined earlier in the same WITH","No — only base table accounts may be referenced","Only if both are declared RECURSIVE","No — CTEs cannot reference each other"],"answer":0},{"id":189,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(flag) = 'X';","options":["UPPER() always improves index efficiency","Functions are required for case-insensitive searches","It forces the optimizer to use a sort-merge join","Applying UPPER() to flag prevents a plain index seek on flag"],"answer":3},{"id":190,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","They are identical in all databases","DELETE cannot use a WHERE clause","TRUNCATE removes one row at a time"],"answer":0},{"id":191,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.qty = b.qty;","options":["Old-style joins are always faster than explicit JOIN","Cannot represent outer joins","Forgetting the WHERE condition produces a silent Cartesian product","This syntax is illegal in SQL:1999+"],"answer":2},{"id":192,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Only valid when two or more tables appear in FROM","HAVING without GROUP BY is the same as WHERE","Often yes — the whole table is treated as one implicit group","Always a syntax error in standard SQL"],"answer":2},{"id":193,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["They behave identically for all inputs","DENSE_RANK leaves gaps; RANK does not","Only RANK supports PARTITION BY","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers"],"answer":3},{"id":194,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT code FROM products) AS x;","options":["When the subquery contains GROUP BY","When the subquery returns zero rows","When the subquery returns more than one row","When the outer query has no FROM clause"],"answer":2},{"id":195,"type":"theoretical","topic":"indexes","q":"Composite index on (name, status) — which filter uses the index for a seek?","options":["WHERE status = ? alone — skips the leading column","ORDER BY status without any filter on name","WHERE name = ? — satisfies the leftmost-prefix rule","WHERE status LIKE '%x%' alone"],"answer":2},{"id":196,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["The combined row (user_id, created_at) — the pair must be unique","Each column independently in two passes","Primary-key rows only","Only the first column user_id"],"answer":0},{"id":197,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(order_id) FROM coupons WHERE region IS NULL; -- assume 0 rows match","options":["An error — MAX on empty set is undefined","0 (zero)","Zero rows are returned","NULL — aggregating an empty set returns NULL"],"answer":3},{"id":198,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["No rows satisfy the ON predicate (including when NULLs prevent matches)","NULL in ON always evaluates to TRUE","INNER JOIN always returns at least one row","INNER JOIN ignores the WHERE clause"],"answer":0},{"id":199,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT status FROM metrics) EXCEPT (SELECT status FROM tickets);","options":["Rows in tickets not present in metrics","Rows in metrics not present in tickets","Rows present in both metrics and tickets","All rows from both queries combined"],"answer":1},{"id":200,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) q FROM flags;","options":["Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Requires PARTITION BY created_at or it errors","Keeps only the top 4 rows","Returns fractional percentiles 0.25 … 1.0"],"answer":0},{"id":201,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE region = NULL;","options":["Nothing; it returns rows where region is NULL","The engine treats NULL as 0 for comparisons","NULL cannot appear after an equality operator","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE"],"answer":3},{"id":202,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT sku, qty, COUNT(*) FROM users GROUP BY sku;","options":["qty is not in GROUP BY and not functionally dependent on sku","COUNT(*) is not allowed alongside GROUP BY","Two non-aggregate columns cannot precede COUNT","GROUP BY must list every column in the table"],"answer":0},{"id":203,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(score) > 10, where should that condition go?\nSELECT price, AVG(score) FROM events GROUP BY price","options":["WHERE AVG(score) > 10 — before GROUP BY","HAVING AVG(score) > 10 — after GROUP BY","LIMIT with a sort by AVG replaces the filter","It cannot be expressed without a subquery"],"answer":1},{"id":204,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.flag = b.flag;","options":["Unmatched accounts rows are kept; matched skus rows are dropped","A Cartesian product is produced","Every row from skus appears; unmatched accounts columns are NULL","Only rows matching on both sides are returned"],"answer":2},{"id":205,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Only rows with equal primary keys","An error unless an ON clause is provided","Duplicate-free union of both tables","Every row of sessions paired with every row of campaigns — Cartesian product"],"answer":3},{"id":206,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT qty FROM payments WHERE name > 0 GROUP BY qty HAVING COUNT(*) > 1 ORDER BY qty;","options":["WHERE → FROM → GROUP BY → HAVING","SELECT → FROM → WHERE → GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","ORDER BY is applied before GROUP BY"],"answer":2},{"id":207,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT price FROM subs) UNION ALL (SELECT price FROM visitors);","options":["UNION ALL removes duplicates; UNION keeps them","UNION ALL sorts by price; UNION does not","UNION ALL keeps duplicate rows; UNION removes them","UNION ALL requires identical column names"],"answer":2},{"id":208,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT kind, ROW_NUMBER() OVER (PARTITION BY order_id) rn FROM tickets;","options":["Runs, but row order within each partition is non-deterministic","Always numbers rows by physical storage order","Behaves like RANK() with gaps","Syntax error — ORDER BY is mandatory inside OVER"],"answer":0},{"id":209,"type":"theoretical","topic":"window","q":"What does LAG(code, 1) return for each row?\nSELECT amount, LAG(code, 1) OVER (ORDER BY amount) prev FROM shipments;","options":["A running sum of all prior code values","The first code in the partition for every row","The value from the next row ahead","The value of code from the previous row in ORDER BY sequence"],"answer":3},{"id":210,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.name = o.name);","options":["When the subquery returns at least one row for that outer row","When COUNT(*) inside the subquery exceeds zero","When the subquery returns exactly one column","When IN would return FALSE"],"answer":0},{"id":211,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(user_id) and COUNT(*)?\nSELECT COUNT(user_id), COUNT(*) FROM clicks;","options":["COUNT(user_id) counts only distinct non-NULL values","COUNT(user_id) skips NULLs; COUNT(*) counts every row","They are always identical","COUNT(*) skips NULLs; COUNT(user_id) counts every row"],"answer":1},{"id":212,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["TRUE","FALSE","UNKNOWN (a value distinct from NULL in the SQL standard)","NULL"],"answer":0},{"id":213,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT amount FROM accounts),\n     b AS (SELECT * FROM a WHERE amount > 0)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","No — only base table accounts may be referenced","Only if both are declared RECURSIVE","Yes — b may reference a defined earlier in the same WITH"],"answer":3},{"id":214,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(status) = 'X';","options":["Applying UPPER() to status prevents a plain index seek on status","UPPER() always improves index efficiency","It forces the optimizer to use a sort-merge join","Functions are required for case-insensitive searches"],"answer":0},{"id":215,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["They are identical in all databases","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","TRUNCATE removes one row at a time","DELETE cannot use a WHERE clause"],"answer":1},{"id":216,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.region = b.region;","options":["Forgetting the WHERE condition produces a silent Cartesian product","Cannot represent outer joins","This syntax is illegal in SQL:1999+","Old-style joins are always faster than explicit JOIN"],"answer":0},{"id":217,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Always a syntax error in standard SQL","Only valid when two or more tables appear in FROM","HAVING without GROUP BY is the same as WHERE","Often yes — the whole table is treated as one implicit group"],"answer":3},{"id":218,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["DENSE_RANK leaves gaps; RANK does not","They behave identically for all inputs","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","Only RANK supports PARTITION BY"],"answer":2},{"id":219,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT flag FROM products) AS x;","options":["When the subquery returns zero rows","When the outer query has no FROM clause","When the subquery returns more than one row","When the subquery contains GROUP BY"],"answer":2},{"id":220,"type":"theoretical","topic":"indexes","q":"Composite index on (tier, code) — which filter uses the index for a seek?","options":["WHERE code LIKE '%x%' alone","ORDER BY code without any filter on tier","WHERE tier = ? — satisfies the leftmost-prefix rule","WHERE code = ? alone — skips the leading column"],"answer":2},{"id":221,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT qty, name FROM invoices;","options":["Each column independently in two passes","Primary-key rows only","Only the first column qty","The combined row (qty, name) — the pair must be unique"],"answer":3},{"id":222,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(price) FROM coupons WHERE user_id IS NULL; -- assume 0 rows match","options":["NULL — aggregating an empty set returns NULL","An error — MAX on empty set is undefined","Zero rows are returned","0 (zero)"],"answer":0},{"id":223,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["INNER JOIN ignores the WHERE clause","No rows satisfy the ON predicate (including when NULLs prevent matches)","INNER JOIN always returns at least one row","NULL in ON always evaluates to TRUE"],"answer":1},{"id":224,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT code FROM metrics) EXCEPT (SELECT code FROM tickets);","options":["Rows in metrics not present in tickets","All rows from both queries combined","Rows in tickets not present in metrics","Rows present in both metrics and tickets"],"answer":0},{"id":225,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT name, NTILE(4) OVER (ORDER BY status) q FROM flags;","options":["Keeps only the top 4 rows","Requires PARTITION BY name or it errors","Returns fractional percentiles 0.25 … 1.0","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)"],"answer":3},{"id":226,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE user_id = NULL;","options":["Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","Nothing; it returns rows where user_id is NULL","NULL cannot appear after an equality operator","The engine treats NULL as 0 for comparisons"],"answer":0},{"id":227,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT order_id, region, COUNT(*) FROM users GROUP BY order_id;","options":["COUNT(*) is not allowed alongside GROUP BY","region is not in GROUP BY and not functionally dependent on order_id","GROUP BY must list every column in the table","Two non-aggregate columns cannot precede COUNT"],"answer":1},{"id":228,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(amount) > 10, where should that condition go?\nSELECT sku, AVG(amount) FROM events GROUP BY sku","options":["HAVING AVG(amount) > 10 — after GROUP BY","LIMIT with a sort by AVG replaces the filter","It cannot be expressed without a subquery","WHERE AVG(amount) > 10 — before GROUP BY"],"answer":0},{"id":229,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.status = b.status;","options":["Only rows matching on both sides are returned","A Cartesian product is produced","Unmatched accounts rows are kept; matched skus rows are dropped","Every row from skus appears; unmatched accounts columns are NULL"],"answer":3},{"id":230,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["An error unless an ON clause is provided","Only rows with equal primary keys","Every row of sessions paired with every row of campaigns — Cartesian product","Duplicate-free union of both tables"],"answer":2},{"id":231,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT region FROM payments WHERE tier > 0 GROUP BY region HAVING COUNT(*) > 1 ORDER BY region;","options":["SELECT → FROM → WHERE → GROUP BY","ORDER BY is applied before GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","WHERE → FROM → GROUP BY → HAVING"],"answer":2},{"id":232,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT sku FROM subs) UNION ALL (SELECT sku FROM visitors);","options":["UNION ALL requires identical column names","UNION ALL sorts by sku; UNION does not","UNION ALL keeps duplicate rows; UNION removes them","UNION ALL removes duplicates; UNION keeps them"],"answer":2},{"id":233,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT score, ROW_NUMBER() OVER (PARTITION BY price) rn FROM tickets;","options":["Syntax error — ORDER BY is mandatory inside OVER","Always numbers rows by physical storage order","Behaves like RANK() with gaps","Runs, but row order within each partition is non-deterministic"],"answer":3},{"id":234,"type":"theoretical","topic":"window","q":"What does LAG(flag, 1) return for each row?\nSELECT kind, LAG(flag, 1) OVER (ORDER BY kind) prev FROM shipments;","options":["The value of flag from the previous row in ORDER BY sequence","A running sum of all prior flag values","The value from the next row ahead","The first flag in the partition for every row"],"answer":0},{"id":235,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.tier = o.tier);","options":["When IN would return FALSE","When the subquery returns at least one row for that outer row","When the subquery returns exactly one column","When COUNT(*) inside the subquery exceeds zero"],"answer":1},{"id":236,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(qty) and COUNT(*)?\nSELECT COUNT(qty), COUNT(*) FROM clicks;","options":["COUNT(qty) skips NULLs; COUNT(*) counts every row","COUNT(*) skips NULLs; COUNT(qty) counts every row","COUNT(qty) counts only distinct non-NULL values","They are always identical"],"answer":0},{"id":237,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["FALSE","UNKNOWN (a value distinct from NULL in the SQL standard)","NULL","TRUE"],"answer":3},{"id":238,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT kind FROM accounts),\n     b AS (SELECT * FROM a WHERE kind > 0)\nSELECT * FROM b;","options":["Yes — b may reference a defined earlier in the same WITH","No — CTEs cannot reference each other","Only if both are declared RECURSIVE","No — only base table accounts may be referenced"],"answer":0},{"id":239,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(code) = 'X';","options":["UPPER() always improves index efficiency","Applying UPPER() to code prevents a plain index seek on code","Functions are required for case-insensitive searches","It forces the optimizer to use a sort-merge join"],"answer":1},{"id":240,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","TRUNCATE removes one row at a time","DELETE cannot use a WHERE clause","They are identical in all databases"],"answer":0},{"id":241,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.user_id = b.user_id;","options":["Old-style joins are always faster than explicit JOIN","Cannot represent outer joins","This syntax is illegal in SQL:1999+","Forgetting the WHERE condition produces a silent Cartesian product"],"answer":3},{"id":242,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Often yes — the whole table is treated as one implicit group","Always a syntax error in standard SQL","Only valid when two or more tables appear in FROM","HAVING without GROUP BY is the same as WHERE"],"answer":0},{"id":243,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["They behave identically for all inputs","Only RANK supports PARTITION BY","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","DENSE_RANK leaves gaps; RANK does not"],"answer":2},{"id":244,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT status FROM products) AS x;","options":["When the subquery contains GROUP BY","When the outer query has no FROM clause","When the subquery returns more than one row","When the subquery returns zero rows"],"answer":2},{"id":245,"type":"theoretical","topic":"indexes","q":"Composite index on (created_at, flag) — which filter uses the index for a seek?","options":["WHERE flag = ? alone — skips the leading column","WHERE flag LIKE '%x%' alone","ORDER BY flag without any filter on created_at","WHERE created_at = ? — satisfies the leftmost-prefix rule"],"answer":3},{"id":246,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT region, tier FROM invoices;","options":["Each column independently in two passes","Only the first column region","The combined row (region, tier) — the pair must be unique","Primary-key rows only"],"answer":2},{"id":247,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(sku) FROM coupons WHERE qty IS NULL; -- assume 0 rows match","options":["0 (zero)","NULL — aggregating an empty set returns NULL","Zero rows are returned","An error — MAX on empty set is undefined"],"answer":1},{"id":248,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["No rows satisfy the ON predicate (including when NULLs prevent matches)","NULL in ON always evaluates to TRUE","INNER JOIN ignores the WHERE clause","INNER JOIN always returns at least one row"],"answer":0},{"id":249,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT flag FROM metrics) EXCEPT (SELECT flag FROM tickets);","options":["All rows from both queries combined","Rows in tickets not present in metrics","Rows present in both metrics and tickets","Rows in metrics not present in tickets"],"answer":3},{"id":250,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT tier, NTILE(4) OVER (ORDER BY code) q FROM flags;","options":["Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Keeps only the top 4 rows","Returns fractional percentiles 0.25 … 1.0","Requires PARTITION BY tier or it errors"],"answer":0},{"id":251,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE qty = NULL;","options":["Nothing; it returns rows where qty is NULL","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","The engine treats NULL as 0 for comparisons","NULL cannot appear after an equality operator"],"answer":1},{"id":252,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT price, user_id, COUNT(*) FROM users GROUP BY price;","options":["user_id is not in GROUP BY and not functionally dependent on price","GROUP BY must list every column in the table","Two non-aggregate columns cannot precede COUNT","COUNT(*) is not allowed alongside GROUP BY"],"answer":0},{"id":253,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(kind) > 10, where should that condition go?\nSELECT order_id, AVG(kind) FROM events GROUP BY order_id","options":["WHERE AVG(kind) > 10 — before GROUP BY","LIMIT with a sort by AVG replaces the filter","It cannot be expressed without a subquery","HAVING AVG(kind) > 10 — after GROUP BY"],"answer":3},{"id":254,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.code = b.code;","options":["Every row from skus appears; unmatched accounts columns are NULL","Only rows matching on both sides are returned","Unmatched accounts rows are kept; matched skus rows are dropped","A Cartesian product is produced"],"answer":0},{"id":255,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Only rows with equal primary keys","Duplicate-free union of both tables","Every row of sessions paired with every row of campaigns — Cartesian product","An error unless an ON clause is provided"],"answer":2},{"id":256,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT user_id FROM payments WHERE created_at > 0 GROUP BY user_id HAVING COUNT(*) > 1 ORDER BY user_id;","options":["WHERE → FROM → GROUP BY → HAVING","ORDER BY is applied before GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","SELECT → FROM → WHERE → GROUP BY"],"answer":2},{"id":257,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT order_id FROM subs) UNION ALL (SELECT order_id FROM visitors);","options":["UNION ALL removes duplicates; UNION keeps them","UNION ALL requires identical column names","UNION ALL sorts by order_id; UNION does not","UNION ALL keeps duplicate rows; UNION removes them"],"answer":3},{"id":258,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT amount, ROW_NUMBER() OVER (PARTITION BY sku) rn FROM tickets;","options":["Always numbers rows by physical storage order","Syntax error — ORDER BY is mandatory inside OVER","Runs, but row order within each partition is non-deterministic","Behaves like RANK() with gaps"],"answer":2},{"id":259,"type":"theoretical","topic":"window","q":"What does LAG(status, 1) return for each row?\nSELECT score, LAG(status, 1) OVER (ORDER BY score) prev FROM shipments;","options":["The value from the next row ahead","The first status in the partition for every row","The value of status from the previous row in ORDER BY sequence","A running sum of all prior status values"],"answer":2},{"id":260,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.created_at = o.created_at);","options":["When the subquery returns at least one row for that outer row","When COUNT(*) inside the subquery exceeds zero","When IN would return FALSE","When the subquery returns exactly one column"],"answer":0},{"id":261,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(region) and COUNT(*)?\nSELECT COUNT(region), COUNT(*) FROM clicks;","options":["COUNT(*) skips NULLs; COUNT(region) counts every row","COUNT(region) counts only distinct non-NULL values","They are always identical","COUNT(region) skips NULLs; COUNT(*) counts every row"],"answer":3},{"id":262,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["TRUE","FALSE","NULL","UNKNOWN (a value distinct from NULL in the SQL standard)"],"answer":0},{"id":263,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT score FROM accounts),\n     b AS (SELECT * FROM a WHERE score > 0)\nSELECT * FROM b;","options":["No — only base table accounts may be referenced","Yes — b may reference a defined earlier in the same WITH","No — CTEs cannot reference each other","Only if both are declared RECURSIVE"],"answer":1},{"id":264,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(flag) = 'X';","options":["Applying UPPER() to flag prevents a plain index seek on flag","Functions are required for case-insensitive searches","It forces the optimizer to use a sort-merge join","UPPER() always improves index efficiency"],"answer":0},{"id":265,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["They are identical in all databases","TRUNCATE removes one row at a time","DELETE cannot use a WHERE clause","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE"],"answer":3},{"id":266,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.qty = b.qty;","options":["Forgetting the WHERE condition produces a silent Cartesian product","Old-style joins are always faster than explicit JOIN","This syntax is illegal in SQL:1999+","Cannot represent outer joins"],"answer":0},{"id":267,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Always a syntax error in standard SQL","Often yes — the whole table is treated as one implicit group","HAVING without GROUP BY is the same as WHERE","Only valid when two or more tables appear in FROM"],"answer":1},{"id":268,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["DENSE_RANK leaves gaps; RANK does not","Only RANK supports PARTITION BY","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","They behave identically for all inputs"],"answer":2},{"id":269,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT code FROM products) AS x;","options":["When the subquery returns zero rows","When the subquery contains GROUP BY","When the outer query has no FROM clause","When the subquery returns more than one row"],"answer":3},{"id":270,"type":"theoretical","topic":"indexes","q":"Composite index on (name, status) — which filter uses the index for a seek?","options":["WHERE status LIKE '%x%' alone","WHERE status = ? alone — skips the leading column","WHERE name = ? — satisfies the leftmost-prefix rule","ORDER BY status without any filter on name"],"answer":2},{"id":271,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["Only the first column user_id","Primary-key rows only","The combined row (user_id, created_at) — the pair must be unique","Each column independently in two passes"],"answer":2},{"id":272,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(order_id) FROM coupons WHERE region IS NULL; -- assume 0 rows match","options":["NULL — aggregating an empty set returns NULL","An error — MAX on empty set is undefined","0 (zero)","Zero rows are returned"],"answer":0},{"id":273,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["NULL in ON always evaluates to TRUE","INNER JOIN ignores the WHERE clause","INNER JOIN always returns at least one row","No rows satisfy the ON predicate (including when NULLs prevent matches)"],"answer":3},{"id":274,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT status FROM metrics) EXCEPT (SELECT status FROM tickets);","options":["Rows in metrics not present in tickets","All rows from both queries combined","Rows present in both metrics and tickets","Rows in tickets not present in metrics"],"answer":0},{"id":275,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) q FROM flags;","options":["Requires PARTITION BY created_at or it errors","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Returns fractional percentiles 0.25 … 1.0","Keeps only the top 4 rows"],"answer":1},{"id":276,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE region = NULL;","options":["Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","NULL cannot appear after an equality operator","The engine treats NULL as 0 for comparisons","Nothing; it returns rows where region is NULL"],"answer":0},{"id":277,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT sku, qty, COUNT(*) FROM users GROUP BY sku;","options":["COUNT(*) is not allowed alongside GROUP BY","GROUP BY must list every column in the table","Two non-aggregate columns cannot precede COUNT","qty is not in GROUP BY and not functionally dependent on sku"],"answer":3},{"id":278,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(score) > 10, where should that condition go?\nSELECT price, AVG(score) FROM events GROUP BY price","options":["HAVING AVG(score) > 10 — after GROUP BY","WHERE AVG(score) > 10 — before GROUP BY","It cannot be expressed without a subquery","LIMIT with a sort by AVG replaces the filter"],"answer":0},{"id":279,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.flag = b.flag;","options":["Only rows matching on both sides are returned","Every row from skus appears; unmatched accounts columns are NULL","A Cartesian product is produced","Unmatched accounts rows are kept; matched skus rows are dropped"],"answer":1},{"id":280,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Every row of sessions paired with every row of campaigns — Cartesian product","Duplicate-free union of both tables","An error unless an ON clause is provided","Only rows with equal primary keys"],"answer":0},{"id":281,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT qty FROM payments WHERE name > 0 GROUP BY qty HAVING COUNT(*) > 1 ORDER BY qty;","options":["SELECT → FROM → WHERE → GROUP BY","WHERE → FROM → GROUP BY → HAVING","ORDER BY is applied before GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY"],"answer":3},{"id":282,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT price FROM subs) UNION ALL (SELECT price FROM visitors);","options":["UNION ALL requires identical column names","UNION ALL removes duplicates; UNION keeps them","UNION ALL keeps duplicate rows; UNION removes them","UNION ALL sorts by price; UNION does not"],"answer":2},{"id":283,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT kind, ROW_NUMBER() OVER (PARTITION BY order_id) rn FROM tickets;","options":["Syntax error — ORDER BY is mandatory inside OVER","Behaves like RANK() with gaps","Runs, but row order within each partition is non-deterministic","Always numbers rows by physical storage order"],"answer":2},{"id":284,"type":"theoretical","topic":"window","q":"What does LAG(code, 1) return for each row?\nSELECT amount, LAG(code, 1) OVER (ORDER BY amount) prev FROM shipments;","options":["A running sum of all prior code values","The first code in the partition for every row","The value of code from the previous row in ORDER BY sequence","The value from the next row ahead"],"answer":2},{"id":285,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.name = o.name);","options":["When COUNT(*) inside the subquery exceeds zero","When IN would return FALSE","When the subquery returns exactly one column","When the subquery returns at least one row for that outer row"],"answer":3},{"id":286,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(user_id) and COUNT(*)?\nSELECT COUNT(user_id), COUNT(*) FROM clicks;","options":["COUNT(user_id) skips NULLs; COUNT(*) counts every row","COUNT(*) skips NULLs; COUNT(user_id) counts every row","They are always identical","COUNT(user_id) counts only distinct non-NULL values"],"answer":0},{"id":287,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["UNKNOWN (a value distinct from NULL in the SQL standard)","TRUE","NULL","FALSE"],"answer":1},{"id":288,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT amount FROM accounts),\n     b AS (SELECT * FROM a WHERE amount > 0)\nSELECT * FROM b;","options":["Yes — b may reference a defined earlier in the same WITH","Only if both are declared RECURSIVE","No — only base table accounts may be referenced","No — CTEs cannot reference each other"],"answer":0},{"id":289,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(status) = 'X';","options":["UPPER() always improves index efficiency","Functions are required for case-insensitive searches","It forces the optimizer to use a sort-merge join","Applying UPPER() to status prevents a plain index seek on status"],"answer":3},{"id":290,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","They are identical in all databases","DELETE cannot use a WHERE clause","TRUNCATE removes one row at a time"],"answer":0},{"id":291,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.region = b.region;","options":["Old-style joins are always faster than explicit JOIN","Forgetting the WHERE condition produces a silent Cartesian product","Cannot represent outer joins","This syntax is illegal in SQL:1999+"],"answer":1},{"id":292,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Often yes — the whole table is treated as one implicit group","HAVING without GROUP BY is the same as WHERE","Only valid when two or more tables appear in FROM","Always a syntax error in standard SQL"],"answer":0},{"id":293,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["They behave identically for all inputs","Only RANK supports PARTITION BY","DENSE_RANK leaves gaps; RANK does not","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers"],"answer":3},{"id":294,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT flag FROM products) AS x;","options":["When the subquery contains GROUP BY","When the subquery returns zero rows","When the subquery returns more than one row","When the outer query has no FROM clause"],"answer":2},{"id":295,"type":"theoretical","topic":"indexes","q":"Composite index on (tier, code) — which filter uses the index for a seek?","options":["WHERE code = ? alone — skips the leading column","ORDER BY code without any filter on tier","WHERE tier = ? — satisfies the leftmost-prefix rule","WHERE code LIKE '%x%' alone"],"answer":2},{"id":296,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT qty, name FROM invoices;","options":["Each column independently in two passes","Primary-key rows only","The combined row (qty, name) — the pair must be unique","Only the first column qty"],"answer":2},{"id":297,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(price) FROM coupons WHERE user_id IS NULL; -- assume 0 rows match","options":["Zero rows are returned","An error — MAX on empty set is undefined","0 (zero)","NULL — aggregating an empty set returns NULL"],"answer":3},{"id":298,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["No rows satisfy the ON predicate (including when NULLs prevent matches)","NULL in ON always evaluates to TRUE","INNER JOIN always returns at least one row","INNER JOIN ignores the WHERE clause"],"answer":0},{"id":299,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT code FROM metrics) EXCEPT (SELECT code FROM tickets);","options":["Rows in tickets not present in metrics","Rows in metrics not present in tickets","Rows present in both metrics and tickets","All rows from both queries combined"],"answer":1},{"id":300,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT name, NTILE(4) OVER (ORDER BY status) q FROM flags;","options":["Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Keeps only the top 4 rows","Requires PARTITION BY name or it errors","Returns fractional percentiles 0.25 … 1.0"],"answer":0},{"id":301,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE user_id = NULL;","options":["NULL cannot appear after an equality operator","The engine treats NULL as 0 for comparisons","Nothing; it returns rows where user_id is NULL","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE"],"answer":3},{"id":302,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT order_id, region, COUNT(*) FROM users GROUP BY order_id;","options":["region is not in GROUP BY and not functionally dependent on order_id","COUNT(*) is not allowed alongside GROUP BY","Two non-aggregate columns cannot precede COUNT","GROUP BY must list every column in the table"],"answer":0},{"id":303,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(amount) > 10, where should that condition go?\nSELECT sku, AVG(amount) FROM events GROUP BY sku","options":["WHERE AVG(amount) > 10 — before GROUP BY","HAVING AVG(amount) > 10 — after GROUP BY","LIMIT with a sort by AVG replaces the filter","It cannot be expressed without a subquery"],"answer":1},{"id":304,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.status = b.status;","options":["Every row from skus appears; unmatched accounts columns are NULL","A Cartesian product is produced","Unmatched accounts rows are kept; matched skus rows are dropped","Only rows matching on both sides are returned"],"answer":0},{"id":305,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Only rows with equal primary keys","Duplicate-free union of both tables","An error unless an ON clause is provided","Every row of sessions paired with every row of campaigns — Cartesian product"],"answer":3},{"id":306,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT region FROM payments WHERE tier > 0 GROUP BY region HAVING COUNT(*) > 1 ORDER BY region;","options":["WHERE → FROM → GROUP BY → HAVING","SELECT → FROM → WHERE → GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","ORDER BY is applied before GROUP BY"],"answer":2},{"id":307,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT sku FROM subs) UNION ALL (SELECT sku FROM visitors);","options":["UNION ALL removes duplicates; UNION keeps them","UNION ALL sorts by sku; UNION does not","UNION ALL keeps duplicate rows; UNION removes them","UNION ALL requires identical column names"],"answer":2},{"id":308,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT score, ROW_NUMBER() OVER (PARTITION BY price) rn FROM tickets;","options":["Always numbers rows by physical storage order","Behaves like RANK() with gaps","Runs, but row order within each partition is non-deterministic","Syntax error — ORDER BY is mandatory inside OVER"],"answer":2},{"id":309,"type":"theoretical","topic":"window","q":"What does LAG(flag, 1) return for each row?\nSELECT kind, LAG(flag, 1) OVER (ORDER BY kind) prev FROM shipments;","options":["The value from the next row ahead","A running sum of all prior flag values","The first flag in the partition for every row","The value of flag from the previous row in ORDER BY sequence"],"answer":3},{"id":310,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.tier = o.tier);","options":["When COUNT(*) inside the subquery exceeds zero","When the subquery returns exactly one column","When the subquery returns at least one row for that outer row","When IN would return FALSE"],"answer":2},{"id":311,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(qty) and COUNT(*)?\nSELECT COUNT(qty), COUNT(*) FROM clicks;","options":["COUNT(qty) counts only distinct non-NULL values","COUNT(qty) skips NULLs; COUNT(*) counts every row","They are always identical","COUNT(*) skips NULLs; COUNT(qty) counts every row"],"answer":1},{"id":312,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["TRUE","FALSE","UNKNOWN (a value distinct from NULL in the SQL standard)","NULL"],"answer":0},{"id":313,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT kind FROM accounts),\n     b AS (SELECT * FROM a WHERE kind > 0)\nSELECT * FROM b;","options":["Only if both are declared RECURSIVE","No — only base table accounts may be referenced","No — CTEs cannot reference each other","Yes — b may reference a defined earlier in the same WITH"],"answer":3},{"id":314,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(code) = 'X';","options":["Applying UPPER() to code prevents a plain index seek on code","It forces the optimizer to use a sort-merge join","UPPER() always improves index efficiency","Functions are required for case-insensitive searches"],"answer":0},{"id":315,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["They are identical in all databases","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","TRUNCATE removes one row at a time","DELETE cannot use a WHERE clause"],"answer":1},{"id":316,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.user_id = b.user_id;","options":["Forgetting the WHERE condition produces a silent Cartesian product","Cannot represent outer joins","This syntax is illegal in SQL:1999+","Old-style joins are always faster than explicit JOIN"],"answer":0},{"id":317,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Always a syntax error in standard SQL","HAVING without GROUP BY is the same as WHERE","Only valid when two or more tables appear in FROM","Often yes — the whole table is treated as one implicit group"],"answer":3},{"id":318,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","They behave identically for all inputs","DENSE_RANK leaves gaps; RANK does not","Only RANK supports PARTITION BY"],"answer":0},{"id":319,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT status FROM products) AS x;","options":["When the subquery returns zero rows","When the outer query has no FROM clause","When the subquery returns more than one row","When the subquery contains GROUP BY"],"answer":2},{"id":320,"type":"theoretical","topic":"indexes","q":"Composite index on (created_at, flag) — which filter uses the index for a seek?","options":["WHERE flag LIKE '%x%' alone","ORDER BY flag without any filter on created_at","WHERE created_at = ? — satisfies the leftmost-prefix rule","WHERE flag = ? alone — skips the leading column"],"answer":2},{"id":321,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT region, tier FROM invoices;","options":["Only the first column region","Each column independently in two passes","Primary-key rows only","The combined row (region, tier) — the pair must be unique"],"answer":3},{"id":322,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(sku) FROM coupons WHERE qty IS NULL; -- assume 0 rows match","options":["An error — MAX on empty set is undefined","Zero rows are returned","NULL — aggregating an empty set returns NULL","0 (zero)"],"answer":2},{"id":323,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["INNER JOIN ignores the WHERE clause","No rows satisfy the ON predicate (including when NULLs prevent matches)","INNER JOIN always returns at least one row","NULL in ON always evaluates to TRUE"],"answer":1},{"id":324,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT flag FROM metrics) EXCEPT (SELECT flag FROM tickets);","options":["Rows in metrics not present in tickets","All rows from both queries combined","Rows in tickets not present in metrics","Rows present in both metrics and tickets"],"answer":0},{"id":325,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT tier, NTILE(4) OVER (ORDER BY code) q FROM flags;","options":["Keeps only the top 4 rows","Requires PARTITION BY tier or it errors","Returns fractional percentiles 0.25 … 1.0","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)"],"answer":3},{"id":326,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE qty = NULL;","options":["Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","NULL cannot appear after an equality operator","Nothing; it returns rows where qty is NULL","The engine treats NULL as 0 for comparisons"],"answer":0},{"id":327,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT price, user_id, COUNT(*) FROM users GROUP BY price;","options":["GROUP BY must list every column in the table","user_id is not in GROUP BY and not functionally dependent on price","COUNT(*) is not allowed alongside GROUP BY","Two non-aggregate columns cannot precede COUNT"],"answer":1},{"id":328,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(kind) > 10, where should that condition go?\nSELECT order_id, AVG(kind) FROM events GROUP BY order_id","options":["HAVING AVG(kind) > 10 — after GROUP BY","LIMIT with a sort by AVG replaces the filter","It cannot be expressed without a subquery","WHERE AVG(kind) > 10 — before GROUP BY"],"answer":0},{"id":329,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.code = b.code;","options":["Only rows matching on both sides are returned","A Cartesian product is produced","Unmatched accounts rows are kept; matched skus rows are dropped","Every row from skus appears; unmatched accounts columns are NULL"],"answer":3},{"id":330,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Every row of sessions paired with every row of campaigns — Cartesian product","Only rows with equal primary keys","An error unless an ON clause is provided","Duplicate-free union of both tables"],"answer":0},{"id":331,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT user_id FROM payments WHERE created_at > 0 GROUP BY user_id HAVING COUNT(*) > 1 ORDER BY user_id;","options":["SELECT → FROM → WHERE → GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","ORDER BY is applied before GROUP BY","WHERE → FROM → GROUP BY → HAVING"],"answer":1},{"id":332,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT order_id FROM subs) UNION ALL (SELECT order_id FROM visitors);","options":["UNION ALL requires identical column names","UNION ALL sorts by order_id; UNION does not","UNION ALL keeps duplicate rows; UNION removes them","UNION ALL removes duplicates; UNION keeps them"],"answer":2},{"id":333,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT amount, ROW_NUMBER() OVER (PARTITION BY sku) rn FROM tickets;","options":["Syntax error — ORDER BY is mandatory inside OVER","Always numbers rows by physical storage order","Behaves like RANK() with gaps","Runs, but row order within each partition is non-deterministic"],"answer":3},{"id":334,"type":"theoretical","topic":"window","q":"What does LAG(status, 1) return for each row?\nSELECT score, LAG(status, 1) OVER (ORDER BY score) prev FROM shipments;","options":["A running sum of all prior status values","The value from the next row ahead","The value of status from the previous row in ORDER BY sequence","The first status in the partition for every row"],"answer":2},{"id":335,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.created_at = o.created_at);","options":["When the subquery returns exactly one column","When IN would return FALSE","When the subquery returns at least one row for that outer row","When COUNT(*) inside the subquery exceeds zero"],"answer":2},{"id":336,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(region) and COUNT(*)?\nSELECT COUNT(region), COUNT(*) FROM clicks;","options":["COUNT(region) skips NULLs; COUNT(*) counts every row","COUNT(*) skips NULLs; COUNT(region) counts every row","COUNT(region) counts only distinct non-NULL values","They are always identical"],"answer":0},{"id":337,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["FALSE","UNKNOWN (a value distinct from NULL in the SQL standard)","NULL","TRUE"],"answer":3},{"id":338,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT score FROM accounts),\n     b AS (SELECT * FROM a WHERE score > 0)\nSELECT * FROM b;","options":["Yes — b may reference a defined earlier in the same WITH","Only if both are declared RECURSIVE","No — CTEs cannot reference each other","No — only base table accounts may be referenced"],"answer":0},{"id":339,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(flag) = 'X';","options":["Functions are required for case-insensitive searches","Applying UPPER() to flag prevents a plain index seek on flag","UPPER() always improves index efficiency","It forces the optimizer to use a sort-merge join"],"answer":1},{"id":340,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","DELETE cannot use a WHERE clause","TRUNCATE removes one row at a time","They are identical in all databases"],"answer":0},{"id":341,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.qty = b.qty;","options":["Old-style joins are always faster than explicit JOIN","Cannot represent outer joins","This syntax is illegal in SQL:1999+","Forgetting the WHERE condition produces a silent Cartesian product"],"answer":3},{"id":342,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Often yes — the whole table is treated as one implicit group","Always a syntax error in standard SQL","Only valid when two or more tables appear in FROM","HAVING without GROUP BY is the same as WHERE"],"answer":0},{"id":343,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["They behave identically for all inputs","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","Only RANK supports PARTITION BY","DENSE_RANK leaves gaps; RANK does not"],"answer":1},{"id":344,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT code FROM products) AS x;","options":["When the subquery returns more than one row","When the outer query has no FROM clause","When the subquery contains GROUP BY","When the subquery returns zero rows"],"answer":0},{"id":345,"type":"theoretical","topic":"indexes","q":"Composite index on (name, status) — which filter uses the index for a seek?","options":["WHERE status = ? alone — skips the leading column","WHERE status LIKE '%x%' alone","ORDER BY status without any filter on name","WHERE name = ? — satisfies the leftmost-prefix rule"],"answer":3},{"id":346,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["Each column independently in two passes","Only the first column user_id","The combined row (user_id, created_at) — the pair must be unique","Primary-key rows only"],"answer":2},{"id":347,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(order_id) FROM coupons WHERE region IS NULL; -- assume 0 rows match","options":["Zero rows are returned","0 (zero)","NULL — aggregating an empty set returns NULL","An error — MAX on empty set is undefined"],"answer":2},{"id":348,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["NULL in ON always evaluates to TRUE","INNER JOIN ignores the WHERE clause","No rows satisfy the ON predicate (including when NULLs prevent matches)","INNER JOIN always returns at least one row"],"answer":2},{"id":349,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT status FROM metrics) EXCEPT (SELECT status FROM tickets);","options":["All rows from both queries combined","Rows in tickets not present in metrics","Rows present in both metrics and tickets","Rows in metrics not present in tickets"],"answer":3},{"id":350,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) q FROM flags;","options":["Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Keeps only the top 4 rows","Returns fractional percentiles 0.25 … 1.0","Requires PARTITION BY created_at or it errors"],"answer":0},{"id":351,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE region = NULL;","options":["The engine treats NULL as 0 for comparisons","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","Nothing; it returns rows where region is NULL","NULL cannot appear after an equality operator"],"answer":1},{"id":352,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT sku, qty, COUNT(*) FROM users GROUP BY sku;","options":["qty is not in GROUP BY and not functionally dependent on sku","Two non-aggregate columns cannot precede COUNT","GROUP BY must list every column in the table","COUNT(*) is not allowed alongside GROUP BY"],"answer":0},{"id":353,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(score) > 10, where should that condition go?\nSELECT price, AVG(score) FROM events GROUP BY price","options":["WHERE AVG(score) > 10 — before GROUP BY","LIMIT with a sort by AVG replaces the filter","It cannot be expressed without a subquery","HAVING AVG(score) > 10 — after GROUP BY"],"answer":3},{"id":354,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.flag = b.flag;","options":["Every row from skus appears; unmatched accounts columns are NULL","Only rows matching on both sides are returned","Unmatched accounts rows are kept; matched skus rows are dropped","A Cartesian product is produced"],"answer":0},{"id":355,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Only rows with equal primary keys","Every row of sessions paired with every row of campaigns — Cartesian product","Duplicate-free union of both tables","An error unless an ON clause is provided"],"answer":1},{"id":356,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT qty FROM payments WHERE name > 0 GROUP BY qty HAVING COUNT(*) > 1 ORDER BY qty;","options":["FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","ORDER BY is applied before GROUP BY","WHERE → FROM → GROUP BY → HAVING","SELECT → FROM → WHERE → GROUP BY"],"answer":0},{"id":357,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT price FROM subs) UNION ALL (SELECT price FROM visitors);","options":["UNION ALL removes duplicates; UNION keeps them","UNION ALL sorts by price; UNION does not","UNION ALL requires identical column names","UNION ALL keeps duplicate rows; UNION removes them"],"answer":3},{"id":358,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT kind, ROW_NUMBER() OVER (PARTITION BY order_id) rn FROM tickets;","options":["Always numbers rows by physical storage order","Syntax error — ORDER BY is mandatory inside OVER","Runs, but row order within each partition is non-deterministic","Behaves like RANK() with gaps"],"answer":2},{"id":359,"type":"theoretical","topic":"window","q":"What does LAG(code, 1) return for each row?\nSELECT amount, LAG(code, 1) OVER (ORDER BY amount) prev FROM shipments;","options":["The value from the next row ahead","The first code in the partition for every row","The value of code from the previous row in ORDER BY sequence","A running sum of all prior code values"],"answer":2},{"id":360,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.name = o.name);","options":["When COUNT(*) inside the subquery exceeds zero","When IN would return FALSE","When the subquery returns at least one row for that outer row","When the subquery returns exactly one column"],"answer":2},{"id":361,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(user_id) and COUNT(*)?\nSELECT COUNT(user_id), COUNT(*) FROM clicks;","options":["They are always identical","COUNT(*) skips NULLs; COUNT(user_id) counts every row","COUNT(user_id) counts only distinct non-NULL values","COUNT(user_id) skips NULLs; COUNT(*) counts every row"],"answer":3},{"id":362,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["TRUE","FALSE","NULL","UNKNOWN (a value distinct from NULL in the SQL standard)"],"answer":0},{"id":363,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT amount FROM accounts),\n     b AS (SELECT * FROM a WHERE amount > 0)\nSELECT * FROM b;","options":["No — only base table accounts may be referenced","Yes — b may reference a defined earlier in the same WITH","No — CTEs cannot reference each other","Only if both are declared RECURSIVE"],"answer":1},{"id":364,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(status) = 'X';","options":["Applying UPPER() to status prevents a plain index seek on status","It forces the optimizer to use a sort-merge join","Functions are required for case-insensitive searches","UPPER() always improves index efficiency"],"answer":0},{"id":365,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["DELETE cannot use a WHERE clause","TRUNCATE removes one row at a time","They are identical in all databases","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE"],"answer":3},{"id":366,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.region = b.region;","options":["Forgetting the WHERE condition produces a silent Cartesian product","Old-style joins are always faster than explicit JOIN","This syntax is illegal in SQL:1999+","Cannot represent outer joins"],"answer":0},{"id":367,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Always a syntax error in standard SQL","Often yes — the whole table is treated as one implicit group","HAVING without GROUP BY is the same as WHERE","Only valid when two or more tables appear in FROM"],"answer":1},{"id":368,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","Only RANK supports PARTITION BY","DENSE_RANK leaves gaps; RANK does not","They behave identically for all inputs"],"answer":0},{"id":369,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT flag FROM products) AS x;","options":["When the subquery returns zero rows","When the outer query has no FROM clause","When the subquery contains GROUP BY","When the subquery returns more than one row"],"answer":3},{"id":370,"type":"theoretical","topic":"indexes","q":"Composite index on (tier, code) — which filter uses the index for a seek?","options":["WHERE code LIKE '%x%' alone","WHERE code = ? alone — skips the leading column","WHERE tier = ? — satisfies the leftmost-prefix rule","ORDER BY code without any filter on tier"],"answer":2},{"id":371,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT qty, name FROM invoices;","options":["Only the first column qty","Primary-key rows only","The combined row (qty, name) — the pair must be unique","Each column independently in two passes"],"answer":2},{"id":372,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(price) FROM coupons WHERE user_id IS NULL; -- assume 0 rows match","options":["An error — MAX on empty set is undefined","0 (zero)","NULL — aggregating an empty set returns NULL","Zero rows are returned"],"answer":2},{"id":373,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["INNER JOIN always returns at least one row","NULL in ON always evaluates to TRUE","INNER JOIN ignores the WHERE clause","No rows satisfy the ON predicate (including when NULLs prevent matches)"],"answer":3},{"id":374,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT code FROM metrics) EXCEPT (SELECT code FROM tickets);","options":["All rows from both queries combined","Rows present in both metrics and tickets","Rows in metrics not present in tickets","Rows in tickets not present in metrics"],"answer":2},{"id":375,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT name, NTILE(4) OVER (ORDER BY status) q FROM flags;","options":["Requires PARTITION BY name or it errors","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Returns fractional percentiles 0.25 … 1.0","Keeps only the top 4 rows"],"answer":1},{"id":376,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE user_id = NULL;","options":["Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","NULL cannot appear after an equality operator","The engine treats NULL as 0 for comparisons","Nothing; it returns rows where user_id is NULL"],"answer":0},{"id":377,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT order_id, region, COUNT(*) FROM users GROUP BY order_id;","options":["Two non-aggregate columns cannot precede COUNT","GROUP BY must list every column in the table","COUNT(*) is not allowed alongside GROUP BY","region is not in GROUP BY and not functionally dependent on order_id"],"answer":3},{"id":378,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(amount) > 10, where should that condition go?\nSELECT sku, AVG(amount) FROM events GROUP BY sku","options":["HAVING AVG(amount) > 10 — after GROUP BY","It cannot be expressed without a subquery","WHERE AVG(amount) > 10 — before GROUP BY","LIMIT with a sort by AVG replaces the filter"],"answer":0},{"id":379,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.status = b.status;","options":["Only rows matching on both sides are returned","Every row from skus appears; unmatched accounts columns are NULL","A Cartesian product is produced","Unmatched accounts rows are kept; matched skus rows are dropped"],"answer":1},{"id":380,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Every row of sessions paired with every row of campaigns — Cartesian product","Duplicate-free union of both tables","An error unless an ON clause is provided","Only rows with equal primary keys"],"answer":0},{"id":381,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT region FROM payments WHERE tier > 0 GROUP BY region HAVING COUNT(*) > 1 ORDER BY region;","options":["SELECT → FROM → WHERE → GROUP BY","ORDER BY is applied before GROUP BY","WHERE → FROM → GROUP BY → HAVING","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY"],"answer":3},{"id":382,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT sku FROM subs) UNION ALL (SELECT sku FROM visitors);","options":["UNION ALL keeps duplicate rows; UNION removes them","UNION ALL removes duplicates; UNION keeps them","UNION ALL requires identical column names","UNION ALL sorts by sku; UNION does not"],"answer":0},{"id":383,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT score, ROW_NUMBER() OVER (PARTITION BY price) rn FROM tickets;","options":["Syntax error — ORDER BY is mandatory inside OVER","Behaves like RANK() with gaps","Runs, but row order within each partition is non-deterministic","Always numbers rows by physical storage order"],"answer":2},{"id":384,"type":"theoretical","topic":"window","q":"What does LAG(flag, 1) return for each row?\nSELECT kind, LAG(flag, 1) OVER (ORDER BY kind) prev FROM shipments;","options":["A running sum of all prior flag values","The first flag in the partition for every row","The value of flag from the previous row in ORDER BY sequence","The value from the next row ahead"],"answer":2},{"id":385,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.tier = o.tier);","options":["When the subquery returns exactly one column","When COUNT(*) inside the subquery exceeds zero","When IN would return FALSE","When the subquery returns at least one row for that outer row"],"answer":3},{"id":386,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(qty) and COUNT(*)?\nSELECT COUNT(qty), COUNT(*) FROM clicks;","options":["COUNT(*) skips NULLs; COUNT(qty) counts every row","They are always identical","COUNT(qty) skips NULLs; COUNT(*) counts every row","COUNT(qty) counts only distinct non-NULL values"],"answer":2},{"id":387,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["UNKNOWN (a value distinct from NULL in the SQL standard)","TRUE","NULL","FALSE"],"answer":1},{"id":388,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT kind FROM accounts),\n     b AS (SELECT * FROM a WHERE kind > 0)\nSELECT * FROM b;","options":["Yes — b may reference a defined earlier in the same WITH","Only if both are declared RECURSIVE","No — only base table accounts may be referenced","No — CTEs cannot reference each other"],"answer":0},{"id":389,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(code) = 'X';","options":["It forces the optimizer to use a sort-merge join","Functions are required for case-insensitive searches","UPPER() always improves index efficiency","Applying UPPER() to code prevents a plain index seek on code"],"answer":3},{"id":390,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","DELETE cannot use a WHERE clause","They are identical in all databases","TRUNCATE removes one row at a time"],"answer":0},{"id":391,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.user_id = b.user_id;","options":["Cannot represent outer joins","Forgetting the WHERE condition produces a silent Cartesian product","Old-style joins are always faster than explicit JOIN","This syntax is illegal in SQL:1999+"],"answer":1},{"id":392,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Often yes — the whole table is treated as one implicit group","HAVING without GROUP BY is the same as WHERE","Only valid when two or more tables appear in FROM","Always a syntax error in standard SQL"],"answer":0},{"id":393,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["They behave identically for all inputs","Only RANK supports PARTITION BY","DENSE_RANK leaves gaps; RANK does not","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers"],"answer":3},{"id":394,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT status FROM products) AS x;","options":["When the subquery returns more than one row","When the subquery returns zero rows","When the subquery contains GROUP BY","When the outer query has no FROM clause"],"answer":0},{"id":395,"type":"theoretical","topic":"indexes","q":"Composite index on (created_at, flag) — which filter uses the index for a seek?","options":["WHERE flag = ? alone — skips the leading column","WHERE created_at = ? — satisfies the leftmost-prefix rule","ORDER BY flag without any filter on created_at","WHERE flag LIKE '%x%' alone"],"answer":1},{"id":396,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT region, tier FROM invoices;","options":["Each column independently in two passes","Primary-key rows only","The combined row (region, tier) — the pair must be unique","Only the first column region"],"answer":2},{"id":397,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(sku) FROM coupons WHERE qty IS NULL; -- assume 0 rows match","options":["Zero rows are returned","An error — MAX on empty set is undefined","0 (zero)","NULL — aggregating an empty set returns NULL"],"answer":3},{"id":398,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["NULL in ON always evaluates to TRUE","INNER JOIN always returns at least one row","No rows satisfy the ON predicate (including when NULLs prevent matches)","INNER JOIN ignores the WHERE clause"],"answer":2},{"id":399,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT flag FROM metrics) EXCEPT (SELECT flag FROM tickets);","options":["Rows present in both metrics and tickets","Rows in tickets not present in metrics","Rows in metrics not present in tickets","All rows from both queries combined"],"answer":2},{"id":400,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT tier, NTILE(4) OVER (ORDER BY code) q FROM flags;","options":["Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Keeps only the top 4 rows","Requires PARTITION BY tier or it errors","Returns fractional percentiles 0.25 … 1.0"],"answer":0},{"id":401,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE qty = NULL;","options":["NULL cannot appear after an equality operator","The engine treats NULL as 0 for comparisons","Nothing; it returns rows where qty is NULL","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE"],"answer":3},{"id":402,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT price, user_id, COUNT(*) FROM users GROUP BY price;","options":["user_id is not in GROUP BY and not functionally dependent on price","Two non-aggregate columns cannot precede COUNT","COUNT(*) is not allowed alongside GROUP BY","GROUP BY must list every column in the table"],"answer":0},{"id":403,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(kind) > 10, where should that condition go?\nSELECT order_id, AVG(kind) FROM events GROUP BY order_id","options":["LIMIT with a sort by AVG replaces the filter","HAVING AVG(kind) > 10 — after GROUP BY","WHERE AVG(kind) > 10 — before GROUP BY","It cannot be expressed without a subquery"],"answer":1},{"id":404,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.code = b.code;","options":["Every row from skus appears; unmatched accounts columns are NULL","A Cartesian product is produced","Unmatched accounts rows are kept; matched skus rows are dropped","Only rows matching on both sides are returned"],"answer":0},{"id":405,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Only rows with equal primary keys","Duplicate-free union of both tables","An error unless an ON clause is provided","Every row of sessions paired with every row of campaigns — Cartesian product"],"answer":3},{"id":406,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT user_id FROM payments WHERE created_at > 0 GROUP BY user_id HAVING COUNT(*) > 1 ORDER BY user_id;","options":["FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","SELECT → FROM → WHERE → GROUP BY","WHERE → FROM → GROUP BY → HAVING","ORDER BY is applied before GROUP BY"],"answer":0},{"id":407,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT order_id FROM subs) UNION ALL (SELECT order_id FROM visitors);","options":["UNION ALL removes duplicates; UNION keeps them","UNION ALL keeps duplicate rows; UNION removes them","UNION ALL sorts by order_id; UNION does not","UNION ALL requires identical column names"],"answer":1},{"id":408,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT amount, ROW_NUMBER() OVER (PARTITION BY sku) rn FROM tickets;","options":["Runs, but row order within each partition is non-deterministic","Behaves like RANK() with gaps","Always numbers rows by physical storage order","Syntax error — ORDER BY is mandatory inside OVER"],"answer":0},{"id":409,"type":"theoretical","topic":"window","q":"What does LAG(status, 1) return for each row?\nSELECT score, LAG(status, 1) OVER (ORDER BY score) prev FROM shipments;","options":["The value from the next row ahead","A running sum of all prior status values","The first status in the partition for every row","The value of status from the previous row in ORDER BY sequence"],"answer":3},{"id":410,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.created_at = o.created_at);","options":["When COUNT(*) inside the subquery exceeds zero","When the subquery returns exactly one column","When the subquery returns at least one row for that outer row","When IN would return FALSE"],"answer":2},{"id":411,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(region) and COUNT(*)?\nSELECT COUNT(region), COUNT(*) FROM clicks;","options":["They are always identical","COUNT(region) counts only distinct non-NULL values","COUNT(region) skips NULLs; COUNT(*) counts every row","COUNT(*) skips NULLs; COUNT(region) counts every row"],"answer":2},{"id":412,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["FALSE","UNKNOWN (a value distinct from NULL in the SQL standard)","TRUE","NULL"],"answer":2},{"id":413,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT score FROM accounts),\n     b AS (SELECT * FROM a WHERE score > 0)\nSELECT * FROM b;","options":["Only if both are declared RECURSIVE","No — only base table accounts may be referenced","No — CTEs cannot reference each other","Yes — b may reference a defined earlier in the same WITH"],"answer":3},{"id":414,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(flag) = 'X';","options":["Applying UPPER() to flag prevents a plain index seek on flag","It forces the optimizer to use a sort-merge join","UPPER() always improves index efficiency","Functions are required for case-insensitive searches"],"answer":0},{"id":415,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["TRUNCATE removes one row at a time","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","They are identical in all databases","DELETE cannot use a WHERE clause"],"answer":1},{"id":416,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.qty = b.qty;","options":["Forgetting the WHERE condition produces a silent Cartesian product","This syntax is illegal in SQL:1999+","Cannot represent outer joins","Old-style joins are always faster than explicit JOIN"],"answer":0},{"id":417,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Always a syntax error in standard SQL","HAVING without GROUP BY is the same as WHERE","Only valid when two or more tables appear in FROM","Often yes — the whole table is treated as one implicit group"],"answer":3},{"id":418,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","They behave identically for all inputs","DENSE_RANK leaves gaps; RANK does not","Only RANK supports PARTITION BY"],"answer":0},{"id":419,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT code FROM products) AS x;","options":["When the subquery returns zero rows","When the subquery returns more than one row","When the outer query has no FROM clause","When the subquery contains GROUP BY"],"answer":1},{"id":420,"type":"theoretical","topic":"indexes","q":"Composite index on (name, status) — which filter uses the index for a seek?","options":["WHERE name = ? — satisfies the leftmost-prefix rule","ORDER BY status without any filter on name","WHERE status LIKE '%x%' alone","WHERE status = ? alone — skips the leading column"],"answer":0},{"id":421,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["Only the first column user_id","Each column independently in two passes","Primary-key rows only","The combined row (user_id, created_at) — the pair must be unique"],"answer":3},{"id":422,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(order_id) FROM coupons WHERE region IS NULL; -- assume 0 rows match","options":["An error — MAX on empty set is undefined","Zero rows are returned","NULL — aggregating an empty set returns NULL","0 (zero)"],"answer":2},{"id":423,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["INNER JOIN always returns at least one row","INNER JOIN ignores the WHERE clause","No rows satisfy the ON predicate (including when NULLs prevent matches)","NULL in ON always evaluates to TRUE"],"answer":2},{"id":424,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT status FROM metrics) EXCEPT (SELECT status FROM tickets);","options":["All rows from both queries combined","Rows in tickets not present in metrics","Rows in metrics not present in tickets","Rows present in both metrics and tickets"],"answer":2},{"id":425,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) q FROM flags;","options":["Returns fractional percentiles 0.25 … 1.0","Keeps only the top 4 rows","Requires PARTITION BY created_at or it errors","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)"],"answer":3},{"id":426,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE region = NULL;","options":["Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","NULL cannot appear after an equality operator","Nothing; it returns rows where region is NULL","The engine treats NULL as 0 for comparisons"],"answer":0},{"id":427,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT sku, qty, COUNT(*) FROM users GROUP BY sku;","options":["GROUP BY must list every column in the table","qty is not in GROUP BY and not functionally dependent on sku","COUNT(*) is not allowed alongside GROUP BY","Two non-aggregate columns cannot precede COUNT"],"answer":1},{"id":428,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(score) > 10, where should that condition go?\nSELECT price, AVG(score) FROM events GROUP BY price","options":["HAVING AVG(score) > 10 — after GROUP BY","It cannot be expressed without a subquery","LIMIT with a sort by AVG replaces the filter","WHERE AVG(score) > 10 — before GROUP BY"],"answer":0},{"id":429,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.flag = b.flag;","options":["Unmatched accounts rows are kept; matched skus rows are dropped","A Cartesian product is produced","Only rows matching on both sides are returned","Every row from skus appears; unmatched accounts columns are NULL"],"answer":3},{"id":430,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Every row of sessions paired with every row of campaigns — Cartesian product","Only rows with equal primary keys","An error unless an ON clause is provided","Duplicate-free union of both tables"],"answer":0},{"id":431,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT qty FROM payments WHERE name > 0 GROUP BY qty HAVING COUNT(*) > 1 ORDER BY qty;","options":["SELECT → FROM → WHERE → GROUP BY","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","ORDER BY is applied before GROUP BY","WHERE → FROM → GROUP BY → HAVING"],"answer":1},{"id":432,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT price FROM subs) UNION ALL (SELECT price FROM visitors);","options":["UNION ALL keeps duplicate rows; UNION removes them","UNION ALL sorts by price; UNION does not","UNION ALL requires identical column names","UNION ALL removes duplicates; UNION keeps them"],"answer":0},{"id":433,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT kind, ROW_NUMBER() OVER (PARTITION BY order_id) rn FROM tickets;","options":["Syntax error — ORDER BY is mandatory inside OVER","Behaves like RANK() with gaps","Always numbers rows by physical storage order","Runs, but row order within each partition is non-deterministic"],"answer":3},{"id":434,"type":"theoretical","topic":"window","q":"What does LAG(code, 1) return for each row?\nSELECT amount, LAG(code, 1) OVER (ORDER BY amount) prev FROM shipments;","options":["A running sum of all prior code values","The value from the next row ahead","The value of code from the previous row in ORDER BY sequence","The first code in the partition for every row"],"answer":2},{"id":435,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.name = o.name);","options":["When the subquery returns exactly one column","When IN would return FALSE","When the subquery returns at least one row for that outer row","When COUNT(*) inside the subquery exceeds zero"],"answer":2},{"id":436,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(user_id) and COUNT(*)?\nSELECT COUNT(user_id), COUNT(*) FROM clicks;","options":["COUNT(*) skips NULLs; COUNT(user_id) counts every row","COUNT(user_id) counts only distinct non-NULL values","COUNT(user_id) skips NULLs; COUNT(*) counts every row","They are always identical"],"answer":2},{"id":437,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["NULL","FALSE","UNKNOWN (a value distinct from NULL in the SQL standard)","TRUE"],"answer":3},{"id":438,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT amount FROM accounts),\n     b AS (SELECT * FROM a WHERE amount > 0)\nSELECT * FROM b;","options":["Only if both are declared RECURSIVE","No — CTEs cannot reference each other","Yes — b may reference a defined earlier in the same WITH","No — only base table accounts may be referenced"],"answer":2},{"id":439,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(status) = 'X';","options":["Functions are required for case-insensitive searches","Applying UPPER() to status prevents a plain index seek on status","UPPER() always improves index efficiency","It forces the optimizer to use a sort-merge join"],"answer":1},{"id":440,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","DELETE cannot use a WHERE clause","TRUNCATE removes one row at a time","They are identical in all databases"],"answer":0},{"id":441,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.region = b.region;","options":["This syntax is illegal in SQL:1999+","Cannot represent outer joins","Old-style joins are always faster than explicit JOIN","Forgetting the WHERE condition produces a silent Cartesian product"],"answer":3},{"id":442,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Often yes — the whole table is treated as one implicit group","Only valid when two or more tables appear in FROM","Always a syntax error in standard SQL","HAVING without GROUP BY is the same as WHERE"],"answer":0},{"id":443,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["They behave identically for all inputs","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","Only RANK supports PARTITION BY","DENSE_RANK leaves gaps; RANK does not"],"answer":1},{"id":444,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT flag FROM products) AS x;","options":["When the subquery returns more than one row","When the outer query has no FROM clause","When the subquery contains GROUP BY","When the subquery returns zero rows"],"answer":0},{"id":445,"type":"theoretical","topic":"indexes","q":"Composite index on (tier, code) — which filter uses the index for a seek?","options":["WHERE code = ? alone — skips the leading column","ORDER BY code without any filter on tier","WHERE code LIKE '%x%' alone","WHERE tier = ? — satisfies the leftmost-prefix rule"],"answer":3},{"id":446,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT qty, name FROM invoices;","options":["The combined row (qty, name) — the pair must be unique","Only the first column qty","Each column independently in two passes","Primary-key rows only"],"answer":0},{"id":447,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(price) FROM coupons WHERE user_id IS NULL; -- assume 0 rows match","options":["Zero rows are returned","0 (zero)","NULL — aggregating an empty set returns NULL","An error — MAX on empty set is undefined"],"answer":2},{"id":448,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["NULL in ON always evaluates to TRUE","INNER JOIN ignores the WHERE clause","No rows satisfy the ON predicate (including when NULLs prevent matches)","INNER JOIN always returns at least one row"],"answer":2},{"id":449,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT code FROM metrics) EXCEPT (SELECT code FROM tickets);","options":["Rows present in both metrics and tickets","All rows from both queries combined","Rows in tickets not present in metrics","Rows in metrics not present in tickets"],"answer":3},{"id":450,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT name, NTILE(4) OVER (ORDER BY status) q FROM flags;","options":["Keeps only the top 4 rows","Returns fractional percentiles 0.25 … 1.0","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Requires PARTITION BY name or it errors"],"answer":2},{"id":451,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE user_id = NULL;","options":["The engine treats NULL as 0 for comparisons","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","Nothing; it returns rows where user_id is NULL","NULL cannot appear after an equality operator"],"answer":1},{"id":452,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT order_id, region, COUNT(*) FROM users GROUP BY order_id;","options":["region is not in GROUP BY and not functionally dependent on order_id","Two non-aggregate columns cannot precede COUNT","GROUP BY must list every column in the table","COUNT(*) is not allowed alongside GROUP BY"],"answer":0},{"id":453,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(amount) > 10, where should that condition go?\nSELECT sku, AVG(amount) FROM events GROUP BY sku","options":["It cannot be expressed without a subquery","LIMIT with a sort by AVG replaces the filter","WHERE AVG(amount) > 10 — before GROUP BY","HAVING AVG(amount) > 10 — after GROUP BY"],"answer":3},{"id":454,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.status = b.status;","options":["Every row from skus appears; unmatched accounts columns are NULL","Unmatched accounts rows are kept; matched skus rows are dropped","Only rows matching on both sides are returned","A Cartesian product is produced"],"answer":0},{"id":455,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Duplicate-free union of both tables","Every row of sessions paired with every row of campaigns — Cartesian product","Only rows with equal primary keys","An error unless an ON clause is provided"],"answer":1},{"id":456,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT region FROM payments WHERE tier > 0 GROUP BY region HAVING COUNT(*) > 1 ORDER BY region;","options":["FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY","ORDER BY is applied before GROUP BY","WHERE → FROM → GROUP BY → HAVING","SELECT → FROM → WHERE → GROUP BY"],"answer":0},{"id":457,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT sku FROM subs) UNION ALL (SELECT sku FROM visitors);","options":["UNION ALL removes duplicates; UNION keeps them","UNION ALL sorts by sku; UNION does not","UNION ALL requires identical column names","UNION ALL keeps duplicate rows; UNION removes them"],"answer":3},{"id":458,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT score, ROW_NUMBER() OVER (PARTITION BY price) rn FROM tickets;","options":["Runs, but row order within each partition is non-deterministic","Syntax error — ORDER BY is mandatory inside OVER","Always numbers rows by physical storage order","Behaves like RANK() with gaps"],"answer":0},{"id":459,"type":"theoretical","topic":"window","q":"What does LAG(flag, 1) return for each row?\nSELECT kind, LAG(flag, 1) OVER (ORDER BY kind) prev FROM shipments;","options":["The value from the next row ahead","The value of flag from the previous row in ORDER BY sequence","The first flag in the partition for every row","A running sum of all prior flag values"],"answer":1},{"id":460,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.tier = o.tier);","options":["When COUNT(*) inside the subquery exceeds zero","When IN would return FALSE","When the subquery returns at least one row for that outer row","When the subquery returns exactly one column"],"answer":2},{"id":461,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(qty) and COUNT(*)?\nSELECT COUNT(qty), COUNT(*) FROM clicks;","options":["They are always identical","COUNT(*) skips NULLs; COUNT(qty) counts every row","COUNT(qty) counts only distinct non-NULL values","COUNT(qty) skips NULLs; COUNT(*) counts every row"],"answer":3},{"id":462,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["FALSE","NULL","TRUE","UNKNOWN (a value distinct from NULL in the SQL standard)"],"answer":2},{"id":463,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT kind FROM accounts),\n     b AS (SELECT * FROM a WHERE kind > 0)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","No — only base table accounts may be referenced","Yes — b may reference a defined earlier in the same WITH","Only if both are declared RECURSIVE"],"answer":2},{"id":464,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(code) = 'X';","options":["Applying UPPER() to code prevents a plain index seek on code","It forces the optimizer to use a sort-merge join","Functions are required for case-insensitive searches","UPPER() always improves index efficiency"],"answer":0},{"id":465,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["DELETE cannot use a WHERE clause","TRUNCATE removes one row at a time","They are identical in all databases","TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE"],"answer":3},{"id":466,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.user_id = b.user_id;","options":["Forgetting the WHERE condition produces a silent Cartesian product","This syntax is illegal in SQL:1999+","Old-style joins are always faster than explicit JOIN","Cannot represent outer joins"],"answer":0},{"id":467,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["HAVING without GROUP BY is the same as WHERE","Often yes — the whole table is treated as one implicit group","Always a syntax error in standard SQL","Only valid when two or more tables appear in FROM"],"answer":1},{"id":468,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers","Only RANK supports PARTITION BY","DENSE_RANK leaves gaps; RANK does not","They behave identically for all inputs"],"answer":0},{"id":469,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT status FROM products) AS x;","options":["When the subquery returns zero rows","When the outer query has no FROM clause","When the subquery contains GROUP BY","When the subquery returns more than one row"],"answer":3},{"id":470,"type":"theoretical","topic":"indexes","q":"Composite index on (created_at, flag) — which filter uses the index for a seek?","options":["WHERE created_at = ? — satisfies the leftmost-prefix rule","WHERE flag = ? alone — skips the leading column","WHERE flag LIKE '%x%' alone","ORDER BY flag without any filter on created_at"],"answer":0},{"id":471,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT region, tier FROM invoices;","options":["Only the first column region","The combined row (region, tier) — the pair must be unique","Primary-key rows only","Each column independently in two passes"],"answer":1},{"id":472,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(sku) FROM coupons WHERE qty IS NULL; -- assume 0 rows match","options":["NULL — aggregating an empty set returns NULL","0 (zero)","An error — MAX on empty set is undefined","Zero rows are returned"],"answer":0},{"id":473,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["INNER JOIN always returns at least one row","NULL in ON always evaluates to TRUE","INNER JOIN ignores the WHERE clause","No rows satisfy the ON predicate (including when NULLs prevent matches)"],"answer":3},{"id":474,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT flag FROM metrics) EXCEPT (SELECT flag FROM tickets);","options":["All rows from both queries combined","Rows present in both metrics and tickets","Rows in metrics not present in tickets","Rows in tickets not present in metrics"],"answer":2},{"id":475,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT tier, NTILE(4) OVER (ORDER BY code) q FROM flags;","options":["Returns fractional percentiles 0.25 … 1.0","Requires PARTITION BY tier or it errors","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Keeps only the top 4 rows"],"answer":2},{"id":476,"type":"theoretical","topic":"nulls","q":"What is wrong with this NULL predicate?\nSELECT * FROM orders WHERE qty = NULL;","options":["NULL cannot appear after an equality operator","The engine treats NULL as 0 for comparisons","Use IS NULL — `= NULL` evaluates to UNKNOWN, not TRUE","Nothing; it returns rows where qty is NULL"],"answer":2},{"id":477,"type":"theoretical","topic":"aggregates","q":"Why is this invalid in standard SQL?\nSELECT price, user_id, COUNT(*) FROM users GROUP BY price;","options":["Two non-aggregate columns cannot precede COUNT","GROUP BY must list every column in the table","COUNT(*) is not allowed alongside GROUP BY","user_id is not in GROUP BY and not functionally dependent on price"],"answer":3},{"id":478,"type":"theoretical","topic":"aggregates","q":"To filter groups where AVG(kind) > 10, where should that condition go?\nSELECT order_id, AVG(kind) FROM events GROUP BY order_id","options":["HAVING AVG(kind) > 10 — after GROUP BY","It cannot be expressed without a subquery","WHERE AVG(kind) > 10 — before GROUP BY","LIMIT with a sort by AVG replaces the filter"],"answer":0},{"id":479,"type":"theoretical","topic":"joins","q":"What does this LEFT JOIN guarantee?\nSELECT * FROM skus a LEFT JOIN accounts b ON a.code = b.code;","options":["A Cartesian product is produced","Every row from skus appears; unmatched accounts columns are NULL","Only rows matching on both sides are returned","Unmatched accounts rows are kept; matched skus rows are dropped"],"answer":1},{"id":480,"type":"theoretical","topic":"joins","q":"What does CROSS JOIN produce?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["Every row of sessions paired with every row of campaigns — Cartesian product","An error unless an ON clause is provided","Duplicate-free union of both tables","Only rows with equal primary keys"],"answer":0},{"id":481,"type":"theoretical","topic":"basics","q":"In what logical order are the clauses processed?\nSELECT user_id FROM payments WHERE created_at > 0 GROUP BY user_id HAVING COUNT(*) > 1 ORDER BY user_id;","options":["SELECT → FROM → WHERE → GROUP BY","ORDER BY is applied before GROUP BY","WHERE → FROM → GROUP BY → HAVING","FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY"],"answer":3},{"id":482,"type":"theoretical","topic":"set_ops","q":"How does UNION ALL differ from UNION?\n(SELECT order_id FROM subs) UNION ALL (SELECT order_id FROM visitors);","options":["UNION ALL keeps duplicate rows; UNION removes them","UNION ALL removes duplicates; UNION keeps them","UNION ALL requires identical column names","UNION ALL sorts by order_id; UNION does not"],"answer":0},{"id":483,"type":"theoretical","topic":"window","q":"What happens when ROW_NUMBER() has no ORDER BY inside OVER?\nSELECT amount, ROW_NUMBER() OVER (PARTITION BY sku) rn FROM tickets;","options":["Syntax error — ORDER BY is mandatory inside OVER","Runs, but row order within each partition is non-deterministic","Behaves like RANK() with gaps","Always numbers rows by physical storage order"],"answer":1},{"id":484,"type":"theoretical","topic":"window","q":"What does LAG(status, 1) return for each row?\nSELECT score, LAG(status, 1) OVER (ORDER BY score) prev FROM shipments;","options":["The value of status from the previous row in ORDER BY sequence","The first status in the partition for every row","A running sum of all prior status values","The value from the next row ahead"],"answer":0},{"id":485,"type":"theoretical","topic":"subqueries","q":"When does EXISTS return TRUE?\nSELECT * FROM refunds o WHERE EXISTS (SELECT 1 FROM products i WHERE i.created_at = o.created_at);","options":["When the subquery returns exactly one column","When COUNT(*) inside the subquery exceeds zero","When IN would return FALSE","When the subquery returns at least one row for that outer row"],"answer":3},{"id":486,"type":"theoretical","topic":"aggregates","q":"What is the difference between COUNT(region) and COUNT(*)?\nSELECT COUNT(region), COUNT(*) FROM clicks;","options":["COUNT(*) skips NULLs; COUNT(region) counts every row","They are always identical","COUNT(region) skips NULLs; COUNT(*) counts every row","COUNT(region) counts only distinct non-NULL values"],"answer":2},{"id":487,"type":"theoretical","topic":"nulls","q":"In SQL three-valued logic, what does NULL OR TRUE evaluate to?","options":["NULL","UNKNOWN (a value distinct from NULL in the SQL standard)","TRUE","FALSE"],"answer":2},{"id":488,"type":"theoretical","topic":"cte","q":"Can a later CTE reference an earlier one in the same WITH?\nWITH a AS (SELECT score FROM accounts),\n     b AS (SELECT * FROM a WHERE score > 0)\nSELECT * FROM b;","options":["Only if both are declared RECURSIVE","No — only base table accounts may be referenced","Yes — b may reference a defined earlier in the same WITH","No — CTEs cannot reference each other"],"answer":2},{"id":489,"type":"theoretical","topic":"optimization","q":"Why may wrapping a column in a function hurt performance?\nSELECT * FROM campaigns WHERE UPPER(flag) = 'X';","options":["UPPER() always improves index efficiency","It forces the optimizer to use a sort-merge join","Functions are required for case-insensitive searches","Applying UPPER() to flag prevents a plain index seek on flag"],"answer":3},{"id":490,"type":"theoretical","topic":"dml_ddl","q":"What is the key difference between these two?\nTRUNCATE TABLE leads;\nDELETE FROM leads;","options":["TRUNCATE removes all rows with minimal logging; DELETE is row-by-row and accepts a WHERE","DELETE cannot use a WHERE clause","They are identical in all databases","TRUNCATE removes one row at a time"],"answer":0},{"id":491,"type":"theoretical","topic":"joins","q":"What is the main risk with old-style implicit joins?\nSELECT * FROM visitors a, flags b WHERE a.qty = b.qty;","options":["Cannot represent outer joins","Forgetting the WHERE condition produces a silent Cartesian product","Old-style joins are always faster than explicit JOIN","This syntax is illegal in SQL:1999+"],"answer":1},{"id":492,"type":"theoretical","topic":"aggregates","q":"Is HAVING valid without GROUP BY?","options":["Often yes — the whole table is treated as one implicit group","Only valid when two or more tables appear in FROM","HAVING without GROUP BY is the same as WHERE","Always a syntax error in standard SQL"],"answer":0},{"id":493,"type":"theoretical","topic":"window","q":"How do RANK() and DENSE_RANK() differ after a tie?","options":["DENSE_RANK leaves gaps; RANK does not","Only RANK supports PARTITION BY","They behave identically for all inputs","RANK leaves gaps after tied groups; DENSE_RANK uses consecutive integers"],"answer":3},{"id":494,"type":"theoretical","topic":"subqueries","q":"When does a scalar subquery cause a runtime error?\nSELECT (SELECT code FROM products) AS x;","options":["When the subquery returns more than one row","When the subquery returns zero rows","When the subquery contains GROUP BY","When the outer query has no FROM clause"],"answer":0},{"id":495,"type":"theoretical","topic":"indexes","q":"Composite index on (name, status) — which filter uses the index for a seek?","options":["WHERE status = ? alone — skips the leading column","WHERE name = ? — satisfies the leftmost-prefix rule","ORDER BY status without any filter on name","WHERE status LIKE '%x%' alone"],"answer":1},{"id":496,"type":"theoretical","topic":"basics","q":"What does DISTINCT apply to here?\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["The combined row (user_id, created_at) — the pair must be unique","Primary-key rows only","Each column independently in two passes","Only the first column user_id"],"answer":0},{"id":497,"type":"theoretical","topic":"aggregates","q":"What does MAX return when no rows match?\nSELECT MAX(order_id) FROM coupons WHERE region IS NULL; -- assume 0 rows match","options":["Zero rows are returned","0 (zero)","An error — MAX on empty set is undefined","NULL — aggregating an empty set returns NULL"],"answer":3},{"id":498,"type":"theoretical","topic":"joins","q":"Why might an INNER JOIN return zero rows?","options":["NULL in ON always evaluates to TRUE","INNER JOIN always returns at least one row","No rows satisfy the ON predicate (including when NULLs prevent matches)","INNER JOIN ignores the WHERE clause"],"answer":2},{"id":499,"type":"theoretical","topic":"set_ops","q":"What does EXCEPT return?\n(SELECT status FROM metrics) EXCEPT (SELECT status FROM tickets);","options":["Rows present in both metrics and tickets","Rows in tickets not present in metrics","Rows in metrics not present in tickets","All rows from both queries combined"],"answer":2},{"id":500,"type":"theoretical","topic":"window","q":"What does NTILE(4) do?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) q FROM flags;","options":["Keeps only the top 4 rows","Requires PARTITION BY created_at or it errors","Splits ordered rows into 4 nearly equal-sized buckets (larger ones come first)","Returns fractional percentiles 0.25 … 1.0"],"answer":2},{"id":501,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE user_id = NULL;","options":["All rows where user_id is NULL","A syntax error at runtime","Same result as WHERE user_id IS NOT NULL","Zero rows — = NULL always evaluates to UNKNOWN"],"answer":3},{"id":502,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT order_id, region, COUNT(*)\nFROM users\nGROUP BY order_id;","options":["Error: region is in SELECT but not in GROUP BY","Error: COUNT(*) cannot appear with a GROUP BY","Valid — any column can appear alongside an aggregate","Error: GROUP BY must use an alias, not a column name"],"answer":0},{"id":503,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT amount, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY amount;","options":["WHERE and HAVING are interchangeable here","Error — aggregate functions are not allowed in WHERE; use HAVING","Returns groups with count > 2 correctly","Runs fine; WHERE filters rows before aggregation"],"answer":1},{"id":504,"type":"practical","topic":"joins","q":"skus has a row where status = 999; accounts has no matching row.\nWhat appears in b.score for that skus row?\nSELECT a.status, b.score\nFROM skus a\nLEFT JOIN accounts b ON a.status = b.status;","options":["NULL — unmatched left rows are kept with NULL on the right side","An error is raised for the unmatched key","b.score takes the column's default value","The row is excluded from the result"],"answer":0},{"id":505,"type":"practical","topic":"joins","q":"sessions has 7 rows; campaigns has 2 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["9 — rows from both tables appended","2 — only matching-key rows","7 — only sessions rows","14 — Cartesian product (7 × 2)"],"answer":3},{"id":506,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT tier, COUNT(*) cnt\nFROM payments\nWHERE region > 0\nGROUP BY tier\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["FROM","WHERE","SELECT","ORDER BY"],"answer":0},{"id":507,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT sku FROM subs)\nUNION ALL\n(SELECT sku FROM visitors);","options":["The duplicate is removed — UNION ALL deduplicates","Both copies are kept — UNION ALL never removes duplicates","Only one of the two queries executes","An error is raised for the duplicate row"],"answer":1},{"id":508,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT score,\n       ROW_NUMBER() OVER (PARTITION BY price) rn\nFROM tickets;","options":["No — without ORDER BY inside OVER, order within each partition is undefined","Error — ORDER BY is required inside OVER with ROW_NUMBER","Yes — ROW_NUMBER is always deterministic","Yes — partitions are always sorted by insertion order"],"answer":0},{"id":509,"type":"practical","topic":"window","q":"What is prev_flag for the very first row in the ordering?\nSELECT kind,\n       flag,\n       LAG(flag) OVER (ORDER BY kind) AS prev_flag\nFROM shipments;","options":["The value of flag for the last row in the table","An error is raised for the first row","0 (zero default when there is no previous row)","NULL — no preceding row exists for the first row"],"answer":3},{"id":510,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.tier = o.tier AND 1 = 0\n);","options":["FALSE — EXISTS is false when the subquery returns no rows","TRUE — EXISTS ignores the inner WHERE conditions","NULL — three-valued logic makes this unknown","Error — subquery must always return at least one row"],"answer":0},{"id":511,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; qty is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(qty) FROM clicks;","options":["4, 4 — both functions skip NULLs","Error — you cannot mix COUNT(*) and COUNT(qty)","6, 4 — COUNT(*) counts all rows; COUNT(qty) skips NULLs","6, 6 — both functions count all rows"],"answer":2},{"id":512,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["TRUE","UNKNOWN (distinct from NULL in the standard)","FALSE — AND with FALSE is always FALSE regardless of the other operand","NULL"],"answer":2},{"id":513,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT kind FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE kind > 0\n)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","No — WITH must contain exactly one named query","Only if both CTEs are declared RECURSIVE","Yes — b can reference a defined earlier in the same WITH"],"answer":3},{"id":514,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on code?\nSELECT * FROM campaigns\nWHERE UPPER(code) = 'ACTIVE';","options":["The optimizer always ignores scalar functions in WHERE","UPPER() always improves index efficiency","UPPER() wraps code, preventing an index seek on the raw column","Indexes do not work with string columns"],"answer":2},{"id":515,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE name IS NOT NULL;","options":["B removes only non-NULL rows; A removes nothing","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","Both statements are identical in every database","A can be rolled back; B cannot"],"answer":1},{"id":516,"type":"practical","topic":"joins","q":"visitors has 6 rows; flags has 5 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["30 — Cartesian product from the missing join condition","11 — rows from both tables appended","0 — missing WHERE means no rows pass","6 — only visitors rows"],"answer":0},{"id":517,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — COUNT(*) cannot appear in both SELECT and HAVING","Returns every row of devices filtered by the count","Error — HAVING requires a GROUP BY clause","Yes — the whole table is one implicit group; returns one row if any rows exist"],"answer":3},{"id":518,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(3, 3) — both leave a gap after a tie","(2, 2) — both functions agree after a tie","(2, 3) — DENSE_RANK skips; RANK does not"],"answer":0},{"id":519,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT status,\n       (SELECT score FROM events) AS x\nFROM products;","options":["The first row's score value is silently used","Runtime error — scalar subquery returned more than one row","Three copies of each products row are returned","The query auto-aggregates events to one row"],"answer":1},{"id":520,"type":"practical","topic":"indexes","q":"An index exists on (created_at, flag).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE flag = 'active';","options":["No — filtering only flag skips the leading column created_at; leftmost-prefix rule not satisfied","No — composite indexes never speed up equality predicates","Yes — flag is in the index so seeks always work","Yes — composite indexes cover any subset of their columns equally"],"answer":0},{"id":521,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT region, tier FROM invoices;","options":["2 — only unique values of the first column region","1 — DISTINCT keeps only the row with the lowest region","4 — DISTINCT has no effect on two-column selects","3 — DISTINCT deduplicates the (region, tier) pair; (1,'a') appears once"],"answer":3},{"id":522,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(sku) FROM coupons\nWHERE 1 = 0;","options":["One row with MAX = NULL","Zero rows returned","Error — MAX on an empty set is undefined","One row with MAX = 0"],"answer":0},{"id":523,"type":"practical","topic":"joins","q":"Both tables have rows where score IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.score = b.score;","options":["Yes — NULLs are treated as equal in ON conditions","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Yes — but only with INNER JOIN, not OUTER JOIN","Only if both sides are NULL simultaneously (special NULL-safe rule)"],"answer":1},{"id":524,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT flag FROM metrics)\nEXCEPT\n(SELECT flag FROM tickets);","options":["All values from both metrics and tickets","Values in tickets that do not appear in metrics","Values in metrics that do not appear in tickets","Values that appear in both metrics and tickets"],"answer":2},{"id":525,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT tier, NTILE(4) OVER (ORDER BY code) bucket\nFROM flags;","options":["2, 2, 3, 3 — smaller buckets come first","4, 3, 2, 1 — strictly decreasing bucket sizes","All 4 buckets have exactly 2 rows (remainder discarded)","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first"],"answer":3},{"id":526,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE qty = NULL;","options":["A syntax error at runtime","All rows where qty is NULL","Zero rows — = NULL always evaluates to UNKNOWN","Same result as WHERE qty IS NOT NULL"],"answer":2},{"id":527,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT price, user_id, COUNT(*)\nFROM users\nGROUP BY price;","options":["Valid — any column can appear alongside an aggregate","Error: GROUP BY must use an alias, not a column name","Error: user_id is in SELECT but not in GROUP BY","Error: COUNT(*) cannot appear with a GROUP BY"],"answer":2},{"id":528,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT kind, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY kind;","options":["Error — aggregate functions are not allowed in WHERE; use HAVING","Runs fine; WHERE filters rows before aggregation","WHERE and HAVING are interchangeable here","Returns groups with count > 2 correctly"],"answer":0},{"id":529,"type":"practical","topic":"joins","q":"skus has a row where code = 999; accounts has no matching row.\nWhat appears in b.amount for that skus row?\nSELECT a.code, b.amount\nFROM skus a\nLEFT JOIN accounts b ON a.code = b.code;","options":["An error is raised for the unmatched key","b.amount takes the column's default value","The row is excluded from the result","NULL — unmatched left rows are kept with NULL on the right side"],"answer":3},{"id":530,"type":"practical","topic":"joins","q":"sessions has 8 rows; campaigns has 3 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["24 — Cartesian product (8 × 3)","11 — rows from both tables appended","8 — only sessions rows","3 — only matching-key rows"],"answer":0},{"id":531,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT created_at, COUNT(*) cnt\nFROM payments\nWHERE user_id > 0\nGROUP BY created_at\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["ORDER BY","FROM","SELECT","WHERE"],"answer":1},{"id":532,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT order_id FROM subs)\nUNION ALL\n(SELECT order_id FROM visitors);","options":["Both copies are kept — UNION ALL never removes duplicates","Only one of the two queries executes","An error is raised for the duplicate row","The duplicate is removed — UNION ALL deduplicates"],"answer":0},{"id":533,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT amount,\n       ROW_NUMBER() OVER (PARTITION BY sku) rn\nFROM tickets;","options":["Yes — partitions are always sorted by insertion order","Error — ORDER BY is required inside OVER with ROW_NUMBER","Yes — ROW_NUMBER is always deterministic","No — without ORDER BY inside OVER, order within each partition is undefined"],"answer":3},{"id":534,"type":"practical","topic":"window","q":"What is prev_status for the very first row in the ordering?\nSELECT score,\n       status,\n       LAG(status) OVER (ORDER BY score) AS prev_status\nFROM shipments;","options":["NULL — no preceding row exists for the first row","The value of status for the last row in the table","0 (zero default when there is no previous row)","An error is raised for the first row"],"answer":0},{"id":535,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.created_at = o.created_at AND 1 = 0\n);","options":["TRUE — EXISTS ignores the inner WHERE conditions","FALSE — EXISTS is false when the subquery returns no rows","Error — subquery must always return at least one row","NULL — three-valued logic makes this unknown"],"answer":1},{"id":536,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; region is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(region) FROM clicks;","options":["6, 6 — both functions count all rows","Error — you cannot mix COUNT(*) and COUNT(region)","6, 4 — COUNT(*) counts all rows; COUNT(region) skips NULLs","4, 4 — both functions skip NULLs"],"answer":2},{"id":537,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["NULL","TRUE","UNKNOWN (distinct from NULL in the standard)","FALSE — AND with FALSE is always FALSE regardless of the other operand"],"answer":3},{"id":538,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT score FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE score > 0\n)\nSELECT * FROM b;","options":["No — WITH must contain exactly one named query","No — CTEs cannot reference each other","Yes — b can reference a defined earlier in the same WITH","Only if both CTEs are declared RECURSIVE"],"answer":2},{"id":539,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on flag?\nSELECT * FROM campaigns\nWHERE UPPER(flag) = 'ACTIVE';","options":["UPPER() always improves index efficiency","Indexes do not work with string columns","UPPER() wraps flag, preventing an index seek on the raw column","The optimizer always ignores scalar functions in WHERE"],"answer":2},{"id":540,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE tier IS NOT NULL;","options":["A can be rolled back; B cannot","B removes only non-NULL rows; A removes nothing","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","Both statements are identical in every database"],"answer":2},{"id":541,"type":"practical","topic":"joins","q":"visitors has 7 rows; flags has 2 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["9 — rows from both tables appended","0 — missing WHERE means no rows pass","7 — only visitors rows","14 — Cartesian product from the missing join condition"],"answer":3},{"id":542,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Yes — the whole table is one implicit group; returns one row if any rows exist","Error — COUNT(*) cannot appear in both SELECT and HAVING","Error — HAVING requires a GROUP BY clause","Returns every row of devices filtered by the count"],"answer":0},{"id":543,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(2, 3) — DENSE_RANK skips; RANK does not","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(2, 2) — both functions agree after a tie","(3, 3) — both leave a gap after a tie"],"answer":1},{"id":544,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT code,\n       (SELECT amount FROM events) AS x\nFROM products;","options":["Runtime error — scalar subquery returned more than one row","The query auto-aggregates events to one row","Three copies of each products row are returned","The first row's amount value is silently used"],"answer":0},{"id":545,"type":"practical","topic":"indexes","q":"An index exists on (name, status).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE status = 'active';","options":["Yes — composite indexes cover any subset of their columns equally","No — composite indexes never speed up equality predicates","Yes — status is in the index so seeks always work","No — filtering only status skips the leading column name; leftmost-prefix rule not satisfied"],"answer":3},{"id":546,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["3 — DISTINCT deduplicates the (user_id, created_at) pair; (1,'a') appears once","2 — only unique values of the first column user_id","4 — DISTINCT has no effect on two-column selects","1 — DISTINCT keeps only the row with the lowest user_id"],"answer":0},{"id":547,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(order_id) FROM coupons\nWHERE 1 = 0;","options":["Zero rows returned","One row with MAX = NULL","One row with MAX = 0","Error — MAX on an empty set is undefined"],"answer":1},{"id":548,"type":"practical","topic":"joins","q":"Both tables have rows where amount IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.amount = b.amount;","options":["No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Yes — but only with INNER JOIN, not OUTER JOIN","Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — NULLs are treated as equal in ON conditions"],"answer":0},{"id":549,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT status FROM metrics)\nEXCEPT\n(SELECT status FROM tickets);","options":["Values that appear in both metrics and tickets","All values from both metrics and tickets","Values in tickets that do not appear in metrics","Values in metrics that do not appear in tickets"],"answer":3},{"id":550,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) bucket\nFROM flags;","options":["4, 3, 2, 1 — strictly decreasing bucket sizes","2, 2, 3, 3 — smaller buckets come first","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","All 4 buckets have exactly 2 rows (remainder discarded)"],"answer":2},{"id":551,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE region = NULL;","options":["All rows where region is NULL","Same result as WHERE region IS NOT NULL","Zero rows — = NULL always evaluates to UNKNOWN","A syntax error at runtime"],"answer":2},{"id":552,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT sku, qty, COUNT(*)\nFROM users\nGROUP BY sku;","options":["Error: COUNT(*) cannot appear with a GROUP BY","Error: GROUP BY must use an alias, not a column name","Error: qty is in SELECT but not in GROUP BY","Valid — any column can appear alongside an aggregate"],"answer":2},{"id":553,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT score, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY score;","options":["Returns groups with count > 2 correctly","Runs fine; WHERE filters rows before aggregation","WHERE and HAVING are interchangeable here","Error — aggregate functions are not allowed in WHERE; use HAVING"],"answer":3},{"id":554,"type":"practical","topic":"joins","q":"skus has a row where flag = 999; accounts has no matching row.\nWhat appears in b.kind for that skus row?\nSELECT a.flag, b.kind\nFROM skus a\nLEFT JOIN accounts b ON a.flag = b.flag;","options":["NULL — unmatched left rows are kept with NULL on the right side","An error is raised for the unmatched key","The row is excluded from the result","b.kind takes the column's default value"],"answer":0},{"id":555,"type":"practical","topic":"joins","q":"sessions has 3 rows; campaigns has 4 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["3 — only matching-key rows","12 — Cartesian product (3 × 4)","3 — only sessions rows","7 — rows from both tables appended"],"answer":1},{"id":556,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT name, COUNT(*) cnt\nFROM payments\nWHERE qty > 0\nGROUP BY name\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["FROM","WHERE","ORDER BY","SELECT"],"answer":0},{"id":557,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT price FROM subs)\nUNION ALL\n(SELECT price FROM visitors);","options":["An error is raised for the duplicate row","Only one of the two queries executes","The duplicate is removed — UNION ALL deduplicates","Both copies are kept — UNION ALL never removes duplicates"],"answer":3},{"id":558,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT kind,\n       ROW_NUMBER() OVER (PARTITION BY order_id) rn\nFROM tickets;","options":["No — without ORDER BY inside OVER, order within each partition is undefined","Yes — partitions are always sorted by insertion order","Yes — ROW_NUMBER is always deterministic","Error — ORDER BY is required inside OVER with ROW_NUMBER"],"answer":0},{"id":559,"type":"practical","topic":"window","q":"What is prev_code for the very first row in the ordering?\nSELECT amount,\n       code,\n       LAG(code) OVER (ORDER BY amount) AS prev_code\nFROM shipments;","options":["The value of code for the last row in the table","NULL — no preceding row exists for the first row","An error is raised for the first row","0 (zero default when there is no previous row)"],"answer":1},{"id":560,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.name = o.name AND 1 = 0\n);","options":["FALSE — EXISTS is false when the subquery returns no rows","Error — subquery must always return at least one row","NULL — three-valued logic makes this unknown","TRUE — EXISTS ignores the inner WHERE conditions"],"answer":0},{"id":561,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; user_id is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(user_id) FROM clicks;","options":["4, 4 — both functions skip NULLs","Error — you cannot mix COUNT(*) and COUNT(user_id)","6, 6 — both functions count all rows","6, 4 — COUNT(*) counts all rows; COUNT(user_id) skips NULLs"],"answer":3},{"id":562,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["TRUE","NULL","FALSE — AND with FALSE is always FALSE regardless of the other operand","UNKNOWN (distinct from NULL in the standard)"],"answer":2},{"id":563,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT amount FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE amount > 0\n)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","Only if both CTEs are declared RECURSIVE","Yes — b can reference a defined earlier in the same WITH","No — WITH must contain exactly one named query"],"answer":2},{"id":564,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on status?\nSELECT * FROM campaigns\nWHERE UPPER(status) = 'ACTIVE';","options":["The optimizer always ignores scalar functions in WHERE","Indexes do not work with string columns","UPPER() wraps status, preventing an index seek on the raw column","UPPER() always improves index efficiency"],"answer":2},{"id":565,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE created_at IS NOT NULL;","options":["Both statements are identical in every database","A can be rolled back; B cannot","B removes only non-NULL rows; A removes nothing","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging"],"answer":3},{"id":566,"type":"practical","topic":"joins","q":"visitors has 8 rows; flags has 3 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["24 — Cartesian product from the missing join condition","11 — rows from both tables appended","8 — only visitors rows","0 — missing WHERE means no rows pass"],"answer":0},{"id":567,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Returns every row of devices filtered by the count","Yes — the whole table is one implicit group; returns one row if any rows exist","Error — HAVING requires a GROUP BY clause","Error — COUNT(*) cannot appear in both SELECT and HAVING"],"answer":1},{"id":568,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(3, 3) — both leave a gap after a tie","(2, 3) — DENSE_RANK skips; RANK does not","(2, 2) — both functions agree after a tie"],"answer":0},{"id":569,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT flag,\n       (SELECT kind FROM events) AS x\nFROM products;","options":["The query auto-aggregates events to one row","Three copies of each products row are returned","The first row's kind value is silently used","Runtime error — scalar subquery returned more than one row"],"answer":3},{"id":570,"type":"practical","topic":"indexes","q":"An index exists on (tier, code).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE code = 'active';","options":["No — filtering only code skips the leading column tier; leftmost-prefix rule not satisfied","Yes — code is in the index so seeks always work","Yes — composite indexes cover any subset of their columns equally","No — composite indexes never speed up equality predicates"],"answer":0},{"id":571,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT qty, name FROM invoices;","options":["2 — only unique values of the first column qty","3 — DISTINCT deduplicates the (qty, name) pair; (1,'a') appears once","1 — DISTINCT keeps only the row with the lowest qty","4 — DISTINCT has no effect on two-column selects"],"answer":1},{"id":572,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(price) FROM coupons\nWHERE 1 = 0;","options":["One row with MAX = NULL","One row with MAX = 0","Error — MAX on an empty set is undefined","Zero rows returned"],"answer":0},{"id":573,"type":"practical","topic":"joins","q":"Both tables have rows where kind IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.kind = b.kind;","options":["Yes — NULLs are treated as equal in ON conditions","Yes — but only with INNER JOIN, not OUTER JOIN","Only if both sides are NULL simultaneously (special NULL-safe rule)","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN"],"answer":3},{"id":574,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT code FROM metrics)\nEXCEPT\n(SELECT code FROM tickets);","options":["Values in metrics that do not appear in tickets","Values that appear in both metrics and tickets","All values from both metrics and tickets","Values in tickets that do not appear in metrics"],"answer":0},{"id":575,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT name, NTILE(4) OVER (ORDER BY status) bucket\nFROM flags;","options":["2, 2, 3, 3 — smaller buckets come first","All 4 buckets have exactly 2 rows (remainder discarded)","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","4, 3, 2, 1 — strictly decreasing bucket sizes"],"answer":2},{"id":576,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE user_id = NULL;","options":["A syntax error at runtime","Same result as WHERE user_id IS NOT NULL","Zero rows — = NULL always evaluates to UNKNOWN","All rows where user_id is NULL"],"answer":2},{"id":577,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT order_id, region, COUNT(*)\nFROM users\nGROUP BY order_id;","options":["Valid — any column can appear alongside an aggregate","Error: COUNT(*) cannot appear with a GROUP BY","Error: GROUP BY must use an alias, not a column name","Error: region is in SELECT but not in GROUP BY"],"answer":3},{"id":578,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT amount, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY amount;","options":["Runs fine; WHERE filters rows before aggregation","Returns groups with count > 2 correctly","Error — aggregate functions are not allowed in WHERE; use HAVING","WHERE and HAVING are interchangeable here"],"answer":2},{"id":579,"type":"practical","topic":"joins","q":"skus has a row where status = 999; accounts has no matching row.\nWhat appears in b.score for that skus row?\nSELECT a.status, b.score\nFROM skus a\nLEFT JOIN accounts b ON a.status = b.status;","options":["b.score takes the column's default value","NULL — unmatched left rows are kept with NULL on the right side","The row is excluded from the result","An error is raised for the unmatched key"],"answer":1},{"id":580,"type":"practical","topic":"joins","q":"sessions has 4 rows; campaigns has 5 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["20 — Cartesian product (4 × 5)","9 — rows from both tables appended","4 — only matching-key rows","4 — only sessions rows"],"answer":0},{"id":581,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT tier, COUNT(*) cnt\nFROM payments\nWHERE region > 0\nGROUP BY tier\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["WHERE","ORDER BY","SELECT","FROM"],"answer":3},{"id":582,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT sku FROM subs)\nUNION ALL\n(SELECT sku FROM visitors);","options":["Both copies are kept — UNION ALL never removes duplicates","An error is raised for the duplicate row","The duplicate is removed — UNION ALL deduplicates","Only one of the two queries executes"],"answer":0},{"id":583,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT score,\n       ROW_NUMBER() OVER (PARTITION BY price) rn\nFROM tickets;","options":["Yes — partitions are always sorted by insertion order","No — without ORDER BY inside OVER, order within each partition is undefined","Error — ORDER BY is required inside OVER with ROW_NUMBER","Yes — ROW_NUMBER is always deterministic"],"answer":1},{"id":584,"type":"practical","topic":"window","q":"What is prev_flag for the very first row in the ordering?\nSELECT kind,\n       flag,\n       LAG(flag) OVER (ORDER BY kind) AS prev_flag\nFROM shipments;","options":["NULL — no preceding row exists for the first row","An error is raised for the first row","0 (zero default when there is no previous row)","The value of flag for the last row in the table"],"answer":0},{"id":585,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.tier = o.tier AND 1 = 0\n);","options":["TRUE — EXISTS ignores the inner WHERE conditions","Error — subquery must always return at least one row","NULL — three-valued logic makes this unknown","FALSE — EXISTS is false when the subquery returns no rows"],"answer":3},{"id":586,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; qty is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(qty) FROM clicks;","options":["6, 4 — COUNT(*) counts all rows; COUNT(qty) skips NULLs","4, 4 — both functions skip NULLs","6, 6 — both functions count all rows","Error — you cannot mix COUNT(*) and COUNT(qty)"],"answer":0},{"id":587,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["NULL","FALSE — AND with FALSE is always FALSE regardless of the other operand","UNKNOWN (distinct from NULL in the standard)","TRUE"],"answer":1},{"id":588,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT kind FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE kind > 0\n)\nSELECT * FROM b;","options":["No — WITH must contain exactly one named query","Only if both CTEs are declared RECURSIVE","Yes — b can reference a defined earlier in the same WITH","No — CTEs cannot reference each other"],"answer":2},{"id":589,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on code?\nSELECT * FROM campaigns\nWHERE UPPER(code) = 'ACTIVE';","options":["UPPER() always improves index efficiency","The optimizer always ignores scalar functions in WHERE","Indexes do not work with string columns","UPPER() wraps code, preventing an index seek on the raw column"],"answer":3},{"id":590,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE name IS NOT NULL;","options":["A can be rolled back; B cannot","Both statements are identical in every database","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","B removes only non-NULL rows; A removes nothing"],"answer":2},{"id":591,"type":"practical","topic":"joins","q":"visitors has 3 rows; flags has 4 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["3 — only visitors rows","0 — missing WHERE means no rows pass","12 — Cartesian product from the missing join condition","7 — rows from both tables appended"],"answer":2},{"id":592,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Yes — the whole table is one implicit group; returns one row if any rows exist","Error — COUNT(*) cannot appear in both SELECT and HAVING","Returns every row of devices filtered by the count","Error — HAVING requires a GROUP BY clause"],"answer":0},{"id":593,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(3, 3) — both leave a gap after a tie","(2, 3) — DENSE_RANK skips; RANK does not","(2, 2) — both functions agree after a tie","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2"],"answer":3},{"id":594,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT status,\n       (SELECT score FROM events) AS x\nFROM products;","options":["Runtime error — scalar subquery returned more than one row","The query auto-aggregates events to one row","The first row's score value is silently used","Three copies of each products row are returned"],"answer":0},{"id":595,"type":"practical","topic":"indexes","q":"An index exists on (created_at, flag).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE flag = 'active';","options":["No — composite indexes never speed up equality predicates","No — filtering only flag skips the leading column created_at; leftmost-prefix rule not satisfied","Yes — composite indexes cover any subset of their columns equally","Yes — flag is in the index so seeks always work"],"answer":1},{"id":596,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT region, tier FROM invoices;","options":["3 — DISTINCT deduplicates the (region, tier) pair; (1,'a') appears once","1 — DISTINCT keeps only the row with the lowest region","4 — DISTINCT has no effect on two-column selects","2 — only unique values of the first column region"],"answer":0},{"id":597,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(sku) FROM coupons\nWHERE 1 = 0;","options":["Zero rows returned","One row with MAX = 0","Error — MAX on an empty set is undefined","One row with MAX = NULL"],"answer":3},{"id":598,"type":"practical","topic":"joins","q":"Both tables have rows where score IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.score = b.score;","options":["No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Yes — NULLs are treated as equal in ON conditions","Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — but only with INNER JOIN, not OUTER JOIN"],"answer":0},{"id":599,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT flag FROM metrics)\nEXCEPT\n(SELECT flag FROM tickets);","options":["Values that appear in both metrics and tickets","Values in metrics that do not appear in tickets","Values in tickets that do not appear in metrics","All values from both metrics and tickets"],"answer":1},{"id":600,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT tier, NTILE(4) OVER (ORDER BY code) bucket\nFROM flags;","options":["4, 3, 2, 1 — strictly decreasing bucket sizes","All 4 buckets have exactly 2 rows (remainder discarded)","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","2, 2, 3, 3 — smaller buckets come first"],"answer":2},{"id":601,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE qty = NULL;","options":["All rows where qty is NULL","A syntax error at runtime","Same result as WHERE qty IS NOT NULL","Zero rows — = NULL always evaluates to UNKNOWN"],"answer":3},{"id":602,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT price, user_id, COUNT(*)\nFROM users\nGROUP BY price;","options":["Error: COUNT(*) cannot appear with a GROUP BY","Valid — any column can appear alongside an aggregate","Error: user_id is in SELECT but not in GROUP BY","Error: GROUP BY must use an alias, not a column name"],"answer":2},{"id":603,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT kind, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY kind;","options":["Returns groups with count > 2 correctly","WHERE and HAVING are interchangeable here","Error — aggregate functions are not allowed in WHERE; use HAVING","Runs fine; WHERE filters rows before aggregation"],"answer":2},{"id":604,"type":"practical","topic":"joins","q":"skus has a row where code = 999; accounts has no matching row.\nWhat appears in b.amount for that skus row?\nSELECT a.code, b.amount\nFROM skus a\nLEFT JOIN accounts b ON a.code = b.code;","options":["An error is raised for the unmatched key","b.amount takes the column's default value","NULL — unmatched left rows are kept with NULL on the right side","The row is excluded from the result"],"answer":2},{"id":605,"type":"practical","topic":"joins","q":"sessions has 5 rows; campaigns has 2 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["7 — rows from both tables appended","2 — only matching-key rows","5 — only sessions rows","10 — Cartesian product (5 × 2)"],"answer":3},{"id":606,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT created_at, COUNT(*) cnt\nFROM payments\nWHERE user_id > 0\nGROUP BY created_at\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["FROM","WHERE","SELECT","ORDER BY"],"answer":0},{"id":607,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT order_id FROM subs)\nUNION ALL\n(SELECT order_id FROM visitors);","options":["Only one of the two queries executes","Both copies are kept — UNION ALL never removes duplicates","The duplicate is removed — UNION ALL deduplicates","An error is raised for the duplicate row"],"answer":1},{"id":608,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT amount,\n       ROW_NUMBER() OVER (PARTITION BY sku) rn\nFROM tickets;","options":["No — without ORDER BY inside OVER, order within each partition is undefined","Yes — ROW_NUMBER is always deterministic","Error — ORDER BY is required inside OVER with ROW_NUMBER","Yes — partitions are always sorted by insertion order"],"answer":0},{"id":609,"type":"practical","topic":"window","q":"What is prev_status for the very first row in the ordering?\nSELECT score,\n       status,\n       LAG(status) OVER (ORDER BY score) AS prev_status\nFROM shipments;","options":["The value of status for the last row in the table","An error is raised for the first row","0 (zero default when there is no previous row)","NULL — no preceding row exists for the first row"],"answer":3},{"id":610,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.created_at = o.created_at AND 1 = 0\n);","options":["FALSE — EXISTS is false when the subquery returns no rows","TRUE — EXISTS ignores the inner WHERE conditions","NULL — three-valued logic makes this unknown","Error — subquery must always return at least one row"],"answer":0},{"id":611,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; region is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(region) FROM clicks;","options":["4, 4 — both functions skip NULLs","6, 4 — COUNT(*) counts all rows; COUNT(region) skips NULLs","Error — you cannot mix COUNT(*) and COUNT(region)","6, 6 — both functions count all rows"],"answer":1},{"id":612,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["FALSE — AND with FALSE is always FALSE regardless of the other operand","UNKNOWN (distinct from NULL in the standard)","TRUE","NULL"],"answer":0},{"id":613,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT score FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE score > 0\n)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","No — WITH must contain exactly one named query","Only if both CTEs are declared RECURSIVE","Yes — b can reference a defined earlier in the same WITH"],"answer":3},{"id":614,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on flag?\nSELECT * FROM campaigns\nWHERE UPPER(flag) = 'ACTIVE';","options":["The optimizer always ignores scalar functions in WHERE","UPPER() always improves index efficiency","UPPER() wraps flag, preventing an index seek on the raw column","Indexes do not work with string columns"],"answer":2},{"id":615,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE tier IS NOT NULL;","options":["Both statements are identical in every database","B removes only non-NULL rows; A removes nothing","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","A can be rolled back; B cannot"],"answer":2},{"id":616,"type":"practical","topic":"joins","q":"visitors has 4 rows; flags has 5 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["9 — rows from both tables appended","0 — missing WHERE means no rows pass","20 — Cartesian product from the missing join condition","4 — only visitors rows"],"answer":2},{"id":617,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — COUNT(*) cannot appear in both SELECT and HAVING","Returns every row of devices filtered by the count","Error — HAVING requires a GROUP BY clause","Yes — the whole table is one implicit group; returns one row if any rows exist"],"answer":3},{"id":618,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(3, 3) — both leave a gap after a tie","(2, 2) — both functions agree after a tie","(2, 3) — DENSE_RANK skips; RANK does not"],"answer":0},{"id":619,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT code,\n       (SELECT amount FROM events) AS x\nFROM products;","options":["Three copies of each products row are returned","Runtime error — scalar subquery returned more than one row","The first row's amount value is silently used","The query auto-aggregates events to one row"],"answer":1},{"id":620,"type":"practical","topic":"indexes","q":"An index exists on (name, status).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE status = 'active';","options":["No — filtering only status skips the leading column name; leftmost-prefix rule not satisfied","Yes — status is in the index so seeks always work","No — composite indexes never speed up equality predicates","Yes — composite indexes cover any subset of their columns equally"],"answer":0},{"id":621,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["4 — DISTINCT has no effect on two-column selects","1 — DISTINCT keeps only the row with the lowest user_id","2 — only unique values of the first column user_id","3 — DISTINCT deduplicates the (user_id, created_at) pair; (1,'a') appears once"],"answer":3},{"id":622,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(order_id) FROM coupons\nWHERE 1 = 0;","options":["One row with MAX = NULL","Zero rows returned","Error — MAX on an empty set is undefined","One row with MAX = 0"],"answer":0},{"id":623,"type":"practical","topic":"joins","q":"Both tables have rows where amount IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.amount = b.amount;","options":["Yes — NULLs are treated as equal in ON conditions","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Yes — but only with INNER JOIN, not OUTER JOIN","Only if both sides are NULL simultaneously (special NULL-safe rule)"],"answer":1},{"id":624,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT status FROM metrics)\nEXCEPT\n(SELECT status FROM tickets);","options":["Values in metrics that do not appear in tickets","Values in tickets that do not appear in metrics","All values from both metrics and tickets","Values that appear in both metrics and tickets"],"answer":0},{"id":625,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) bucket\nFROM flags;","options":["2, 2, 3, 3 — smaller buckets come first","All 4 buckets have exactly 2 rows (remainder discarded)","4, 3, 2, 1 — strictly decreasing bucket sizes","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first"],"answer":3},{"id":626,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE region = NULL;","options":["A syntax error at runtime","All rows where region is NULL","Zero rows — = NULL always evaluates to UNKNOWN","Same result as WHERE region IS NOT NULL"],"answer":2},{"id":627,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT sku, qty, COUNT(*)\nFROM users\nGROUP BY sku;","options":["Valid — any column can appear alongside an aggregate","Error: GROUP BY must use an alias, not a column name","Error: qty is in SELECT but not in GROUP BY","Error: COUNT(*) cannot appear with a GROUP BY"],"answer":2},{"id":628,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT score, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY score;","options":["Runs fine; WHERE filters rows before aggregation","WHERE and HAVING are interchangeable here","Error — aggregate functions are not allowed in WHERE; use HAVING","Returns groups with count > 2 correctly"],"answer":2},{"id":629,"type":"practical","topic":"joins","q":"skus has a row where flag = 999; accounts has no matching row.\nWhat appears in b.kind for that skus row?\nSELECT a.flag, b.kind\nFROM skus a\nLEFT JOIN accounts b ON a.flag = b.flag;","options":["The row is excluded from the result","An error is raised for the unmatched key","b.kind takes the column's default value","NULL — unmatched left rows are kept with NULL on the right side"],"answer":3},{"id":630,"type":"practical","topic":"joins","q":"sessions has 6 rows; campaigns has 3 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["18 — Cartesian product (6 × 3)","9 — rows from both tables appended","6 — only sessions rows","3 — only matching-key rows"],"answer":0},{"id":631,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT name, COUNT(*) cnt\nFROM payments\nWHERE qty > 0\nGROUP BY name\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["ORDER BY","FROM","SELECT","WHERE"],"answer":1},{"id":632,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT price FROM subs)\nUNION ALL\n(SELECT price FROM visitors);","options":["Both copies are kept — UNION ALL never removes duplicates","An error is raised for the duplicate row","Only one of the two queries executes","The duplicate is removed — UNION ALL deduplicates"],"answer":0},{"id":633,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT kind,\n       ROW_NUMBER() OVER (PARTITION BY order_id) rn\nFROM tickets;","options":["Yes — ROW_NUMBER is always deterministic","Error — ORDER BY is required inside OVER with ROW_NUMBER","Yes — partitions are always sorted by insertion order","No — without ORDER BY inside OVER, order within each partition is undefined"],"answer":3},{"id":634,"type":"practical","topic":"window","q":"What is prev_code for the very first row in the ordering?\nSELECT amount,\n       code,\n       LAG(code) OVER (ORDER BY amount) AS prev_code\nFROM shipments;","options":["NULL — no preceding row exists for the first row","The value of code for the last row in the table","0 (zero default when there is no previous row)","An error is raised for the first row"],"answer":0},{"id":635,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.name = o.name AND 1 = 0\n);","options":["TRUE — EXISTS ignores the inner WHERE conditions","FALSE — EXISTS is false when the subquery returns no rows","Error — subquery must always return at least one row","NULL — three-valued logic makes this unknown"],"answer":1},{"id":636,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; user_id is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(user_id) FROM clicks;","options":["6, 4 — COUNT(*) counts all rows; COUNT(user_id) skips NULLs","Error — you cannot mix COUNT(*) and COUNT(user_id)","6, 6 — both functions count all rows","4, 4 — both functions skip NULLs"],"answer":0},{"id":637,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["NULL","UNKNOWN (distinct from NULL in the standard)","TRUE","FALSE — AND with FALSE is always FALSE regardless of the other operand"],"answer":3},{"id":638,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT amount FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE amount > 0\n)\nSELECT * FROM b;","options":["Yes — b can reference a defined earlier in the same WITH","No — CTEs cannot reference each other","No — WITH must contain exactly one named query","Only if both CTEs are declared RECURSIVE"],"answer":0},{"id":639,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on status?\nSELECT * FROM campaigns\nWHERE UPPER(status) = 'ACTIVE';","options":["UPPER() always improves index efficiency","Indexes do not work with string columns","UPPER() wraps status, preventing an index seek on the raw column","The optimizer always ignores scalar functions in WHERE"],"answer":2},{"id":640,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE created_at IS NOT NULL;","options":["A can be rolled back; B cannot","B removes only non-NULL rows; A removes nothing","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","Both statements are identical in every database"],"answer":2},{"id":641,"type":"practical","topic":"joins","q":"visitors has 5 rows; flags has 2 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["5 — only visitors rows","7 — rows from both tables appended","0 — missing WHERE means no rows pass","10 — Cartesian product from the missing join condition"],"answer":3},{"id":642,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — COUNT(*) cannot appear in both SELECT and HAVING","Error — HAVING requires a GROUP BY clause","Yes — the whole table is one implicit group; returns one row if any rows exist","Returns every row of devices filtered by the count"],"answer":2},{"id":643,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(2, 3) — DENSE_RANK skips; RANK does not","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(2, 2) — both functions agree after a tie","(3, 3) — both leave a gap after a tie"],"answer":1},{"id":644,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT flag,\n       (SELECT kind FROM events) AS x\nFROM products;","options":["Runtime error — scalar subquery returned more than one row","The query auto-aggregates events to one row","Three copies of each products row are returned","The first row's kind value is silently used"],"answer":0},{"id":645,"type":"practical","topic":"indexes","q":"An index exists on (tier, code).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE code = 'active';","options":["Yes — code is in the index so seeks always work","No — composite indexes never speed up equality predicates","Yes — composite indexes cover any subset of their columns equally","No — filtering only code skips the leading column tier; leftmost-prefix rule not satisfied"],"answer":3},{"id":646,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT qty, name FROM invoices;","options":["3 — DISTINCT deduplicates the (qty, name) pair; (1,'a') appears once","4 — DISTINCT has no effect on two-column selects","2 — only unique values of the first column qty","1 — DISTINCT keeps only the row with the lowest qty"],"answer":0},{"id":647,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(price) FROM coupons\nWHERE 1 = 0;","options":["Zero rows returned","One row with MAX = NULL","One row with MAX = 0","Error — MAX on an empty set is undefined"],"answer":1},{"id":648,"type":"practical","topic":"joins","q":"Both tables have rows where kind IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.kind = b.kind;","options":["No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Yes — but only with INNER JOIN, not OUTER JOIN","Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — NULLs are treated as equal in ON conditions"],"answer":0},{"id":649,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT code FROM metrics)\nEXCEPT\n(SELECT code FROM tickets);","options":["Values that appear in both metrics and tickets","Values in tickets that do not appear in metrics","All values from both metrics and tickets","Values in metrics that do not appear in tickets"],"answer":3},{"id":650,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT name, NTILE(4) OVER (ORDER BY status) bucket\nFROM flags;","options":["3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","2, 2, 3, 3 — smaller buckets come first","4, 3, 2, 1 — strictly decreasing bucket sizes","All 4 buckets have exactly 2 rows (remainder discarded)"],"answer":0},{"id":651,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE user_id = NULL;","options":["All rows where user_id is NULL","Same result as WHERE user_id IS NOT NULL","Zero rows — = NULL always evaluates to UNKNOWN","A syntax error at runtime"],"answer":2},{"id":652,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT order_id, region, COUNT(*)\nFROM users\nGROUP BY order_id;","options":["Error: COUNT(*) cannot appear with a GROUP BY","Error: GROUP BY must use an alias, not a column name","Error: region is in SELECT but not in GROUP BY","Valid — any column can appear alongside an aggregate"],"answer":2},{"id":653,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT amount, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY amount;","options":["Returns groups with count > 2 correctly","Runs fine; WHERE filters rows before aggregation","WHERE and HAVING are interchangeable here","Error — aggregate functions are not allowed in WHERE; use HAVING"],"answer":3},{"id":654,"type":"practical","topic":"joins","q":"skus has a row where status = 999; accounts has no matching row.\nWhat appears in b.score for that skus row?\nSELECT a.status, b.score\nFROM skus a\nLEFT JOIN accounts b ON a.status = b.status;","options":["An error is raised for the unmatched key","The row is excluded from the result","NULL — unmatched left rows are kept with NULL on the right side","b.score takes the column's default value"],"answer":2},{"id":655,"type":"practical","topic":"joins","q":"sessions has 7 rows; campaigns has 4 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["7 — only sessions rows","4 — only matching-key rows","28 — Cartesian product (7 × 4)","11 — rows from both tables appended"],"answer":2},{"id":656,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT tier, COUNT(*) cnt\nFROM payments\nWHERE region > 0\nGROUP BY tier\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["FROM","WHERE","ORDER BY","SELECT"],"answer":0},{"id":657,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT sku FROM subs)\nUNION ALL\n(SELECT sku FROM visitors);","options":["An error is raised for the duplicate row","Only one of the two queries executes","The duplicate is removed — UNION ALL deduplicates","Both copies are kept — UNION ALL never removes duplicates"],"answer":3},{"id":658,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT score,\n       ROW_NUMBER() OVER (PARTITION BY price) rn\nFROM tickets;","options":["No — without ORDER BY inside OVER, order within each partition is undefined","Yes — ROW_NUMBER is always deterministic","Yes — partitions are always sorted by insertion order","Error — ORDER BY is required inside OVER with ROW_NUMBER"],"answer":0},{"id":659,"type":"practical","topic":"window","q":"What is prev_flag for the very first row in the ordering?\nSELECT kind,\n       flag,\n       LAG(flag) OVER (ORDER BY kind) AS prev_flag\nFROM shipments;","options":["An error is raised for the first row","NULL — no preceding row exists for the first row","The value of flag for the last row in the table","0 (zero default when there is no previous row)"],"answer":1},{"id":660,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.tier = o.tier AND 1 = 0\n);","options":["FALSE — EXISTS is false when the subquery returns no rows","Error — subquery must always return at least one row","NULL — three-valued logic makes this unknown","TRUE — EXISTS ignores the inner WHERE conditions"],"answer":0},{"id":661,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; qty is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(qty) FROM clicks;","options":["4, 4 — both functions skip NULLs","Error — you cannot mix COUNT(*) and COUNT(qty)","6, 6 — both functions count all rows","6, 4 — COUNT(*) counts all rows; COUNT(qty) skips NULLs"],"answer":3},{"id":662,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["FALSE — AND with FALSE is always FALSE regardless of the other operand","NULL","TRUE","UNKNOWN (distinct from NULL in the standard)"],"answer":0},{"id":663,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT kind FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE kind > 0\n)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","Yes — b can reference a defined earlier in the same WITH","Only if both CTEs are declared RECURSIVE","No — WITH must contain exactly one named query"],"answer":1},{"id":664,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on code?\nSELECT * FROM campaigns\nWHERE UPPER(code) = 'ACTIVE';","options":["The optimizer always ignores scalar functions in WHERE","Indexes do not work with string columns","UPPER() wraps code, preventing an index seek on the raw column","UPPER() always improves index efficiency"],"answer":2},{"id":665,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE name IS NOT NULL;","options":["Both statements are identical in every database","A can be rolled back; B cannot","B removes only non-NULL rows; A removes nothing","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging"],"answer":3},{"id":666,"type":"practical","topic":"joins","q":"visitors has 6 rows; flags has 3 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["9 — rows from both tables appended","6 — only visitors rows","18 — Cartesian product from the missing join condition","0 — missing WHERE means no rows pass"],"answer":2},{"id":667,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — HAVING requires a GROUP BY clause","Returns every row of devices filtered by the count","Yes — the whole table is one implicit group; returns one row if any rows exist","Error — COUNT(*) cannot appear in both SELECT and HAVING"],"answer":2},{"id":668,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(3, 3) — both leave a gap after a tie","(2, 3) — DENSE_RANK skips; RANK does not","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(2, 2) — both functions agree after a tie"],"answer":2},{"id":669,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT status,\n       (SELECT score FROM events) AS x\nFROM products;","options":["The query auto-aggregates events to one row","Three copies of each products row are returned","The first row's score value is silently used","Runtime error — scalar subquery returned more than one row"],"answer":3},{"id":670,"type":"practical","topic":"indexes","q":"An index exists on (created_at, flag).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE flag = 'active';","options":["No — filtering only flag skips the leading column created_at; leftmost-prefix rule not satisfied","Yes — flag is in the index so seeks always work","Yes — composite indexes cover any subset of their columns equally","No — composite indexes never speed up equality predicates"],"answer":0},{"id":671,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT region, tier FROM invoices;","options":["1 — DISTINCT keeps only the row with the lowest region","3 — DISTINCT deduplicates the (region, tier) pair; (1,'a') appears once","2 — only unique values of the first column region","4 — DISTINCT has no effect on two-column selects"],"answer":1},{"id":672,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(sku) FROM coupons\nWHERE 1 = 0;","options":["One row with MAX = NULL","Error — MAX on an empty set is undefined","One row with MAX = 0","Zero rows returned"],"answer":0},{"id":673,"type":"practical","topic":"joins","q":"Both tables have rows where score IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.score = b.score;","options":["Yes — NULLs are treated as equal in ON conditions","Yes — but only with INNER JOIN, not OUTER JOIN","Only if both sides are NULL simultaneously (special NULL-safe rule)","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN"],"answer":3},{"id":674,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT flag FROM metrics)\nEXCEPT\n(SELECT flag FROM tickets);","options":["Values in metrics that do not appear in tickets","Values that appear in both metrics and tickets","All values from both metrics and tickets","Values in tickets that do not appear in metrics"],"answer":0},{"id":675,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT tier, NTILE(4) OVER (ORDER BY code) bucket\nFROM flags;","options":["2, 2, 3, 3 — smaller buckets come first","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","All 4 buckets have exactly 2 rows (remainder discarded)","4, 3, 2, 1 — strictly decreasing bucket sizes"],"answer":1},{"id":676,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE qty = NULL;","options":["Zero rows — = NULL always evaluates to UNKNOWN","Same result as WHERE qty IS NOT NULL","A syntax error at runtime","All rows where qty is NULL"],"answer":0},{"id":677,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT price, user_id, COUNT(*)\nFROM users\nGROUP BY price;","options":["Valid — any column can appear alongside an aggregate","Error: COUNT(*) cannot appear with a GROUP BY","Error: GROUP BY must use an alias, not a column name","Error: user_id is in SELECT but not in GROUP BY"],"answer":3},{"id":678,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT kind, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY kind;","options":["Runs fine; WHERE filters rows before aggregation","Returns groups with count > 2 correctly","Error — aggregate functions are not allowed in WHERE; use HAVING","WHERE and HAVING are interchangeable here"],"answer":2},{"id":679,"type":"practical","topic":"joins","q":"skus has a row where code = 999; accounts has no matching row.\nWhat appears in b.amount for that skus row?\nSELECT a.code, b.amount\nFROM skus a\nLEFT JOIN accounts b ON a.code = b.code;","options":["The row is excluded from the result","b.amount takes the column's default value","NULL — unmatched left rows are kept with NULL on the right side","An error is raised for the unmatched key"],"answer":2},{"id":680,"type":"practical","topic":"joins","q":"sessions has 8 rows; campaigns has 5 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["13 — rows from both tables appended","5 — only matching-key rows","40 — Cartesian product (8 × 5)","8 — only sessions rows"],"answer":2},{"id":681,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT created_at, COUNT(*) cnt\nFROM payments\nWHERE user_id > 0\nGROUP BY created_at\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["WHERE","ORDER BY","SELECT","FROM"],"answer":3},{"id":682,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT order_id FROM subs)\nUNION ALL\n(SELECT order_id FROM visitors);","options":["Both copies are kept — UNION ALL never removes duplicates","An error is raised for the duplicate row","The duplicate is removed — UNION ALL deduplicates","Only one of the two queries executes"],"answer":0},{"id":683,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT amount,\n       ROW_NUMBER() OVER (PARTITION BY sku) rn\nFROM tickets;","options":["Error — ORDER BY is required inside OVER with ROW_NUMBER","No — without ORDER BY inside OVER, order within each partition is undefined","Yes — partitions are always sorted by insertion order","Yes — ROW_NUMBER is always deterministic"],"answer":1},{"id":684,"type":"practical","topic":"window","q":"What is prev_status for the very first row in the ordering?\nSELECT score,\n       status,\n       LAG(status) OVER (ORDER BY score) AS prev_status\nFROM shipments;","options":["NULL — no preceding row exists for the first row","0 (zero default when there is no previous row)","An error is raised for the first row","The value of status for the last row in the table"],"answer":0},{"id":685,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.created_at = o.created_at AND 1 = 0\n);","options":["NULL — three-valued logic makes this unknown","Error — subquery must always return at least one row","TRUE — EXISTS ignores the inner WHERE conditions","FALSE — EXISTS is false when the subquery returns no rows"],"answer":3},{"id":686,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; region is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(region) FROM clicks;","options":["6, 4 — COUNT(*) counts all rows; COUNT(region) skips NULLs","4, 4 — both functions skip NULLs","6, 6 — both functions count all rows","Error — you cannot mix COUNT(*) and COUNT(region)"],"answer":0},{"id":687,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["NULL","FALSE — AND with FALSE is always FALSE regardless of the other operand","UNKNOWN (distinct from NULL in the standard)","TRUE"],"answer":1},{"id":688,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT score FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE score > 0\n)\nSELECT * FROM b;","options":["Yes — b can reference a defined earlier in the same WITH","Only if both CTEs are declared RECURSIVE","No — WITH must contain exactly one named query","No — CTEs cannot reference each other"],"answer":0},{"id":689,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on flag?\nSELECT * FROM campaigns\nWHERE UPPER(flag) = 'ACTIVE';","options":["UPPER() always improves index efficiency","Indexes do not work with string columns","The optimizer always ignores scalar functions in WHERE","UPPER() wraps flag, preventing an index seek on the raw column"],"answer":3},{"id":690,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE tier IS NOT NULL;","options":["A can be rolled back; B cannot","Both statements are identical in every database","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","B removes only non-NULL rows; A removes nothing"],"answer":2},{"id":691,"type":"practical","topic":"joins","q":"visitors has 7 rows; flags has 4 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["7 — only visitors rows","0 — missing WHERE means no rows pass","28 — Cartesian product from the missing join condition","11 — rows from both tables appended"],"answer":2},{"id":692,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — COUNT(*) cannot appear in both SELECT and HAVING","Returns every row of devices filtered by the count","Yes — the whole table is one implicit group; returns one row if any rows exist","Error — HAVING requires a GROUP BY clause"],"answer":2},{"id":693,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(2, 2) — both functions agree after a tie","(3, 3) — both leave a gap after a tie","(2, 3) — DENSE_RANK skips; RANK does not","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2"],"answer":3},{"id":694,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT code,\n       (SELECT amount FROM events) AS x\nFROM products;","options":["Runtime error — scalar subquery returned more than one row","The query auto-aggregates events to one row","The first row's amount value is silently used","Three copies of each products row are returned"],"answer":0},{"id":695,"type":"practical","topic":"indexes","q":"An index exists on (name, status).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE status = 'active';","options":["No — composite indexes never speed up equality predicates","No — filtering only status skips the leading column name; leftmost-prefix rule not satisfied","Yes — composite indexes cover any subset of their columns equally","Yes — status is in the index so seeks always work"],"answer":1},{"id":696,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["3 — DISTINCT deduplicates the (user_id, created_at) pair; (1,'a') appears once","4 — DISTINCT has no effect on two-column selects","1 — DISTINCT keeps only the row with the lowest user_id","2 — only unique values of the first column user_id"],"answer":0},{"id":697,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(order_id) FROM coupons\nWHERE 1 = 0;","options":["Error — MAX on an empty set is undefined","One row with MAX = 0","Zero rows returned","One row with MAX = NULL"],"answer":3},{"id":698,"type":"practical","topic":"joins","q":"Both tables have rows where amount IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.amount = b.amount;","options":["No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Yes — NULLs are treated as equal in ON conditions","Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — but only with INNER JOIN, not OUTER JOIN"],"answer":0},{"id":699,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT status FROM metrics)\nEXCEPT\n(SELECT status FROM tickets);","options":["Values that appear in both metrics and tickets","Values in metrics that do not appear in tickets","Values in tickets that do not appear in metrics","All values from both metrics and tickets"],"answer":1},{"id":700,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) bucket\nFROM flags;","options":["3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","All 4 buckets have exactly 2 rows (remainder discarded)","4, 3, 2, 1 — strictly decreasing bucket sizes","2, 2, 3, 3 — smaller buckets come first"],"answer":0},{"id":701,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE region = NULL;","options":["All rows where region is NULL","Same result as WHERE region IS NOT NULL","A syntax error at runtime","Zero rows — = NULL always evaluates to UNKNOWN"],"answer":3},{"id":702,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT sku, qty, COUNT(*)\nFROM users\nGROUP BY sku;","options":["Error: qty is in SELECT but not in GROUP BY","Valid — any column can appear alongside an aggregate","Error: COUNT(*) cannot appear with a GROUP BY","Error: GROUP BY must use an alias, not a column name"],"answer":0},{"id":703,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT score, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY score;","options":["Returns groups with count > 2 correctly","WHERE and HAVING are interchangeable here","Error — aggregate functions are not allowed in WHERE; use HAVING","Runs fine; WHERE filters rows before aggregation"],"answer":2},{"id":704,"type":"practical","topic":"joins","q":"skus has a row where flag = 999; accounts has no matching row.\nWhat appears in b.kind for that skus row?\nSELECT a.flag, b.kind\nFROM skus a\nLEFT JOIN accounts b ON a.flag = b.flag;","options":["An error is raised for the unmatched key","b.kind takes the column's default value","NULL — unmatched left rows are kept with NULL on the right side","The row is excluded from the result"],"answer":2},{"id":705,"type":"practical","topic":"joins","q":"sessions has 3 rows; campaigns has 2 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["3 — only sessions rows","5 — rows from both tables appended","2 — only matching-key rows","6 — Cartesian product (3 × 2)"],"answer":3},{"id":706,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT name, COUNT(*) cnt\nFROM payments\nWHERE qty > 0\nGROUP BY name\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["WHERE","SELECT","FROM","ORDER BY"],"answer":2},{"id":707,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT price FROM subs)\nUNION ALL\n(SELECT price FROM visitors);","options":["Only one of the two queries executes","Both copies are kept — UNION ALL never removes duplicates","The duplicate is removed — UNION ALL deduplicates","An error is raised for the duplicate row"],"answer":1},{"id":708,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT kind,\n       ROW_NUMBER() OVER (PARTITION BY order_id) rn\nFROM tickets;","options":["No — without ORDER BY inside OVER, order within each partition is undefined","Yes — ROW_NUMBER is always deterministic","Error — ORDER BY is required inside OVER with ROW_NUMBER","Yes — partitions are always sorted by insertion order"],"answer":0},{"id":709,"type":"practical","topic":"window","q":"What is prev_code for the very first row in the ordering?\nSELECT amount,\n       code,\n       LAG(code) OVER (ORDER BY amount) AS prev_code\nFROM shipments;","options":["0 (zero default when there is no previous row)","An error is raised for the first row","The value of code for the last row in the table","NULL — no preceding row exists for the first row"],"answer":3},{"id":710,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.name = o.name AND 1 = 0\n);","options":["FALSE — EXISTS is false when the subquery returns no rows","NULL — three-valued logic makes this unknown","TRUE — EXISTS ignores the inner WHERE conditions","Error — subquery must always return at least one row"],"answer":0},{"id":711,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; user_id is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(user_id) FROM clicks;","options":["4, 4 — both functions skip NULLs","6, 4 — COUNT(*) counts all rows; COUNT(user_id) skips NULLs","Error — you cannot mix COUNT(*) and COUNT(user_id)","6, 6 — both functions count all rows"],"answer":1},{"id":712,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["FALSE — AND with FALSE is always FALSE regardless of the other operand","UNKNOWN (distinct from NULL in the standard)","TRUE","NULL"],"answer":0},{"id":713,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT amount FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE amount > 0\n)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","Only if both CTEs are declared RECURSIVE","No — WITH must contain exactly one named query","Yes — b can reference a defined earlier in the same WITH"],"answer":3},{"id":714,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on status?\nSELECT * FROM campaigns\nWHERE UPPER(status) = 'ACTIVE';","options":["UPPER() wraps status, preventing an index seek on the raw column","UPPER() always improves index efficiency","The optimizer always ignores scalar functions in WHERE","Indexes do not work with string columns"],"answer":0},{"id":715,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE created_at IS NOT NULL;","options":["Both statements are identical in every database","B removes only non-NULL rows; A removes nothing","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","A can be rolled back; B cannot"],"answer":2},{"id":716,"type":"practical","topic":"joins","q":"visitors has 8 rows; flags has 5 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["13 — rows from both tables appended","0 — missing WHERE means no rows pass","40 — Cartesian product from the missing join condition","8 — only visitors rows"],"answer":2},{"id":717,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — HAVING requires a GROUP BY clause","Error — COUNT(*) cannot appear in both SELECT and HAVING","Returns every row of devices filtered by the count","Yes — the whole table is one implicit group; returns one row if any rows exist"],"answer":3},{"id":718,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(3, 3) — both leave a gap after a tie","(2, 2) — both functions agree after a tie","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(2, 3) — DENSE_RANK skips; RANK does not"],"answer":2},{"id":719,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT flag,\n       (SELECT kind FROM events) AS x\nFROM products;","options":["The first row's kind value is silently used","Three copies of each products row are returned","Runtime error — scalar subquery returned more than one row","The query auto-aggregates events to one row"],"answer":2},{"id":720,"type":"practical","topic":"indexes","q":"An index exists on (tier, code).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE code = 'active';","options":["No — filtering only code skips the leading column tier; leftmost-prefix rule not satisfied","Yes — code is in the index so seeks always work","No — composite indexes never speed up equality predicates","Yes — composite indexes cover any subset of their columns equally"],"answer":0},{"id":721,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT qty, name FROM invoices;","options":["4 — DISTINCT has no effect on two-column selects","1 — DISTINCT keeps only the row with the lowest qty","2 — only unique values of the first column qty","3 — DISTINCT deduplicates the (qty, name) pair; (1,'a') appears once"],"answer":3},{"id":722,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(price) FROM coupons\nWHERE 1 = 0;","options":["One row with MAX = NULL","Error — MAX on an empty set is undefined","Zero rows returned","One row with MAX = 0"],"answer":0},{"id":723,"type":"practical","topic":"joins","q":"Both tables have rows where kind IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.kind = b.kind;","options":["Yes — but only with INNER JOIN, not OUTER JOIN","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Yes — NULLs are treated as equal in ON conditions","Only if both sides are NULL simultaneously (special NULL-safe rule)"],"answer":1},{"id":724,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT code FROM metrics)\nEXCEPT\n(SELECT code FROM tickets);","options":["Values in metrics that do not appear in tickets","Values in tickets that do not appear in metrics","All values from both metrics and tickets","Values that appear in both metrics and tickets"],"answer":0},{"id":725,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT name, NTILE(4) OVER (ORDER BY status) bucket\nFROM flags;","options":["2, 2, 3, 3 — smaller buckets come first","All 4 buckets have exactly 2 rows (remainder discarded)","4, 3, 2, 1 — strictly decreasing bucket sizes","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first"],"answer":3},{"id":726,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE user_id = NULL;","options":["Zero rows — = NULL always evaluates to UNKNOWN","All rows where user_id is NULL","A syntax error at runtime","Same result as WHERE user_id IS NOT NULL"],"answer":0},{"id":727,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT order_id, region, COUNT(*)\nFROM users\nGROUP BY order_id;","options":["Valid — any column can appear alongside an aggregate","Error: region is in SELECT but not in GROUP BY","Error: GROUP BY must use an alias, not a column name","Error: COUNT(*) cannot appear with a GROUP BY"],"answer":1},{"id":728,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT amount, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY amount;","options":["Runs fine; WHERE filters rows before aggregation","WHERE and HAVING are interchangeable here","Error — aggregate functions are not allowed in WHERE; use HAVING","Returns groups with count > 2 correctly"],"answer":2},{"id":729,"type":"practical","topic":"joins","q":"skus has a row where status = 999; accounts has no matching row.\nWhat appears in b.score for that skus row?\nSELECT a.status, b.score\nFROM skus a\nLEFT JOIN accounts b ON a.status = b.status;","options":["The row is excluded from the result","An error is raised for the unmatched key","b.score takes the column's default value","NULL — unmatched left rows are kept with NULL on the right side"],"answer":3},{"id":730,"type":"practical","topic":"joins","q":"sessions has 4 rows; campaigns has 3 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["7 — rows from both tables appended","4 — only sessions rows","12 — Cartesian product (4 × 3)","3 — only matching-key rows"],"answer":2},{"id":731,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT tier, COUNT(*) cnt\nFROM payments\nWHERE region > 0\nGROUP BY tier\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["SELECT","ORDER BY","FROM","WHERE"],"answer":2},{"id":732,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT sku FROM subs)\nUNION ALL\n(SELECT sku FROM visitors);","options":["Both copies are kept — UNION ALL never removes duplicates","An error is raised for the duplicate row","Only one of the two queries executes","The duplicate is removed — UNION ALL deduplicates"],"answer":0},{"id":733,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT score,\n       ROW_NUMBER() OVER (PARTITION BY price) rn\nFROM tickets;","options":["Yes — ROW_NUMBER is always deterministic","Error — ORDER BY is required inside OVER with ROW_NUMBER","Yes — partitions are always sorted by insertion order","No — without ORDER BY inside OVER, order within each partition is undefined"],"answer":3},{"id":734,"type":"practical","topic":"window","q":"What is prev_flag for the very first row in the ordering?\nSELECT kind,\n       flag,\n       LAG(flag) OVER (ORDER BY kind) AS prev_flag\nFROM shipments;","options":["NULL — no preceding row exists for the first row","0 (zero default when there is no previous row)","The value of flag for the last row in the table","An error is raised for the first row"],"answer":0},{"id":735,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.tier = o.tier AND 1 = 0\n);","options":["Error — subquery must always return at least one row","FALSE — EXISTS is false when the subquery returns no rows","TRUE — EXISTS ignores the inner WHERE conditions","NULL — three-valued logic makes this unknown"],"answer":1},{"id":736,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; qty is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(qty) FROM clicks;","options":["6, 4 — COUNT(*) counts all rows; COUNT(qty) skips NULLs","6, 6 — both functions count all rows","Error — you cannot mix COUNT(*) and COUNT(qty)","4, 4 — both functions skip NULLs"],"answer":0},{"id":737,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["NULL","UNKNOWN (distinct from NULL in the standard)","TRUE","FALSE — AND with FALSE is always FALSE regardless of the other operand"],"answer":3},{"id":738,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT kind FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE kind > 0\n)\nSELECT * FROM b;","options":["Yes — b can reference a defined earlier in the same WITH","No — CTEs cannot reference each other","No — WITH must contain exactly one named query","Only if both CTEs are declared RECURSIVE"],"answer":0},{"id":739,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on code?\nSELECT * FROM campaigns\nWHERE UPPER(code) = 'ACTIVE';","options":["UPPER() always improves index efficiency","UPPER() wraps code, preventing an index seek on the raw column","Indexes do not work with string columns","The optimizer always ignores scalar functions in WHERE"],"answer":1},{"id":740,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE name IS NOT NULL;","options":["B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","B removes only non-NULL rows; A removes nothing","A can be rolled back; B cannot","Both statements are identical in every database"],"answer":0},{"id":741,"type":"practical","topic":"joins","q":"visitors has 3 rows; flags has 2 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["3 — only visitors rows","5 — rows from both tables appended","0 — missing WHERE means no rows pass","6 — Cartesian product from the missing join condition"],"answer":3},{"id":742,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — COUNT(*) cannot appear in both SELECT and HAVING","Error — HAVING requires a GROUP BY clause","Yes — the whole table is one implicit group; returns one row if any rows exist","Returns every row of devices filtered by the count"],"answer":2},{"id":743,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(2, 2) — both functions agree after a tie","(2, 3) — DENSE_RANK skips; RANK does not","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(3, 3) — both leave a gap after a tie"],"answer":2},{"id":744,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT status,\n       (SELECT score FROM events) AS x\nFROM products;","options":["The query auto-aggregates events to one row","Three copies of each products row are returned","Runtime error — scalar subquery returned more than one row","The first row's score value is silently used"],"answer":2},{"id":745,"type":"practical","topic":"indexes","q":"An index exists on (created_at, flag).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE flag = 'active';","options":["Yes — flag is in the index so seeks always work","No — composite indexes never speed up equality predicates","Yes — composite indexes cover any subset of their columns equally","No — filtering only flag skips the leading column created_at; leftmost-prefix rule not satisfied"],"answer":3},{"id":746,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT region, tier FROM invoices;","options":["3 — DISTINCT deduplicates the (region, tier) pair; (1,'a') appears once","4 — DISTINCT has no effect on two-column selects","2 — only unique values of the first column region","1 — DISTINCT keeps only the row with the lowest region"],"answer":0},{"id":747,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(sku) FROM coupons\nWHERE 1 = 0;","options":["One row with MAX = 0","One row with MAX = NULL","Zero rows returned","Error — MAX on an empty set is undefined"],"answer":1},{"id":748,"type":"practical","topic":"joins","q":"Both tables have rows where score IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.score = b.score;","options":["No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — but only with INNER JOIN, not OUTER JOIN","Yes — NULLs are treated as equal in ON conditions"],"answer":0},{"id":749,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT flag FROM metrics)\nEXCEPT\n(SELECT flag FROM tickets);","options":["Values that appear in both metrics and tickets","Values in tickets that do not appear in metrics","All values from both metrics and tickets","Values in metrics that do not appear in tickets"],"answer":3},{"id":750,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT tier, NTILE(4) OVER (ORDER BY code) bucket\nFROM flags;","options":["3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","2, 2, 3, 3 — smaller buckets come first","4, 3, 2, 1 — strictly decreasing bucket sizes","All 4 buckets have exactly 2 rows (remainder discarded)"],"answer":0},{"id":751,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE qty = NULL;","options":["All rows where qty is NULL","Zero rows — = NULL always evaluates to UNKNOWN","Same result as WHERE qty IS NOT NULL","A syntax error at runtime"],"answer":1},{"id":752,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT price, user_id, COUNT(*)\nFROM users\nGROUP BY price;","options":["Error: user_id is in SELECT but not in GROUP BY","Error: GROUP BY must use an alias, not a column name","Error: COUNT(*) cannot appear with a GROUP BY","Valid — any column can appear alongside an aggregate"],"answer":0},{"id":753,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT kind, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY kind;","options":["Returns groups with count > 2 correctly","WHERE and HAVING are interchangeable here","Runs fine; WHERE filters rows before aggregation","Error — aggregate functions are not allowed in WHERE; use HAVING"],"answer":3},{"id":754,"type":"practical","topic":"joins","q":"skus has a row where code = 999; accounts has no matching row.\nWhat appears in b.amount for that skus row?\nSELECT a.code, b.amount\nFROM skus a\nLEFT JOIN accounts b ON a.code = b.code;","options":["An error is raised for the unmatched key","The row is excluded from the result","NULL — unmatched left rows are kept with NULL on the right side","b.amount takes the column's default value"],"answer":2},{"id":755,"type":"practical","topic":"joins","q":"sessions has 5 rows; campaigns has 4 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["5 — only sessions rows","4 — only matching-key rows","20 — Cartesian product (5 × 4)","9 — rows from both tables appended"],"answer":2},{"id":756,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT created_at, COUNT(*) cnt\nFROM payments\nWHERE user_id > 0\nGROUP BY created_at\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["WHERE","ORDER BY","FROM","SELECT"],"answer":2},{"id":757,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT order_id FROM subs)\nUNION ALL\n(SELECT order_id FROM visitors);","options":["The duplicate is removed — UNION ALL deduplicates","An error is raised for the duplicate row","Only one of the two queries executes","Both copies are kept — UNION ALL never removes duplicates"],"answer":3},{"id":758,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT amount,\n       ROW_NUMBER() OVER (PARTITION BY sku) rn\nFROM tickets;","options":["No — without ORDER BY inside OVER, order within each partition is undefined","Yes — ROW_NUMBER is always deterministic","Yes — partitions are always sorted by insertion order","Error — ORDER BY is required inside OVER with ROW_NUMBER"],"answer":0},{"id":759,"type":"practical","topic":"window","q":"What is prev_status for the very first row in the ordering?\nSELECT score,\n       status,\n       LAG(status) OVER (ORDER BY score) AS prev_status\nFROM shipments;","options":["An error is raised for the first row","NULL — no preceding row exists for the first row","The value of status for the last row in the table","0 (zero default when there is no previous row)"],"answer":1},{"id":760,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.created_at = o.created_at AND 1 = 0\n);","options":["FALSE — EXISTS is false when the subquery returns no rows","NULL — three-valued logic makes this unknown","Error — subquery must always return at least one row","TRUE — EXISTS ignores the inner WHERE conditions"],"answer":0},{"id":761,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; region is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(region) FROM clicks;","options":["6, 6 — both functions count all rows","Error — you cannot mix COUNT(*) and COUNT(region)","4, 4 — both functions skip NULLs","6, 4 — COUNT(*) counts all rows; COUNT(region) skips NULLs"],"answer":3},{"id":762,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["FALSE — AND with FALSE is always FALSE regardless of the other operand","NULL","TRUE","UNKNOWN (distinct from NULL in the standard)"],"answer":0},{"id":763,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT score FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE score > 0\n)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","Yes — b can reference a defined earlier in the same WITH","Only if both CTEs are declared RECURSIVE","No — WITH must contain exactly one named query"],"answer":1},{"id":764,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on flag?\nSELECT * FROM campaigns\nWHERE UPPER(flag) = 'ACTIVE';","options":["UPPER() wraps flag, preventing an index seek on the raw column","Indexes do not work with string columns","The optimizer always ignores scalar functions in WHERE","UPPER() always improves index efficiency"],"answer":0},{"id":765,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE tier IS NOT NULL;","options":["Both statements are identical in every database","B removes only non-NULL rows; A removes nothing","A can be rolled back; B cannot","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging"],"answer":3},{"id":766,"type":"practical","topic":"joins","q":"visitors has 4 rows; flags has 3 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["7 — rows from both tables appended","4 — only visitors rows","12 — Cartesian product from the missing join condition","0 — missing WHERE means no rows pass"],"answer":2},{"id":767,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — HAVING requires a GROUP BY clause","Returns every row of devices filtered by the count","Yes — the whole table is one implicit group; returns one row if any rows exist","Error — COUNT(*) cannot appear in both SELECT and HAVING"],"answer":2},{"id":768,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(3, 3) — both leave a gap after a tie","(2, 3) — DENSE_RANK skips; RANK does not","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(2, 2) — both functions agree after a tie"],"answer":2},{"id":769,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT code,\n       (SELECT amount FROM events) AS x\nFROM products;","options":["The first row's amount value is silently used","The query auto-aggregates events to one row","Three copies of each products row are returned","Runtime error — scalar subquery returned more than one row"],"answer":3},{"id":770,"type":"practical","topic":"indexes","q":"An index exists on (name, status).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE status = 'active';","options":["Yes — status is in the index so seeks always work","Yes — composite indexes cover any subset of their columns equally","No — filtering only status skips the leading column name; leftmost-prefix rule not satisfied","No — composite indexes never speed up equality predicates"],"answer":2},{"id":771,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["1 — DISTINCT keeps only the row with the lowest user_id","3 — DISTINCT deduplicates the (user_id, created_at) pair; (1,'a') appears once","2 — only unique values of the first column user_id","4 — DISTINCT has no effect on two-column selects"],"answer":1},{"id":772,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(order_id) FROM coupons\nWHERE 1 = 0;","options":["One row with MAX = NULL","Error — MAX on an empty set is undefined","One row with MAX = 0","Zero rows returned"],"answer":0},{"id":773,"type":"practical","topic":"joins","q":"Both tables have rows where amount IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.amount = b.amount;","options":["Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — but only with INNER JOIN, not OUTER JOIN","Yes — NULLs are treated as equal in ON conditions","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN"],"answer":3},{"id":774,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT status FROM metrics)\nEXCEPT\n(SELECT status FROM tickets);","options":["Values in metrics that do not appear in tickets","All values from both metrics and tickets","Values that appear in both metrics and tickets","Values in tickets that do not appear in metrics"],"answer":0},{"id":775,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) bucket\nFROM flags;","options":["2, 2, 3, 3 — smaller buckets come first","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","All 4 buckets have exactly 2 rows (remainder discarded)","4, 3, 2, 1 — strictly decreasing bucket sizes"],"answer":1},{"id":776,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE region = NULL;","options":["Zero rows — = NULL always evaluates to UNKNOWN","Same result as WHERE region IS NOT NULL","A syntax error at runtime","All rows where region is NULL"],"answer":0},{"id":777,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT sku, qty, COUNT(*)\nFROM users\nGROUP BY sku;","options":["Valid — any column can appear alongside an aggregate","Error: GROUP BY must use an alias, not a column name","Error: COUNT(*) cannot appear with a GROUP BY","Error: qty is in SELECT but not in GROUP BY"],"answer":3},{"id":778,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT score, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY score;","options":["Error — aggregate functions are not allowed in WHERE; use HAVING","Returns groups with count > 2 correctly","Runs fine; WHERE filters rows before aggregation","WHERE and HAVING are interchangeable here"],"answer":0},{"id":779,"type":"practical","topic":"joins","q":"skus has a row where flag = 999; accounts has no matching row.\nWhat appears in b.kind for that skus row?\nSELECT a.flag, b.kind\nFROM skus a\nLEFT JOIN accounts b ON a.flag = b.flag;","options":["The row is excluded from the result","b.kind takes the column's default value","NULL — unmatched left rows are kept with NULL on the right side","An error is raised for the unmatched key"],"answer":2},{"id":780,"type":"practical","topic":"joins","q":"sessions has 6 rows; campaigns has 5 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["11 — rows from both tables appended","5 — only matching-key rows","30 — Cartesian product (6 × 5)","6 — only sessions rows"],"answer":2},{"id":781,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT name, COUNT(*) cnt\nFROM payments\nWHERE qty > 0\nGROUP BY name\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["SELECT","WHERE","ORDER BY","FROM"],"answer":3},{"id":782,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT price FROM subs)\nUNION ALL\n(SELECT price FROM visitors);","options":["An error is raised for the duplicate row","The duplicate is removed — UNION ALL deduplicates","Both copies are kept — UNION ALL never removes duplicates","Only one of the two queries executes"],"answer":2},{"id":783,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT kind,\n       ROW_NUMBER() OVER (PARTITION BY order_id) rn\nFROM tickets;","options":["Yes — partitions are always sorted by insertion order","Error — ORDER BY is required inside OVER with ROW_NUMBER","No — without ORDER BY inside OVER, order within each partition is undefined","Yes — ROW_NUMBER is always deterministic"],"answer":2},{"id":784,"type":"practical","topic":"window","q":"What is prev_code for the very first row in the ordering?\nSELECT amount,\n       code,\n       LAG(code) OVER (ORDER BY amount) AS prev_code\nFROM shipments;","options":["NULL — no preceding row exists for the first row","0 (zero default when there is no previous row)","An error is raised for the first row","The value of code for the last row in the table"],"answer":0},{"id":785,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.name = o.name AND 1 = 0\n);","options":["NULL — three-valued logic makes this unknown","Error — subquery must always return at least one row","TRUE — EXISTS ignores the inner WHERE conditions","FALSE — EXISTS is false when the subquery returns no rows"],"answer":3},{"id":786,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; user_id is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(user_id) FROM clicks;","options":["6, 4 — COUNT(*) counts all rows; COUNT(user_id) skips NULLs","6, 6 — both functions count all rows","4, 4 — both functions skip NULLs","Error — you cannot mix COUNT(*) and COUNT(user_id)"],"answer":0},{"id":787,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["UNKNOWN (distinct from NULL in the standard)","FALSE — AND with FALSE is always FALSE regardless of the other operand","NULL","TRUE"],"answer":1},{"id":788,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT amount FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE amount > 0\n)\nSELECT * FROM b;","options":["Yes — b can reference a defined earlier in the same WITH","Only if both CTEs are declared RECURSIVE","No — WITH must contain exactly one named query","No — CTEs cannot reference each other"],"answer":0},{"id":789,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on status?\nSELECT * FROM campaigns\nWHERE UPPER(status) = 'ACTIVE';","options":["UPPER() always improves index efficiency","Indexes do not work with string columns","The optimizer always ignores scalar functions in WHERE","UPPER() wraps status, preventing an index seek on the raw column"],"answer":3},{"id":790,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE created_at IS NOT NULL;","options":["B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","Both statements are identical in every database","A can be rolled back; B cannot","B removes only non-NULL rows; A removes nothing"],"answer":0},{"id":791,"type":"practical","topic":"joins","q":"visitors has 5 rows; flags has 4 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["5 — only visitors rows","20 — Cartesian product from the missing join condition","0 — missing WHERE means no rows pass","9 — rows from both tables appended"],"answer":1},{"id":792,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — COUNT(*) cannot appear in both SELECT and HAVING","Returns every row of devices filtered by the count","Yes — the whole table is one implicit group; returns one row if any rows exist","Error — HAVING requires a GROUP BY clause"],"answer":2},{"id":793,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(2, 2) — both functions agree after a tie","(3, 3) — both leave a gap after a tie","(2, 3) — DENSE_RANK skips; RANK does not","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2"],"answer":3},{"id":794,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT flag,\n       (SELECT kind FROM events) AS x\nFROM products;","options":["The query auto-aggregates events to one row","The first row's kind value is silently used","Runtime error — scalar subquery returned more than one row","Three copies of each products row are returned"],"answer":2},{"id":795,"type":"practical","topic":"indexes","q":"An index exists on (tier, code).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE code = 'active';","options":["Yes — composite indexes cover any subset of their columns equally","No — composite indexes never speed up equality predicates","No — filtering only code skips the leading column tier; leftmost-prefix rule not satisfied","Yes — code is in the index so seeks always work"],"answer":2},{"id":796,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT qty, name FROM invoices;","options":["3 — DISTINCT deduplicates the (qty, name) pair; (1,'a') appears once","4 — DISTINCT has no effect on two-column selects","1 — DISTINCT keeps only the row with the lowest qty","2 — only unique values of the first column qty"],"answer":0},{"id":797,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(price) FROM coupons\nWHERE 1 = 0;","options":["Error — MAX on an empty set is undefined","One row with MAX = 0","Zero rows returned","One row with MAX = NULL"],"answer":3},{"id":798,"type":"practical","topic":"joins","q":"Both tables have rows where kind IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.kind = b.kind;","options":["No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — NULLs are treated as equal in ON conditions","Yes — but only with INNER JOIN, not OUTER JOIN"],"answer":0},{"id":799,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT code FROM metrics)\nEXCEPT\n(SELECT code FROM tickets);","options":["Values in tickets that do not appear in metrics","Values in metrics that do not appear in tickets","Values that appear in both metrics and tickets","All values from both metrics and tickets"],"answer":1},{"id":800,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT name, NTILE(4) OVER (ORDER BY status) bucket\nFROM flags;","options":["3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","4, 3, 2, 1 — strictly decreasing bucket sizes","All 4 buckets have exactly 2 rows (remainder discarded)","2, 2, 3, 3 — smaller buckets come first"],"answer":0},{"id":801,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE user_id = NULL;","options":["All rows where user_id is NULL","Same result as WHERE user_id IS NOT NULL","A syntax error at runtime","Zero rows — = NULL always evaluates to UNKNOWN"],"answer":3},{"id":802,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT order_id, region, COUNT(*)\nFROM users\nGROUP BY order_id;","options":["Error: region is in SELECT but not in GROUP BY","Valid — any column can appear alongside an aggregate","Error: COUNT(*) cannot appear with a GROUP BY","Error: GROUP BY must use an alias, not a column name"],"answer":0},{"id":803,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT amount, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY amount;","options":["Returns groups with count > 2 correctly","Error — aggregate functions are not allowed in WHERE; use HAVING","WHERE and HAVING are interchangeable here","Runs fine; WHERE filters rows before aggregation"],"answer":1},{"id":804,"type":"practical","topic":"joins","q":"skus has a row where status = 999; accounts has no matching row.\nWhat appears in b.score for that skus row?\nSELECT a.status, b.score\nFROM skus a\nLEFT JOIN accounts b ON a.status = b.status;","options":["NULL — unmatched left rows are kept with NULL on the right side","b.score takes the column's default value","An error is raised for the unmatched key","The row is excluded from the result"],"answer":0},{"id":805,"type":"practical","topic":"joins","q":"sessions has 7 rows; campaigns has 2 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["7 — only sessions rows","9 — rows from both tables appended","2 — only matching-key rows","14 — Cartesian product (7 × 2)"],"answer":3},{"id":806,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT tier, COUNT(*) cnt\nFROM payments\nWHERE region > 0\nGROUP BY tier\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["WHERE","SELECT","FROM","ORDER BY"],"answer":2},{"id":807,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT sku FROM subs)\nUNION ALL\n(SELECT sku FROM visitors);","options":["The duplicate is removed — UNION ALL deduplicates","Only one of the two queries executes","Both copies are kept — UNION ALL never removes duplicates","An error is raised for the duplicate row"],"answer":2},{"id":808,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT score,\n       ROW_NUMBER() OVER (PARTITION BY price) rn\nFROM tickets;","options":["Yes — ROW_NUMBER is always deterministic","Error — ORDER BY is required inside OVER with ROW_NUMBER","No — without ORDER BY inside OVER, order within each partition is undefined","Yes — partitions are always sorted by insertion order"],"answer":2},{"id":809,"type":"practical","topic":"window","q":"What is prev_flag for the very first row in the ordering?\nSELECT kind,\n       flag,\n       LAG(flag) OVER (ORDER BY kind) AS prev_flag\nFROM shipments;","options":["0 (zero default when there is no previous row)","An error is raised for the first row","The value of flag for the last row in the table","NULL — no preceding row exists for the first row"],"answer":3},{"id":810,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.tier = o.tier AND 1 = 0\n);","options":["FALSE — EXISTS is false when the subquery returns no rows","NULL — three-valued logic makes this unknown","TRUE — EXISTS ignores the inner WHERE conditions","Error — subquery must always return at least one row"],"answer":0},{"id":811,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; qty is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(qty) FROM clicks;","options":["Error — you cannot mix COUNT(*) and COUNT(qty)","6, 4 — COUNT(*) counts all rows; COUNT(qty) skips NULLs","4, 4 — both functions skip NULLs","6, 6 — both functions count all rows"],"answer":1},{"id":812,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["FALSE — AND with FALSE is always FALSE regardless of the other operand","TRUE","UNKNOWN (distinct from NULL in the standard)","NULL"],"answer":0},{"id":813,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT kind FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE kind > 0\n)\nSELECT * FROM b;","options":["No — CTEs cannot reference each other","Only if both CTEs are declared RECURSIVE","No — WITH must contain exactly one named query","Yes — b can reference a defined earlier in the same WITH"],"answer":3},{"id":814,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on code?\nSELECT * FROM campaigns\nWHERE UPPER(code) = 'ACTIVE';","options":["UPPER() wraps code, preventing an index seek on the raw column","UPPER() always improves index efficiency","The optimizer always ignores scalar functions in WHERE","Indexes do not work with string columns"],"answer":0},{"id":815,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE name IS NOT NULL;","options":["Both statements are identical in every database","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","B removes only non-NULL rows; A removes nothing","A can be rolled back; B cannot"],"answer":1},{"id":816,"type":"practical","topic":"joins","q":"visitors has 6 rows; flags has 5 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["30 — Cartesian product from the missing join condition","0 — missing WHERE means no rows pass","11 — rows from both tables appended","6 — only visitors rows"],"answer":0},{"id":817,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — HAVING requires a GROUP BY clause","Returns every row of devices filtered by the count","Error — COUNT(*) cannot appear in both SELECT and HAVING","Yes — the whole table is one implicit group; returns one row if any rows exist"],"answer":3},{"id":818,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(3, 3) — both leave a gap after a tie","(2, 2) — both functions agree after a tie","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(2, 3) — DENSE_RANK skips; RANK does not"],"answer":2},{"id":819,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT status,\n       (SELECT score FROM events) AS x\nFROM products;","options":["The first row's score value is silently used","Three copies of each products row are returned","Runtime error — scalar subquery returned more than one row","The query auto-aggregates events to one row"],"answer":2},{"id":820,"type":"practical","topic":"indexes","q":"An index exists on (created_at, flag).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE flag = 'active';","options":["Yes — flag is in the index so seeks always work","No — composite indexes never speed up equality predicates","No — filtering only flag skips the leading column created_at; leftmost-prefix rule not satisfied","Yes — composite indexes cover any subset of their columns equally"],"answer":2},{"id":821,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT region, tier FROM invoices;","options":["2 — only unique values of the first column region","4 — DISTINCT has no effect on two-column selects","1 — DISTINCT keeps only the row with the lowest region","3 — DISTINCT deduplicates the (region, tier) pair; (1,'a') appears once"],"answer":3},{"id":822,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(sku) FROM coupons\nWHERE 1 = 0;","options":["One row with MAX = NULL","Error — MAX on an empty set is undefined","Zero rows returned","One row with MAX = 0"],"answer":0},{"id":823,"type":"practical","topic":"joins","q":"Both tables have rows where score IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.score = b.score;","options":["Yes — but only with INNER JOIN, not OUTER JOIN","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Yes — NULLs are treated as equal in ON conditions","Only if both sides are NULL simultaneously (special NULL-safe rule)"],"answer":1},{"id":824,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT flag FROM metrics)\nEXCEPT\n(SELECT flag FROM tickets);","options":["Values in metrics that do not appear in tickets","All values from both metrics and tickets","Values in tickets that do not appear in metrics","Values that appear in both metrics and tickets"],"answer":0},{"id":825,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT tier, NTILE(4) OVER (ORDER BY code) bucket\nFROM flags;","options":["4, 3, 2, 1 — strictly decreasing bucket sizes","All 4 buckets have exactly 2 rows (remainder discarded)","2, 2, 3, 3 — smaller buckets come first","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first"],"answer":3},{"id":826,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE qty = NULL;","options":["Zero rows — = NULL always evaluates to UNKNOWN","All rows where qty is NULL","A syntax error at runtime","Same result as WHERE qty IS NOT NULL"],"answer":0},{"id":827,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT price, user_id, COUNT(*)\nFROM users\nGROUP BY price;","options":["Valid — any column can appear alongside an aggregate","Error: user_id is in SELECT but not in GROUP BY","Error: GROUP BY must use an alias, not a column name","Error: COUNT(*) cannot appear with a GROUP BY"],"answer":1},{"id":828,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT kind, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY kind;","options":["Error — aggregate functions are not allowed in WHERE; use HAVING","WHERE and HAVING are interchangeable here","Runs fine; WHERE filters rows before aggregation","Returns groups with count > 2 correctly"],"answer":0},{"id":829,"type":"practical","topic":"joins","q":"skus has a row where code = 999; accounts has no matching row.\nWhat appears in b.amount for that skus row?\nSELECT a.code, b.amount\nFROM skus a\nLEFT JOIN accounts b ON a.code = b.code;","options":["The row is excluded from the result","b.amount takes the column's default value","An error is raised for the unmatched key","NULL — unmatched left rows are kept with NULL on the right side"],"answer":3},{"id":830,"type":"practical","topic":"joins","q":"sessions has 8 rows; campaigns has 3 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["11 — rows from both tables appended","8 — only sessions rows","24 — Cartesian product (8 × 3)","3 — only matching-key rows"],"answer":2},{"id":831,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT created_at, COUNT(*) cnt\nFROM payments\nWHERE user_id > 0\nGROUP BY created_at\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["SELECT","ORDER BY","FROM","WHERE"],"answer":2},{"id":832,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT order_id FROM subs)\nUNION ALL\n(SELECT order_id FROM visitors);","options":["An error is raised for the duplicate row","Only one of the two queries executes","Both copies are kept — UNION ALL never removes duplicates","The duplicate is removed — UNION ALL deduplicates"],"answer":2},{"id":833,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT amount,\n       ROW_NUMBER() OVER (PARTITION BY sku) rn\nFROM tickets;","options":["Yes — partitions are always sorted by insertion order","Yes — ROW_NUMBER is always deterministic","Error — ORDER BY is required inside OVER with ROW_NUMBER","No — without ORDER BY inside OVER, order within each partition is undefined"],"answer":3},{"id":834,"type":"practical","topic":"window","q":"What is prev_status for the very first row in the ordering?\nSELECT score,\n       status,\n       LAG(status) OVER (ORDER BY score) AS prev_status\nFROM shipments;","options":["0 (zero default when there is no previous row)","The value of status for the last row in the table","NULL — no preceding row exists for the first row","An error is raised for the first row"],"answer":2},{"id":835,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.created_at = o.created_at AND 1 = 0\n);","options":["Error — subquery must always return at least one row","FALSE — EXISTS is false when the subquery returns no rows","TRUE — EXISTS ignores the inner WHERE conditions","NULL — three-valued logic makes this unknown"],"answer":1},{"id":836,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; region is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(region) FROM clicks;","options":["6, 4 — COUNT(*) counts all rows; COUNT(region) skips NULLs","6, 6 — both functions count all rows","Error — you cannot mix COUNT(*) and COUNT(region)","4, 4 — both functions skip NULLs"],"answer":0},{"id":837,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["TRUE","UNKNOWN (distinct from NULL in the standard)","NULL","FALSE — AND with FALSE is always FALSE regardless of the other operand"],"answer":3},{"id":838,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT score FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE score > 0\n)\nSELECT * FROM b;","options":["Yes — b can reference a defined earlier in the same WITH","No — WITH must contain exactly one named query","No — CTEs cannot reference each other","Only if both CTEs are declared RECURSIVE"],"answer":0},{"id":839,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on flag?\nSELECT * FROM campaigns\nWHERE UPPER(flag) = 'ACTIVE';","options":["UPPER() always improves index efficiency","UPPER() wraps flag, preventing an index seek on the raw column","Indexes do not work with string columns","The optimizer always ignores scalar functions in WHERE"],"answer":1},{"id":840,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE tier IS NOT NULL;","options":["B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","B removes only non-NULL rows; A removes nothing","A can be rolled back; B cannot","Both statements are identical in every database"],"answer":0},{"id":841,"type":"practical","topic":"joins","q":"visitors has 7 rows; flags has 2 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["7 — only visitors rows","0 — missing WHERE means no rows pass","9 — rows from both tables appended","14 — Cartesian product from the missing join condition"],"answer":3},{"id":842,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Yes — the whole table is one implicit group; returns one row if any rows exist","Error — HAVING requires a GROUP BY clause","Error — COUNT(*) cannot appear in both SELECT and HAVING","Returns every row of devices filtered by the count"],"answer":0},{"id":843,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(2, 2) — both functions agree after a tie","(2, 3) — DENSE_RANK skips; RANK does not","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(3, 3) — both leave a gap after a tie"],"answer":2},{"id":844,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT code,\n       (SELECT amount FROM events) AS x\nFROM products;","options":["The query auto-aggregates events to one row","Three copies of each products row are returned","Runtime error — scalar subquery returned more than one row","The first row's amount value is silently used"],"answer":2},{"id":845,"type":"practical","topic":"indexes","q":"An index exists on (name, status).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE status = 'active';","options":["Yes — composite indexes cover any subset of their columns equally","Yes — status is in the index so seeks always work","No — composite indexes never speed up equality predicates","No — filtering only status skips the leading column name; leftmost-prefix rule not satisfied"],"answer":3},{"id":846,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["4 — DISTINCT has no effect on two-column selects","2 — only unique values of the first column user_id","3 — DISTINCT deduplicates the (user_id, created_at) pair; (1,'a') appears once","1 — DISTINCT keeps only the row with the lowest user_id"],"answer":2},{"id":847,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(order_id) FROM coupons\nWHERE 1 = 0;","options":["One row with MAX = 0","One row with MAX = NULL","Zero rows returned","Error — MAX on an empty set is undefined"],"answer":1},{"id":848,"type":"practical","topic":"joins","q":"Both tables have rows where amount IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.amount = b.amount;","options":["No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — but only with INNER JOIN, not OUTER JOIN","Yes — NULLs are treated as equal in ON conditions"],"answer":0},{"id":849,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT status FROM metrics)\nEXCEPT\n(SELECT status FROM tickets);","options":["All values from both metrics and tickets","Values in tickets that do not appear in metrics","Values that appear in both metrics and tickets","Values in metrics that do not appear in tickets"],"answer":3},{"id":850,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) bucket\nFROM flags;","options":["3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","4, 3, 2, 1 — strictly decreasing bucket sizes","2, 2, 3, 3 — smaller buckets come first","All 4 buckets have exactly 2 rows (remainder discarded)"],"answer":0},{"id":851,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE region = NULL;","options":["Same result as WHERE region IS NOT NULL","Zero rows — = NULL always evaluates to UNKNOWN","All rows where region is NULL","A syntax error at runtime"],"answer":1},{"id":852,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT sku, qty, COUNT(*)\nFROM users\nGROUP BY sku;","options":["Error: qty is in SELECT but not in GROUP BY","Error: GROUP BY must use an alias, not a column name","Error: COUNT(*) cannot appear with a GROUP BY","Valid — any column can appear alongside an aggregate"],"answer":0},{"id":853,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT score, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY score;","options":["Returns groups with count > 2 correctly","WHERE and HAVING are interchangeable here","Runs fine; WHERE filters rows before aggregation","Error — aggregate functions are not allowed in WHERE; use HAVING"],"answer":3},{"id":854,"type":"practical","topic":"joins","q":"skus has a row where flag = 999; accounts has no matching row.\nWhat appears in b.kind for that skus row?\nSELECT a.flag, b.kind\nFROM skus a\nLEFT JOIN accounts b ON a.flag = b.flag;","options":["NULL — unmatched left rows are kept with NULL on the right side","The row is excluded from the result","An error is raised for the unmatched key","b.kind takes the column's default value"],"answer":0},{"id":855,"type":"practical","topic":"joins","q":"sessions has 3 rows; campaigns has 4 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["3 — only sessions rows","12 — Cartesian product (3 × 4)","3 — only matching-key rows","7 — rows from both tables appended"],"answer":1},{"id":856,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT name, COUNT(*) cnt\nFROM payments\nWHERE qty > 0\nGROUP BY name\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["WHERE","ORDER BY","FROM","SELECT"],"answer":2},{"id":857,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT price FROM subs)\nUNION ALL\n(SELECT price FROM visitors);","options":["The duplicate is removed — UNION ALL deduplicates","An error is raised for the duplicate row","Only one of the two queries executes","Both copies are kept — UNION ALL never removes duplicates"],"answer":3},{"id":858,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT kind,\n       ROW_NUMBER() OVER (PARTITION BY order_id) rn\nFROM tickets;","options":["Yes — ROW_NUMBER is always deterministic","Yes — partitions are always sorted by insertion order","No — without ORDER BY inside OVER, order within each partition is undefined","Error — ORDER BY is required inside OVER with ROW_NUMBER"],"answer":2},{"id":859,"type":"practical","topic":"window","q":"What is prev_code for the very first row in the ordering?\nSELECT amount,\n       code,\n       LAG(code) OVER (ORDER BY amount) AS prev_code\nFROM shipments;","options":["The value of code for the last row in the table","An error is raised for the first row","NULL — no preceding row exists for the first row","0 (zero default when there is no previous row)"],"answer":2},{"id":860,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.name = o.name AND 1 = 0\n);","options":["FALSE — EXISTS is false when the subquery returns no rows","NULL — three-valued logic makes this unknown","Error — subquery must always return at least one row","TRUE — EXISTS ignores the inner WHERE conditions"],"answer":0},{"id":861,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; user_id is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(user_id) FROM clicks;","options":["6, 6 — both functions count all rows","Error — you cannot mix COUNT(*) and COUNT(user_id)","4, 4 — both functions skip NULLs","6, 4 — COUNT(*) counts all rows; COUNT(user_id) skips NULLs"],"answer":3},{"id":862,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["FALSE — AND with FALSE is always FALSE regardless of the other operand","TRUE","NULL","UNKNOWN (distinct from NULL in the standard)"],"answer":0},{"id":863,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT amount FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE amount > 0\n)\nSELECT * FROM b;","options":["Only if both CTEs are declared RECURSIVE","Yes — b can reference a defined earlier in the same WITH","No — CTEs cannot reference each other","No — WITH must contain exactly one named query"],"answer":1},{"id":864,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on status?\nSELECT * FROM campaigns\nWHERE UPPER(status) = 'ACTIVE';","options":["UPPER() wraps status, preventing an index seek on the raw column","Indexes do not work with string columns","The optimizer always ignores scalar functions in WHERE","UPPER() always improves index efficiency"],"answer":0},{"id":865,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE created_at IS NOT NULL;","options":["Both statements are identical in every database","B removes only non-NULL rows; A removes nothing","A can be rolled back; B cannot","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging"],"answer":3},{"id":866,"type":"practical","topic":"joins","q":"visitors has 8 rows; flags has 3 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["24 — Cartesian product from the missing join condition","8 — only visitors rows","11 — rows from both tables appended","0 — missing WHERE means no rows pass"],"answer":0},{"id":867,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — HAVING requires a GROUP BY clause","Yes — the whole table is one implicit group; returns one row if any rows exist","Returns every row of devices filtered by the count","Error — COUNT(*) cannot appear in both SELECT and HAVING"],"answer":1},{"id":868,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(2, 3) — DENSE_RANK skips; RANK does not","(3, 3) — both leave a gap after a tie","(2, 2) — both functions agree after a tie"],"answer":0},{"id":869,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT flag,\n       (SELECT kind FROM events) AS x\nFROM products;","options":["The first row's kind value is silently used","The query auto-aggregates events to one row","Three copies of each products row are returned","Runtime error — scalar subquery returned more than one row"],"answer":3},{"id":870,"type":"practical","topic":"indexes","q":"An index exists on (tier, code).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE code = 'active';","options":["Yes — code is in the index so seeks always work","Yes — composite indexes cover any subset of their columns equally","No — filtering only code skips the leading column tier; leftmost-prefix rule not satisfied","No — composite indexes never speed up equality predicates"],"answer":2},{"id":871,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT qty, name FROM invoices;","options":["2 — only unique values of the first column qty","1 — DISTINCT keeps only the row with the lowest qty","3 — DISTINCT deduplicates the (qty, name) pair; (1,'a') appears once","4 — DISTINCT has no effect on two-column selects"],"answer":2},{"id":872,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(price) FROM coupons\nWHERE 1 = 0;","options":["Error — MAX on an empty set is undefined","One row with MAX = 0","One row with MAX = NULL","Zero rows returned"],"answer":2},{"id":873,"type":"practical","topic":"joins","q":"Both tables have rows where kind IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.kind = b.kind;","options":["Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — but only with INNER JOIN, not OUTER JOIN","Yes — NULLs are treated as equal in ON conditions","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN"],"answer":3},{"id":874,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT code FROM metrics)\nEXCEPT\n(SELECT code FROM tickets);","options":["Values in metrics that do not appear in tickets","All values from both metrics and tickets","Values that appear in both metrics and tickets","Values in tickets that do not appear in metrics"],"answer":0},{"id":875,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT name, NTILE(4) OVER (ORDER BY status) bucket\nFROM flags;","options":["All 4 buckets have exactly 2 rows (remainder discarded)","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","2, 2, 3, 3 — smaller buckets come first","4, 3, 2, 1 — strictly decreasing bucket sizes"],"answer":1},{"id":876,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE user_id = NULL;","options":["Zero rows — = NULL always evaluates to UNKNOWN","A syntax error at runtime","Same result as WHERE user_id IS NOT NULL","All rows where user_id is NULL"],"answer":0},{"id":877,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT order_id, region, COUNT(*)\nFROM users\nGROUP BY order_id;","options":["Valid — any column can appear alongside an aggregate","Error: GROUP BY must use an alias, not a column name","Error: COUNT(*) cannot appear with a GROUP BY","Error: region is in SELECT but not in GROUP BY"],"answer":3},{"id":878,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT amount, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY amount;","options":["Error — aggregate functions are not allowed in WHERE; use HAVING","Returns groups with count > 2 correctly","Runs fine; WHERE filters rows before aggregation","WHERE and HAVING are interchangeable here"],"answer":0},{"id":879,"type":"practical","topic":"joins","q":"skus has a row where status = 999; accounts has no matching row.\nWhat appears in b.score for that skus row?\nSELECT a.status, b.score\nFROM skus a\nLEFT JOIN accounts b ON a.status = b.status;","options":["The row is excluded from the result","NULL — unmatched left rows are kept with NULL on the right side","b.score takes the column's default value","An error is raised for the unmatched key"],"answer":1},{"id":880,"type":"practical","topic":"joins","q":"sessions has 4 rows; campaigns has 5 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["20 — Cartesian product (4 × 5)","4 — only matching-key rows","9 — rows from both tables appended","4 — only sessions rows"],"answer":0},{"id":881,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT tier, COUNT(*) cnt\nFROM payments\nWHERE region > 0\nGROUP BY tier\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["SELECT","WHERE","ORDER BY","FROM"],"answer":3},{"id":882,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT sku FROM subs)\nUNION ALL\n(SELECT sku FROM visitors);","options":["An error is raised for the duplicate row","The duplicate is removed — UNION ALL deduplicates","Both copies are kept — UNION ALL never removes duplicates","Only one of the two queries executes"],"answer":2},{"id":883,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT score,\n       ROW_NUMBER() OVER (PARTITION BY price) rn\nFROM tickets;","options":["Yes — partitions are always sorted by insertion order","Error — ORDER BY is required inside OVER with ROW_NUMBER","No — without ORDER BY inside OVER, order within each partition is undefined","Yes — ROW_NUMBER is always deterministic"],"answer":2},{"id":884,"type":"practical","topic":"window","q":"What is prev_flag for the very first row in the ordering?\nSELECT kind,\n       flag,\n       LAG(flag) OVER (ORDER BY kind) AS prev_flag\nFROM shipments;","options":["0 (zero default when there is no previous row)","An error is raised for the first row","NULL — no preceding row exists for the first row","The value of flag for the last row in the table"],"answer":2},{"id":885,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.tier = o.tier AND 1 = 0\n);","options":["TRUE — EXISTS ignores the inner WHERE conditions","NULL — three-valued logic makes this unknown","Error — subquery must always return at least one row","FALSE — EXISTS is false when the subquery returns no rows"],"answer":3},{"id":886,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; qty is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(qty) FROM clicks;","options":["6, 4 — COUNT(*) counts all rows; COUNT(qty) skips NULLs","6, 6 — both functions count all rows","4, 4 — both functions skip NULLs","Error — you cannot mix COUNT(*) and COUNT(qty)"],"answer":0},{"id":887,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["UNKNOWN (distinct from NULL in the standard)","FALSE — AND with FALSE is always FALSE regardless of the other operand","NULL","TRUE"],"answer":1},{"id":888,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT kind FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE kind > 0\n)\nSELECT * FROM b;","options":["Yes — b can reference a defined earlier in the same WITH","No — WITH must contain exactly one named query","Only if both CTEs are declared RECURSIVE","No — CTEs cannot reference each other"],"answer":0},{"id":889,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on code?\nSELECT * FROM campaigns\nWHERE UPPER(code) = 'ACTIVE';","options":["The optimizer always ignores scalar functions in WHERE","Indexes do not work with string columns","UPPER() always improves index efficiency","UPPER() wraps code, preventing an index seek on the raw column"],"answer":3},{"id":890,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE name IS NOT NULL;","options":["B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","Both statements are identical in every database","A can be rolled back; B cannot","B removes only non-NULL rows; A removes nothing"],"answer":0},{"id":891,"type":"practical","topic":"joins","q":"visitors has 3 rows; flags has 4 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["3 — only visitors rows","12 — Cartesian product from the missing join condition","0 — missing WHERE means no rows pass","7 — rows from both tables appended"],"answer":1},{"id":892,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Yes — the whole table is one implicit group; returns one row if any rows exist","Returns every row of devices filtered by the count","Error — COUNT(*) cannot appear in both SELECT and HAVING","Error — HAVING requires a GROUP BY clause"],"answer":0},{"id":893,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(2, 2) — both functions agree after a tie","(2, 3) — DENSE_RANK skips; RANK does not","(3, 3) — both leave a gap after a tie","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2"],"answer":3},{"id":894,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT status,\n       (SELECT score FROM events) AS x\nFROM products;","options":["The query auto-aggregates events to one row","The first row's score value is silently used","Runtime error — scalar subquery returned more than one row","Three copies of each products row are returned"],"answer":2},{"id":895,"type":"practical","topic":"indexes","q":"An index exists on (created_at, flag).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE flag = 'active';","options":["Yes — composite indexes cover any subset of their columns equally","No — composite indexes never speed up equality predicates","No — filtering only flag skips the leading column created_at; leftmost-prefix rule not satisfied","Yes — flag is in the index so seeks always work"],"answer":2},{"id":896,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT region, tier FROM invoices;","options":["4 — DISTINCT has no effect on two-column selects","1 — DISTINCT keeps only the row with the lowest region","3 — DISTINCT deduplicates the (region, tier) pair; (1,'a') appears once","2 — only unique values of the first column region"],"answer":2},{"id":897,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(sku) FROM coupons\nWHERE 1 = 0;","options":["Zero rows returned","Error — MAX on an empty set is undefined","One row with MAX = 0","One row with MAX = NULL"],"answer":3},{"id":898,"type":"practical","topic":"joins","q":"Both tables have rows where score IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.score = b.score;","options":["Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — NULLs are treated as equal in ON conditions","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Yes — but only with INNER JOIN, not OUTER JOIN"],"answer":2},{"id":899,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT flag FROM metrics)\nEXCEPT\n(SELECT flag FROM tickets);","options":["Values in tickets that do not appear in metrics","Values in metrics that do not appear in tickets","Values that appear in both metrics and tickets","All values from both metrics and tickets"],"answer":1},{"id":900,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT tier, NTILE(4) OVER (ORDER BY code) bucket\nFROM flags;","options":["3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","4, 3, 2, 1 — strictly decreasing bucket sizes","All 4 buckets have exactly 2 rows (remainder discarded)","2, 2, 3, 3 — smaller buckets come first"],"answer":0},{"id":901,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE qty = NULL;","options":["A syntax error at runtime","Same result as WHERE qty IS NOT NULL","All rows where qty is NULL","Zero rows — = NULL always evaluates to UNKNOWN"],"answer":3},{"id":902,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT price, user_id, COUNT(*)\nFROM users\nGROUP BY price;","options":["Error: user_id is in SELECT but not in GROUP BY","Error: COUNT(*) cannot appear with a GROUP BY","Valid — any column can appear alongside an aggregate","Error: GROUP BY must use an alias, not a column name"],"answer":0},{"id":903,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT kind, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY kind;","options":["Returns groups with count > 2 correctly","Error — aggregate functions are not allowed in WHERE; use HAVING","WHERE and HAVING are interchangeable here","Runs fine; WHERE filters rows before aggregation"],"answer":1},{"id":904,"type":"practical","topic":"joins","q":"skus has a row where code = 999; accounts has no matching row.\nWhat appears in b.amount for that skus row?\nSELECT a.code, b.amount\nFROM skus a\nLEFT JOIN accounts b ON a.code = b.code;","options":["NULL — unmatched left rows are kept with NULL on the right side","b.amount takes the column's default value","An error is raised for the unmatched key","The row is excluded from the result"],"answer":0},{"id":905,"type":"practical","topic":"joins","q":"sessions has 5 rows; campaigns has 2 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["5 — only sessions rows","2 — only matching-key rows","7 — rows from both tables appended","10 — Cartesian product (5 × 2)"],"answer":3},{"id":906,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT created_at, COUNT(*) cnt\nFROM payments\nWHERE user_id > 0\nGROUP BY created_at\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["FROM","SELECT","WHERE","ORDER BY"],"answer":0},{"id":907,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT order_id FROM subs)\nUNION ALL\n(SELECT order_id FROM visitors);","options":["The duplicate is removed — UNION ALL deduplicates","Only one of the two queries executes","Both copies are kept — UNION ALL never removes duplicates","An error is raised for the duplicate row"],"answer":2},{"id":908,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT amount,\n       ROW_NUMBER() OVER (PARTITION BY sku) rn\nFROM tickets;","options":["Yes — ROW_NUMBER is always deterministic","Error — ORDER BY is required inside OVER with ROW_NUMBER","No — without ORDER BY inside OVER, order within each partition is undefined","Yes — partitions are always sorted by insertion order"],"answer":2},{"id":909,"type":"practical","topic":"window","q":"What is prev_status for the very first row in the ordering?\nSELECT score,\n       status,\n       LAG(status) OVER (ORDER BY score) AS prev_status\nFROM shipments;","options":["The value of status for the last row in the table","0 (zero default when there is no previous row)","An error is raised for the first row","NULL — no preceding row exists for the first row"],"answer":3},{"id":910,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.created_at = o.created_at AND 1 = 0\n);","options":["NULL — three-valued logic makes this unknown","TRUE — EXISTS ignores the inner WHERE conditions","FALSE — EXISTS is false when the subquery returns no rows","Error — subquery must always return at least one row"],"answer":2},{"id":911,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; region is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(region) FROM clicks;","options":["Error — you cannot mix COUNT(*) and COUNT(region)","6, 4 — COUNT(*) counts all rows; COUNT(region) skips NULLs","4, 4 — both functions skip NULLs","6, 6 — both functions count all rows"],"answer":1},{"id":912,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["FALSE — AND with FALSE is always FALSE regardless of the other operand","TRUE","UNKNOWN (distinct from NULL in the standard)","NULL"],"answer":0},{"id":913,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT score FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE score > 0\n)\nSELECT * FROM b;","options":["No — WITH must contain exactly one named query","Only if both CTEs are declared RECURSIVE","No — CTEs cannot reference each other","Yes — b can reference a defined earlier in the same WITH"],"answer":3},{"id":914,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on flag?\nSELECT * FROM campaigns\nWHERE UPPER(flag) = 'ACTIVE';","options":["UPPER() wraps flag, preventing an index seek on the raw column","The optimizer always ignores scalar functions in WHERE","UPPER() always improves index efficiency","Indexes do not work with string columns"],"answer":0},{"id":915,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE tier IS NOT NULL;","options":["B removes only non-NULL rows; A removes nothing","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","Both statements are identical in every database","A can be rolled back; B cannot"],"answer":1},{"id":916,"type":"practical","topic":"joins","q":"visitors has 4 rows; flags has 5 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["20 — Cartesian product from the missing join condition","0 — missing WHERE means no rows pass","9 — rows from both tables appended","4 — only visitors rows"],"answer":0},{"id":917,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — HAVING requires a GROUP BY clause","Returns every row of devices filtered by the count","Error — COUNT(*) cannot appear in both SELECT and HAVING","Yes — the whole table is one implicit group; returns one row if any rows exist"],"answer":3},{"id":918,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(2, 2) — both functions agree after a tie","(3, 3) — both leave a gap after a tie","(2, 3) — DENSE_RANK skips; RANK does not"],"answer":0},{"id":919,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT code,\n       (SELECT amount FROM events) AS x\nFROM products;","options":["The first row's amount value is silently used","Runtime error — scalar subquery returned more than one row","Three copies of each products row are returned","The query auto-aggregates events to one row"],"answer":1},{"id":920,"type":"practical","topic":"indexes","q":"An index exists on (name, status).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE status = 'active';","options":["Yes — status is in the index so seeks always work","No — composite indexes never speed up equality predicates","No — filtering only status skips the leading column name; leftmost-prefix rule not satisfied","Yes — composite indexes cover any subset of their columns equally"],"answer":2},{"id":921,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["2 — only unique values of the first column user_id","4 — DISTINCT has no effect on two-column selects","1 — DISTINCT keeps only the row with the lowest user_id","3 — DISTINCT deduplicates the (user_id, created_at) pair; (1,'a') appears once"],"answer":3},{"id":922,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(order_id) FROM coupons\nWHERE 1 = 0;","options":["Error — MAX on an empty set is undefined","Zero rows returned","One row with MAX = NULL","One row with MAX = 0"],"answer":2},{"id":923,"type":"practical","topic":"joins","q":"Both tables have rows where amount IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.amount = b.amount;","options":["Yes — NULLs are treated as equal in ON conditions","Yes — but only with INNER JOIN, not OUTER JOIN","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Only if both sides are NULL simultaneously (special NULL-safe rule)"],"answer":2},{"id":924,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT status FROM metrics)\nEXCEPT\n(SELECT status FROM tickets);","options":["Values in metrics that do not appear in tickets","All values from both metrics and tickets","Values in tickets that do not appear in metrics","Values that appear in both metrics and tickets"],"answer":0},{"id":925,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) bucket\nFROM flags;","options":["4, 3, 2, 1 — strictly decreasing bucket sizes","All 4 buckets have exactly 2 rows (remainder discarded)","2, 2, 3, 3 — smaller buckets come first","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first"],"answer":3},{"id":926,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE region = NULL;","options":["Zero rows — = NULL always evaluates to UNKNOWN","A syntax error at runtime","All rows where region is NULL","Same result as WHERE region IS NOT NULL"],"answer":0},{"id":927,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT sku, qty, COUNT(*)\nFROM users\nGROUP BY sku;","options":["Error: GROUP BY must use an alias, not a column name","Error: qty is in SELECT but not in GROUP BY","Valid — any column can appear alongside an aggregate","Error: COUNT(*) cannot appear with a GROUP BY"],"answer":1},{"id":928,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT score, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY score;","options":["Error — aggregate functions are not allowed in WHERE; use HAVING","WHERE and HAVING are interchangeable here","Runs fine; WHERE filters rows before aggregation","Returns groups with count > 2 correctly"],"answer":0},{"id":929,"type":"practical","topic":"joins","q":"skus has a row where flag = 999; accounts has no matching row.\nWhat appears in b.kind for that skus row?\nSELECT a.flag, b.kind\nFROM skus a\nLEFT JOIN accounts b ON a.flag = b.flag;","options":["The row is excluded from the result","b.kind takes the column's default value","An error is raised for the unmatched key","NULL — unmatched left rows are kept with NULL on the right side"],"answer":3},{"id":930,"type":"practical","topic":"joins","q":"sessions has 6 rows; campaigns has 3 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["18 — Cartesian product (6 × 3)","6 — only sessions rows","9 — rows from both tables appended","3 — only matching-key rows"],"answer":0},{"id":931,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT name, COUNT(*) cnt\nFROM payments\nWHERE qty > 0\nGROUP BY name\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["SELECT","FROM","ORDER BY","WHERE"],"answer":1},{"id":932,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT price FROM subs)\nUNION ALL\n(SELECT price FROM visitors);","options":["Both copies are kept — UNION ALL never removes duplicates","Only one of the two queries executes","An error is raised for the duplicate row","The duplicate is removed — UNION ALL deduplicates"],"answer":0},{"id":933,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT kind,\n       ROW_NUMBER() OVER (PARTITION BY order_id) rn\nFROM tickets;","options":["Yes — partitions are always sorted by insertion order","Yes — ROW_NUMBER is always deterministic","Error — ORDER BY is required inside OVER with ROW_NUMBER","No — without ORDER BY inside OVER, order within each partition is undefined"],"answer":3},{"id":934,"type":"practical","topic":"window","q":"What is prev_code for the very first row in the ordering?\nSELECT amount,\n       code,\n       LAG(code) OVER (ORDER BY amount) AS prev_code\nFROM shipments;","options":["0 (zero default when there is no previous row)","The value of code for the last row in the table","NULL — no preceding row exists for the first row","An error is raised for the first row"],"answer":2},{"id":935,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.name = o.name AND 1 = 0\n);","options":["TRUE — EXISTS ignores the inner WHERE conditions","Error — subquery must always return at least one row","FALSE — EXISTS is false when the subquery returns no rows","NULL — three-valued logic makes this unknown"],"answer":2},{"id":936,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; user_id is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(user_id) FROM clicks;","options":["6, 6 — both functions count all rows","Error — you cannot mix COUNT(*) and COUNT(user_id)","6, 4 — COUNT(*) counts all rows; COUNT(user_id) skips NULLs","4, 4 — both functions skip NULLs"],"answer":2},{"id":937,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["TRUE","UNKNOWN (distinct from NULL in the standard)","NULL","FALSE — AND with FALSE is always FALSE regardless of the other operand"],"answer":3},{"id":938,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT amount FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE amount > 0\n)\nSELECT * FROM b;","options":["Yes — b can reference a defined earlier in the same WITH","No — WITH must contain exactly one named query","No — CTEs cannot reference each other","Only if both CTEs are declared RECURSIVE"],"answer":0},{"id":939,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on status?\nSELECT * FROM campaigns\nWHERE UPPER(status) = 'ACTIVE';","options":["Indexes do not work with string columns","UPPER() wraps status, preventing an index seek on the raw column","UPPER() always improves index efficiency","The optimizer always ignores scalar functions in WHERE"],"answer":1},{"id":940,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE created_at IS NOT NULL;","options":["B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","A can be rolled back; B cannot","B removes only non-NULL rows; A removes nothing","Both statements are identical in every database"],"answer":0},{"id":941,"type":"practical","topic":"joins","q":"visitors has 5 rows; flags has 2 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["5 — only visitors rows","0 — missing WHERE means no rows pass","7 — rows from both tables appended","10 — Cartesian product from the missing join condition"],"answer":3},{"id":942,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Yes — the whole table is one implicit group; returns one row if any rows exist","Error — HAVING requires a GROUP BY clause","Error — COUNT(*) cannot appear in both SELECT and HAVING","Returns every row of devices filtered by the count"],"answer":0},{"id":943,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(2, 2) — both functions agree after a tie","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(2, 3) — DENSE_RANK skips; RANK does not","(3, 3) — both leave a gap after a tie"],"answer":1},{"id":944,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT flag,\n       (SELECT kind FROM events) AS x\nFROM products;","options":["Runtime error — scalar subquery returned more than one row","Three copies of each products row are returned","The query auto-aggregates events to one row","The first row's kind value is silently used"],"answer":0},{"id":945,"type":"practical","topic":"indexes","q":"An index exists on (tier, code).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE code = 'active';","options":["Yes — composite indexes cover any subset of their columns equally","Yes — code is in the index so seeks always work","No — composite indexes never speed up equality predicates","No — filtering only code skips the leading column tier; leftmost-prefix rule not satisfied"],"answer":3},{"id":946,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT qty, name FROM invoices;","options":["4 — DISTINCT has no effect on two-column selects","2 — only unique values of the first column qty","3 — DISTINCT deduplicates the (qty, name) pair; (1,'a') appears once","1 — DISTINCT keeps only the row with the lowest qty"],"answer":2},{"id":947,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(price) FROM coupons\nWHERE 1 = 0;","options":["Zero rows returned","One row with MAX = 0","One row with MAX = NULL","Error — MAX on an empty set is undefined"],"answer":2},{"id":948,"type":"practical","topic":"joins","q":"Both tables have rows where kind IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.kind = b.kind;","options":["Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — but only with INNER JOIN, not OUTER JOIN","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Yes — NULLs are treated as equal in ON conditions"],"answer":2},{"id":949,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT code FROM metrics)\nEXCEPT\n(SELECT code FROM tickets);","options":["Values that appear in both metrics and tickets","All values from both metrics and tickets","Values in tickets that do not appear in metrics","Values in metrics that do not appear in tickets"],"answer":3},{"id":950,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT name, NTILE(4) OVER (ORDER BY status) bucket\nFROM flags;","options":["3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","4, 3, 2, 1 — strictly decreasing bucket sizes","2, 2, 3, 3 — smaller buckets come first","All 4 buckets have exactly 2 rows (remainder discarded)"],"answer":0},{"id":951,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE user_id = NULL;","options":["Same result as WHERE user_id IS NOT NULL","Zero rows — = NULL always evaluates to UNKNOWN","All rows where user_id is NULL","A syntax error at runtime"],"answer":1},{"id":952,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT order_id, region, COUNT(*)\nFROM users\nGROUP BY order_id;","options":["Error: region is in SELECT but not in GROUP BY","Error: COUNT(*) cannot appear with a GROUP BY","Error: GROUP BY must use an alias, not a column name","Valid — any column can appear alongside an aggregate"],"answer":0},{"id":953,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT amount, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY amount;","options":["Runs fine; WHERE filters rows before aggregation","WHERE and HAVING are interchangeable here","Returns groups with count > 2 correctly","Error — aggregate functions are not allowed in WHERE; use HAVING"],"answer":3},{"id":954,"type":"practical","topic":"joins","q":"skus has a row where status = 999; accounts has no matching row.\nWhat appears in b.score for that skus row?\nSELECT a.status, b.score\nFROM skus a\nLEFT JOIN accounts b ON a.status = b.status;","options":["NULL — unmatched left rows are kept with NULL on the right side","The row is excluded from the result","An error is raised for the unmatched key","b.score takes the column's default value"],"answer":0},{"id":955,"type":"practical","topic":"joins","q":"sessions has 7 rows; campaigns has 4 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["7 — only sessions rows","28 — Cartesian product (7 × 4)","4 — only matching-key rows","11 — rows from both tables appended"],"answer":1},{"id":956,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT tier, COUNT(*) cnt\nFROM payments\nWHERE region > 0\nGROUP BY tier\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["FROM","ORDER BY","WHERE","SELECT"],"answer":0},{"id":957,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT sku FROM subs)\nUNION ALL\n(SELECT sku FROM visitors);","options":["The duplicate is removed — UNION ALL deduplicates","Only one of the two queries executes","An error is raised for the duplicate row","Both copies are kept — UNION ALL never removes duplicates"],"answer":3},{"id":958,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT score,\n       ROW_NUMBER() OVER (PARTITION BY price) rn\nFROM tickets;","options":["Yes — ROW_NUMBER is always deterministic","Yes — partitions are always sorted by insertion order","No — without ORDER BY inside OVER, order within each partition is undefined","Error — ORDER BY is required inside OVER with ROW_NUMBER"],"answer":2},{"id":959,"type":"practical","topic":"window","q":"What is prev_flag for the very first row in the ordering?\nSELECT kind,\n       flag,\n       LAG(flag) OVER (ORDER BY kind) AS prev_flag\nFROM shipments;","options":["The value of flag for the last row in the table","An error is raised for the first row","NULL — no preceding row exists for the first row","0 (zero default when there is no previous row)"],"answer":2},{"id":960,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.tier = o.tier AND 1 = 0\n);","options":["NULL — three-valued logic makes this unknown","Error — subquery must always return at least one row","FALSE — EXISTS is false when the subquery returns no rows","TRUE — EXISTS ignores the inner WHERE conditions"],"answer":2},{"id":961,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; qty is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(qty) FROM clicks;","options":["4, 4 — both functions skip NULLs","6, 6 — both functions count all rows","Error — you cannot mix COUNT(*) and COUNT(qty)","6, 4 — COUNT(*) counts all rows; COUNT(qty) skips NULLs"],"answer":3},{"id":962,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["FALSE — AND with FALSE is always FALSE regardless of the other operand","TRUE","NULL","UNKNOWN (distinct from NULL in the standard)"],"answer":0},{"id":963,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT kind FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE kind > 0\n)\nSELECT * FROM b;","options":["Only if both CTEs are declared RECURSIVE","Yes — b can reference a defined earlier in the same WITH","No — CTEs cannot reference each other","No — WITH must contain exactly one named query"],"answer":1},{"id":964,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on code?\nSELECT * FROM campaigns\nWHERE UPPER(code) = 'ACTIVE';","options":["UPPER() wraps code, preventing an index seek on the raw column","The optimizer always ignores scalar functions in WHERE","Indexes do not work with string columns","UPPER() always improves index efficiency"],"answer":0},{"id":965,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE name IS NOT NULL;","options":["A can be rolled back; B cannot","B removes only non-NULL rows; A removes nothing","Both statements are identical in every database","B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging"],"answer":3},{"id":966,"type":"practical","topic":"joins","q":"visitors has 6 rows; flags has 3 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["18 — Cartesian product from the missing join condition","9 — rows from both tables appended","6 — only visitors rows","0 — missing WHERE means no rows pass"],"answer":0},{"id":967,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Error — HAVING requires a GROUP BY clause","Yes — the whole table is one implicit group; returns one row if any rows exist","Returns every row of devices filtered by the count","Error — COUNT(*) cannot appear in both SELECT and HAVING"],"answer":1},{"id":968,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2","(2, 3) — DENSE_RANK skips; RANK does not","(3, 3) — both leave a gap after a tie","(2, 2) — both functions agree after a tie"],"answer":0},{"id":969,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT status,\n       (SELECT score FROM events) AS x\nFROM products;","options":["The first row's score value is silently used","Three copies of each products row are returned","The query auto-aggregates events to one row","Runtime error — scalar subquery returned more than one row"],"answer":3},{"id":970,"type":"practical","topic":"indexes","q":"An index exists on (created_at, flag).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE flag = 'active';","options":["No — filtering only flag skips the leading column created_at; leftmost-prefix rule not satisfied","Yes — composite indexes cover any subset of their columns equally","Yes — flag is in the index so seeks always work","No — composite indexes never speed up equality predicates"],"answer":0},{"id":971,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT region, tier FROM invoices;","options":["2 — only unique values of the first column region","1 — DISTINCT keeps only the row with the lowest region","3 — DISTINCT deduplicates the (region, tier) pair; (1,'a') appears once","4 — DISTINCT has no effect on two-column selects"],"answer":2},{"id":972,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(sku) FROM coupons\nWHERE 1 = 0;","options":["Error — MAX on an empty set is undefined","One row with MAX = 0","One row with MAX = NULL","Zero rows returned"],"answer":2},{"id":973,"type":"practical","topic":"joins","q":"Both tables have rows where score IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.score = b.score;","options":["Yes — NULLs are treated as equal in ON conditions","Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — but only with INNER JOIN, not OUTER JOIN","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN"],"answer":3},{"id":974,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT flag FROM metrics)\nEXCEPT\n(SELECT flag FROM tickets);","options":["All values from both metrics and tickets","Values that appear in both metrics and tickets","Values in metrics that do not appear in tickets","Values in tickets that do not appear in metrics"],"answer":2},{"id":975,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT tier, NTILE(4) OVER (ORDER BY code) bucket\nFROM flags;","options":["All 4 buckets have exactly 2 rows (remainder discarded)","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","2, 2, 3, 3 — smaller buckets come first","4, 3, 2, 1 — strictly decreasing bucket sizes"],"answer":1},{"id":976,"type":"practical","topic":"nulls","q":"What does this query return?\nSELECT * FROM orders\nWHERE qty = NULL;","options":["Zero rows — = NULL always evaluates to UNKNOWN","A syntax error at runtime","Same result as WHERE qty IS NOT NULL","All rows where qty is NULL"],"answer":0},{"id":977,"type":"practical","topic":"aggregates","q":"Identify the problem:\nSELECT price, user_id, COUNT(*)\nFROM users\nGROUP BY price;","options":["Error: COUNT(*) cannot appear with a GROUP BY","Error: GROUP BY must use an alias, not a column name","Valid — any column can appear alongside an aggregate","Error: user_id is in SELECT but not in GROUP BY"],"answer":3},{"id":978,"type":"practical","topic":"aggregates","q":"What happens when you run this?\nSELECT kind, COUNT(*) n\nFROM events\nWHERE COUNT(*) > 2\nGROUP BY kind;","options":["Error — aggregate functions are not allowed in WHERE; use HAVING","Runs fine; WHERE filters rows before aggregation","Returns groups with count > 2 correctly","WHERE and HAVING are interchangeable here"],"answer":0},{"id":979,"type":"practical","topic":"joins","q":"skus has a row where code = 999; accounts has no matching row.\nWhat appears in b.amount for that skus row?\nSELECT a.code, b.amount\nFROM skus a\nLEFT JOIN accounts b ON a.code = b.code;","options":["The row is excluded from the result","NULL — unmatched left rows are kept with NULL on the right side","b.amount takes the column's default value","An error is raised for the unmatched key"],"answer":1},{"id":980,"type":"practical","topic":"joins","q":"sessions has 8 rows; campaigns has 5 rows.\nHow many rows does this return?\nSELECT * FROM sessions CROSS JOIN campaigns;","options":["40 — Cartesian product (8 × 5)","5 — only matching-key rows","13 — rows from both tables appended","8 — only sessions rows"],"answer":0},{"id":981,"type":"practical","topic":"basics","q":"Which clause is logically evaluated FIRST?\nSELECT created_at, COUNT(*) cnt\nFROM payments\nWHERE user_id > 0\nGROUP BY created_at\nHAVING cnt > 1\nORDER BY cnt DESC;","options":["SELECT","ORDER BY","WHERE","FROM"],"answer":3},{"id":982,"type":"practical","topic":"set_ops","q":"Both queries return the row (1, 'active').\nHow does UNION ALL handle the duplicate?\n(SELECT order_id FROM subs)\nUNION ALL\n(SELECT order_id FROM visitors);","options":["Both copies are kept — UNION ALL never removes duplicates","The duplicate is removed — UNION ALL deduplicates","An error is raised for the duplicate row","Only one of the two queries executes"],"answer":0},{"id":983,"type":"practical","topic":"window","q":"Is the row numbering deterministic?\nSELECT amount,\n       ROW_NUMBER() OVER (PARTITION BY sku) rn\nFROM tickets;","options":["Yes — partitions are always sorted by insertion order","No — without ORDER BY inside OVER, order within each partition is undefined","Error — ORDER BY is required inside OVER with ROW_NUMBER","Yes — ROW_NUMBER is always deterministic"],"answer":1},{"id":984,"type":"practical","topic":"window","q":"What is prev_status for the very first row in the ordering?\nSELECT score,\n       status,\n       LAG(status) OVER (ORDER BY score) AS prev_status\nFROM shipments;","options":["0 (zero default when there is no previous row)","An error is raised for the first row","NULL — no preceding row exists for the first row","The value of status for the last row in the table"],"answer":2},{"id":985,"type":"practical","topic":"subqueries","q":"The inner WHERE always filters out every row.\nWhat does EXISTS return for each outer row?\nSELECT * FROM refunds o\nWHERE EXISTS (\n  SELECT 1 FROM products i\n  WHERE i.created_at = o.created_at AND 1 = 0\n);","options":["TRUE — EXISTS ignores the inner WHERE conditions","NULL — three-valued logic makes this unknown","Error — subquery must always return at least one row","FALSE — EXISTS is false when the subquery returns no rows"],"answer":3},{"id":986,"type":"practical","topic":"aggregates","q":"clicks has 6 rows; region is NULL in 2 of them.\nWhat does this return?\nSELECT COUNT(*), COUNT(region) FROM clicks;","options":["6, 6 — both functions count all rows","4, 4 — both functions skip NULLs","6, 4 — COUNT(*) counts all rows; COUNT(region) skips NULLs","Error — you cannot mix COUNT(*) and COUNT(region)"],"answer":2},{"id":987,"type":"practical","topic":"nulls","q":"In SQL three-valued logic, what does NULL AND FALSE evaluate to?","options":["NULL","UNKNOWN (distinct from NULL in the standard)","FALSE — AND with FALSE is always FALSE regardless of the other operand","TRUE"],"answer":2},{"id":988,"type":"practical","topic":"cte","q":"Does this query run without error?\nWITH a AS (\n  SELECT score FROM accounts\n),\nb AS (\n  SELECT * FROM a WHERE score > 0\n)\nSELECT * FROM b;","options":["Yes — b can reference a defined earlier in the same WITH","No — WITH must contain exactly one named query","Only if both CTEs are declared RECURSIVE","No — CTEs cannot reference each other"],"answer":0},{"id":989,"type":"practical","topic":"optimization","q":"Why might this query NOT use an index on flag?\nSELECT * FROM campaigns\nWHERE UPPER(flag) = 'ACTIVE';","options":["The optimizer always ignores scalar functions in WHERE","Indexes do not work with string columns","UPPER() always improves index efficiency","UPPER() wraps flag, preventing an index seek on the raw column"],"answer":3},{"id":990,"type":"practical","topic":"dml_ddl","q":"What is the key behavioural difference?\nA: TRUNCATE TABLE leads;\nB: DELETE FROM leads WHERE tier IS NOT NULL;","options":["B is row-by-row with a predicate and fully logged; A removes all rows with minimal logging","A can be rolled back; B cannot","Both statements are identical in every database","B removes only non-NULL rows; A removes nothing"],"answer":0},{"id":991,"type":"practical","topic":"joins","q":"visitors has 7 rows; flags has 4 rows.\nThe ON / WHERE was accidentally omitted.\nHow many rows does this return?\nSELECT * FROM visitors a, flags b;","options":["0 — missing WHERE means no rows pass","28 — Cartesian product from the missing join condition","7 — only visitors rows","11 — rows from both tables appended"],"answer":1},{"id":992,"type":"practical","topic":"aggregates","q":"Does this run, and what does it return?\nSELECT COUNT(*) cnt\nFROM devices\nHAVING COUNT(*) > 0;","options":["Yes — the whole table is one implicit group; returns one row if any rows exist","Returns every row of devices filtered by the count","Error — COUNT(*) cannot appear in both SELECT and HAVING","Error — HAVING requires a GROUP BY clause"],"answer":0},{"id":993,"type":"practical","topic":"window","q":"Scores: 90, 90, 80, 70 ordered DESC.\nWhat is (RANK, DENSE_RANK) for the row with score = 80?\nSELECT score,\n       RANK()       OVER (ORDER BY score DESC) r,\n       DENSE_RANK() OVER (ORDER BY score DESC) dr\nFROM regions;","options":["(2, 2) — both functions agree after a tie","(2, 3) — DENSE_RANK skips; RANK does not","(3, 3) — both leave a gap after a tie","(3, 2) — two rows tie at 1; RANK skips to 3, DENSE_RANK goes to 2"],"answer":3},{"id":994,"type":"practical","topic":"subqueries","q":"events has 3 rows. What happens when this runs?\nSELECT code,\n       (SELECT amount FROM events) AS x\nFROM products;","options":["Runtime error — scalar subquery returned more than one row","The first row's amount value is silently used","The query auto-aggregates events to one row","Three copies of each products row are returned"],"answer":0},{"id":995,"type":"practical","topic":"indexes","q":"An index exists on (name, status).\nCan this query use that index for a seek?\nSELECT * FROM teams\nWHERE status = 'active';","options":["Yes — composite indexes cover any subset of their columns equally","No — filtering only status skips the leading column name; leftmost-prefix rule not satisfied","No — composite indexes never speed up equality predicates","Yes — status is in the index so seeks always work"],"answer":1},{"id":996,"type":"practical","topic":"basics","q":"How many distinct rows are returned?\n-- invoices contains: (1,'a'), (1,'b'), (1,'a'), (2,'a')\nSELECT DISTINCT user_id, created_at FROM invoices;","options":["3 — DISTINCT deduplicates the (user_id, created_at) pair; (1,'a') appears once","1 — DISTINCT keeps only the row with the lowest user_id","4 — DISTINCT has no effect on two-column selects","2 — only unique values of the first column user_id"],"answer":0},{"id":997,"type":"practical","topic":"aggregates","q":"No rows match the filter. What does MAX return?\nSELECT MAX(order_id) FROM coupons\nWHERE 1 = 0;","options":["Zero rows returned","Error — MAX on an empty set is undefined","One row with MAX = 0","One row with MAX = NULL"],"answer":3},{"id":998,"type":"practical","topic":"joins","q":"Both tables have rows where amount IS NULL.\nDo those NULL rows join with each other?\nSELECT *\nFROM widgets a\nJOIN subs b ON a.amount = b.amount;","options":["Only if both sides are NULL simultaneously (special NULL-safe rule)","Yes — NULLs are treated as equal in ON conditions","No — NULL = NULL is UNKNOWN, not TRUE; NULLs never match in a JOIN","Yes — but only with INNER JOIN, not OUTER JOIN"],"answer":2},{"id":999,"type":"practical","topic":"set_ops","q":"What rows does this return?\n(SELECT status FROM metrics)\nEXCEPT\n(SELECT status FROM tickets);","options":["Values that appear in both metrics and tickets","Values in tickets that do not appear in metrics","Values in metrics that do not appear in tickets","All values from both metrics and tickets"],"answer":2},{"id":1000,"type":"practical","topic":"window","q":"flags has 10 rows. What are the bucket sizes?\nSELECT created_at, NTILE(4) OVER (ORDER BY flag) bucket\nFROM flags;","options":["4, 3, 2, 1 — strictly decreasing bucket sizes","All 4 buckets have exactly 2 rows (remainder discarded)","3, 3, 2, 2 — rows distributed as evenly as possible; larger buckets first","2, 2, 3, 3 — smaller buckets come first"],"answer":2}]}