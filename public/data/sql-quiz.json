{"version":2,"topicLabels":{"basics":"SQL fundamentals & clauses","joins":"JOINs & relational combinations","aggregates":"GROUP BY, aggregates & HAVING","subqueries":"Subqueries & EXISTS / IN","window":"Window functions & OVER","cte":"CTEs & WITH","optimization":"Query tuning & patterns","indexes":"Indexes & access paths","nulls":"NULL handling & functions","dml_ddl":"DML / DDL basics","set_ops":"UNION / INTERSECT / EXCEPT"},"questions":[{"id":1,"topic":"joins","q":"In standard SQL, what does INNER JOIN typically produce?","options":["Every row from the left is kept and non-matching right-side columns are filled with NULL","Every row from the right is kept and non-matching left-side columns are filled with NULL","All combinations of left and right rows appear without evaluating a join condition","Only rows that satisfy the join predicate on both sides appear in the result set"],"answer":3},{"id":2,"topic":"joins","q":"In standard SQL, what does LEFT JOIN typically produce?","options":["All right-hand rows are returned; left columns become NULL when no match exists","Only pairs of rows that match the join condition on both sides are returned once NULL handling, collation, and three-valued logic are taken into account","All left-hand rows are returned; matching right rows attach, else right columns are NULL","The result is the Cartesian product of the two inputs with duplicate keys removed"],"answer":2},{"id":3,"topic":"joins","q":"In standard SQL, what does RIGHT JOIN typically produce?","options":["Only pairs of rows that satisfy the predicate on both sides are returned as documented in the SQL standard, aside from vendor-specific extensions","Rows are paired strictly by primary-key equality with no NULL padding allowed","All right-hand rows are returned; matching left rows attach, else left columns are NULL","All left-hand rows are returned; right columns become NULL when no match exists"],"answer":2},{"id":4,"topic":"joins","q":"In standard SQL, what does FULL OUTER JOIN typically produce?","options":["Rows from either side appear; unmatched columns from the other side are padded with NULL","Only non-NULL keys from both tables participate; the rest are discarded first","Every left row is paired with every right row before duplicate removal when the optimizer sees selective predicates and proper key statistics","Only rows that match on both sides are kept, identical to an inner join"],"answer":0},{"id":5,"topic":"joins","q":"In standard SQL, what does CROSS JOIN typically produce?","options":["Duplicate keys are collapsed automatically before any pairing occurs","The join requires a matching foreign-key definition between the two tables","Only rows satisfying an ON or USING clause are combined from both inputs","Each left row is paired with every right row, producing the Cartesian product"],"answer":3},{"id":6,"topic":"aggregates","q":"What does COUNT(*) compute in a grouped query?","options":["Counts every row in the group, including those where individual columns are NULL","Returns the number of distinct physical pages touched during the scan for most analytical queries, though dialect quirks should always be verified","Counts only non-NULL values in the argument column, ignoring NULL rows entirely","Requires an ORDER BY clause in the same SELECT list to produce a defined count"],"answer":0},{"id":7,"topic":"aggregates","q":"What does COUNT(column) compute in a grouped query?","options":["Can only be applied to columns that participate in the PRIMARY KEY","Counts non-NULL values in that column for each group","Counts all rows including those where that column is NULL, same as COUNT(*)","Returns the maximum numeric magnitude found in that column per group"],"answer":1},{"id":8,"topic":"aggregates","q":"What does SUM(column) compute in a grouped query?","options":["Sums non-NULL numeric values in that column within each group","Ignores the GROUP BY clause and aggregates the entire table in one value","Always rounds fractional results to the nearest integer before returning","Concatenates string representations of the column across the group in SQL standard"],"answer":0},{"id":9,"topic":"aggregates","q":"What does AVG(column) compute in a grouped query?","options":["Treats NULL as zero for every skipped cell when forming the average","Is defined only for INTEGER columns and raises an error for DECIMAL types","Returns the middle element after sorting the group (the statistical median)","Computes the mean of non-NULL values in that column for each group"],"answer":3},{"id":10,"topic":"aggregates","q":"What does MAX(column) compute in a grouped query?","options":["Returns the greatest value in the group according to the data type ordering rules","Always returns the first physical row encountered in storage order","Returns the smallest value when the column is indexed in descending order","Is restricted to DATE and TIMESTAMP columns in the SQL standard in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":0},{"id":11,"topic":"aggregates","q":"What does MIN(column) compute in a grouped query?","options":["Returns the largest value whenever ORDER BY DESC appears elsewhere in the query","Returns the smallest value in the group according to the data type ordering rules","Cannot appear in the same SELECT list as MAX on another column once NULL handling, collation, and three-valued logic are taken into account","Skips character columns and only considers numeric fields in the group"],"answer":1},{"id":12,"topic":"window","q":"In SQL window functions, what is ROW_NUMBER() used for?","options":["Assigns a unique integer to each row in the partition in deterministic order","Replaces PARTITION BY and must be used without an OVER clause as documented in the SQL standard, aside from vendor-specific extensions","Numbers rows starting at zero and resets at every physical page boundary","Assigns the same integer to every row that ties on the ORDER BY keys"],"answer":0},{"id":13,"topic":"window","q":"In SQL window functions, what is RANK() used for?","options":["Assigns dense consecutive integers with no gaps after ties in the partition","Requires exactly two PARTITION BY columns to behave deterministically","Is identical to ROW_NUMBER for every input regardless of ties","Assigns ranks with gaps after tied groups share the same rank value"],"answer":3},{"id":14,"topic":"window","q":"In SQL window functions, what is DENSE_RANK() used for?","options":["Orders the partition implicitly by primary key when ORDER BY is omitted","Always inserts a gap equal to the tie count after each tied block","Assigns consecutive rank values without leaving gaps after tied groups","Counts only NULL values when ranking non-NULL business metrics"],"answer":2},{"id":15,"topic":"window","q":"In SQL window functions, what is LAG(expr) used for?","options":["Returns the value from the next row ahead in the window ordering","Is only valid inside a GROUP BY aggregate list, not inside OVER","Returns the value of expr from a prior row in the window ordering","Accumulates a running sum of expr over all prior rows in the partition"],"answer":2},{"id":16,"topic":"window","q":"In SQL window functions, what is LEAD(expr) used for?","options":["Computes the difference between MAX and MIN of expr in the frame","Cannot be combined with ORDER BY inside the same OVER specification","Returns the value of expr from a following row in the window ordering","Returns the value from the previous row behind in the window ordering"],"answer":2},{"id":17,"topic":"window","q":"In SQL window functions, what is NTILE(n) used for?","options":["Counts NULL buckets separately and excludes them from the bucket total","Is equivalent to FLOOR(row_number / n) with no ordering requirement","Returns a fractional percentile between zero and one for expr once NULL handling, collation, and three-valued logic are taken into account","Divides each partition into up to n buckets with nearly equal row counts"],"answer":3},{"id":18,"topic":"basics","q":"In relational SQL, which statement best describes Primary key?","options":["Uniquely identifies each row and enforces non-NULL for those columns in the table","Must always be stored as a variable-length character string type in typical OLTP and warehouse engines when standard SQL rules apply","Automatically speeds up every query in the database without additional indexes","Allows unlimited duplicate NULL markers across rows in standard SQL"],"answer":0},{"id":19,"topic":"basics","q":"In relational SQL, which statement best describes Foreign key?","options":["Forbids NULL in the referencing columns in every SQL implementation when the optimizer sees selective predicates and proper key statistics","Enforces that values reference existing keys in the parent table (or NULL where allowed)","Creates a clustered index on the child table without an explicit CREATE INDEX","Is interchangeable with PRIMARY KEY constraints on the same column list"],"answer":1},{"id":20,"topic":"basics","q":"In relational SQL, which statement best describes SELECT?","options":["Projects columns and expressions from tables or subqueries into a result relation","Creates a new persistent table structure in the current schema as documented in the SQL standard, aside from vendor-specific extensions","Commits the current transaction and releases all row locks held when the optimizer sees selective predicates and proper key statistics","Removes rows from base tables according to the WHERE predicate in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":0},{"id":21,"topic":"basics","q":"In relational SQL, which statement best describes WHERE?","options":["Filters grouped aggregates after GROUP BY similar to a post-aggregate gate","Specifies only how two tables are related in a join and nothing else","Defines the sort order for the final result set before LIMIT is evaluated","Filters individual rows before grouping based on predicates on row values"],"answer":3},{"id":22,"topic":"basics","q":"In relational SQL, which statement best describes HAVING?","options":["Restricts grouped result rows after aggregates have been computed","Restricts base-table rows before any GROUP BY bucketing takes place","Replaces the SELECT list when only aggregates are needed in output","Creates or rebuilds indexes referenced in the GROUP BY column list"],"answer":0},{"id":23,"topic":"basics","q":"In relational SQL, which statement best describes GROUP BY?","options":["Sorts the entire result globally without changing aggregate semantics","Forms buckets so aggregate functions are evaluated once per distinct bucket","Defines how two relations are combined using equality predicates only","Removes duplicate rows from the final output in place of DISTINCT in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":1},{"id":24,"topic":"basics","q":"In relational SQL, which statement best describes ORDER BY?","options":["Determines the sequence of rows in the cursor or final result set","Is logically evaluated before WHERE filters in the processing pipeline","Defines window partitions independently of the frame clause","Removes NULL values from the result before returning rows to the client"],"answer":0},{"id":25,"topic":"basics","q":"In relational SQL, which statement best describes DISTINCT?","options":["Sorts ascending on the first column and descending on all remaining columns","Creates a unique index on the underlying base table automatically","Applies only when exactly one column appears in the SELECT list","Eliminates duplicate result rows after SELECT expressions are evaluated"],"answer":3},{"id":26,"topic":"basics","q":"In relational SQL, which statement best describes LIMIT / OFFSET?","options":["Substitutes for HAVING when working with grouped aggregate queries","Defines the sliding frame for window functions inside OVER clauses","Caps row count and optionally skips a leading prefix of the ordered result","Is supported only on Oracle Database and not in portable SQL text once NULL handling, collation, and three-valued logic are taken into account"],"answer":2},{"id":27,"topic":"basics","q":"In relational SQL, which statement best describes PRIMARY KEY vs UNIQUE?","options":["The two constraints are identical for every column list in the standard","PRIMARY KEY permits multiple NULLs in the key columns across different rows","Primary key columns are NOT NULL and uniquely identify rows; UNIQUE allows NULLs per rules","UNIQUE always implies PRIMARY KEY when declared on a single column when the optimizer sees selective predicates and proper key statistics"],"answer":2},{"id":28,"topic":"nulls","q":"In SQL, what is correct about NULL = NULL?","options":["Evaluates to FALSE and therefore excludes the row from every predicate","Is required to be TRUE so that outer joins can pad unmatched rows for most analytical queries, though dialect quirks should always be verified","Evaluates to UNKNOWN in SQL three-valued logic; use IS NULL / IS NOT NULL instead","Evaluates to TRUE whenever both operands are NULL in the WHERE clause"],"answer":2},{"id":29,"topic":"nulls","q":"In SQL, what is correct about NULL in arithmetic?","options":["Is coerced to zero for addition and to one for multiplication in the standard","Is coerced to one for addition and to zero for division consistently once NULL handling, collation, and three-valued logic are taken into account","Raises a runtime exception in every major database for basic arithmetic","Typically propagates NULL through expressions unless functions like COALESCE wrap it"],"answer":3},{"id":30,"topic":"nulls","q":"In SQL, what is correct about COALESCE(a,b)?","options":["Returns the first argument that is not NULL from left to right","Always returns the rightmost argument regardless of NULL status","Returns the arithmetic mean of all non-NULL arguments provided","Concatenates all arguments into a single VARCHAR result value"],"answer":0},{"id":31,"topic":"nulls","q":"In SQL, what is correct about NULLIF(a,b)?","options":["Casts both arguments to BOOLEAN before comparison in all dialects","Returns NULL when a and b compare equal; otherwise returns a","Always returns b when either argument is NULL in the expression","Behaves identically to COALESCE with the arguments reversed"],"answer":1},{"id":32,"topic":"set_ops","q":"What does UNION do between two compatible SELECT statements?","options":["Stacks query results and removes duplicate rows unless UNION ALL is specified","Requires result column names to match exactly or the operation fails","Sorts the combined result globally before duplicates are considered as documented in the SQL standard, aside from vendor-specific extensions","Stacks query results and retains all duplicate rows by default in the standard"],"answer":0},{"id":33,"topic":"set_ops","q":"What does UNION ALL do between two compatible SELECT statements?","options":["Allows only two columns total in each participating SELECT list","Is always slower than an INNER JOIN between the same two row sets","Removes duplicates exactly like UNION without an ALL keyword","Concatenates result sets preserving all rows including duplicates"],"answer":3},{"id":34,"topic":"set_ops","q":"What does INTERSECT do between two compatible SELECT statements?","options":["Returns rows that appear in both SELECT results where the dialect implements it","Returns every row from the first SELECT regardless of the second query","Subtracts the second result from the first, similar to EXCEPT semantics","Is an alias for FULL OUTER JOIN when column counts already match when the optimizer sees selective predicates and proper key statistics"],"answer":0},{"id":35,"topic":"set_ops","q":"What does EXCEPT / MINUS do between two compatible SELECT statements?","options":["Returns the union of both queries with duplicates removed automatically","Returns rows from the first query not present in the second (per dialect rules)","Returns the Cartesian product restricted by the WHERE predicate only in typical OLTP and warehouse engines when standard SQL rules apply","Returns only rows that appear in both queries on the same key columns"],"answer":1},{"id":36,"topic":"indexes","q":"Which description fits a B-tree index?","options":["Supports point lookups and range scans efficiently on ordered key values","Cannot be declared UNIQUE under any circumstances in SQL engines","Stores exclusively NULL markers and cannot reference non-NULL keys","Forces the optimizer to read every table page end-to-end sequentially"],"answer":0},{"id":37,"topic":"indexes","q":"Which description fits a Covering index?","options":["Always increases write amplification without benefiting read latency","Applies only to VARCHAR columns longer than 256 characters as documented in the SQL standard, aside from vendor-specific extensions","Prevents any WHERE clause predicates from referencing indexed columns","Contains all columns needed so the engine can answer the query from the index alone"],"answer":3},{"id":38,"topic":"indexes","q":"Which description fits a Composite index (a,b)?","options":["Makes only the second column b searchable for equality predicates in typical OLTP and warehouse engines when standard SQL rules apply","Can be used for seeks on b alone with the same efficiency as on a alone","Can be used for seeks on a alone or on (a,b) following leftmost-prefix rules","Behaves exactly like two completely independent single-column indexes always"],"answer":2},{"id":39,"topic":"optimization","q":"What is a good characterization of SELECT *?","options":["Is always the fastest way to retrieve rows because the star avoids planning cost","Guarantees a higher buffer-pool hit rate than projecting explicit columns","Often widens I/O and couples queries to schema changes; listing columns is usually safer","Is required for the optimizer to choose any index-backed access path once NULL handling, collation, and three-valued logic are taken into account"],"answer":2},{"id":40,"topic":"optimization","q":"What is a good characterization of N+1 query pattern?","options":["Is how columnar warehouses expect you to load dimension attributes","Is automatically removed from the plan when DISTINCT is present","Issues many round-trips; often replaced with JOINs or batched IN lists","Is the recommended pattern for large OLAP scans on fact tables"],"answer":2},{"id":41,"topic":"optimization","q":"What is a good characterization of Predicate sargability?","options":["Encourages applying functions to indexed columns inside WHERE freely","Discourages using WHERE altogether in favor of HAVING on raw rows for most analytical queries, though dialect quirks should always be verified","Applies only to document databases and not to relational SQL engines","Means predicates are written so indexes can be used without wrapping indexed columns"],"answer":3},{"id":42,"topic":"optimization","q":"What is a good characterization of EXPLAIN?","options":["Shows how the planner intends to execute or executed a statement","Rebuilds only table statistics in MySQL and has no meaning elsewhere","Commits the surrounding transaction and persists pending changes","Drops secondary indexes that are not used in the current session"],"answer":0},{"id":43,"topic":"cte","q":"What is true about a WITH clause (CTE) in SQL?","options":["Is evaluated strictly after ORDER BY in the outer query finishes sorting","Introduces named subqueries that can be referenced later in the same statement","Materializes a permanent base table visible to all future sessions as documented in the SQL standard, aside from vendor-specific extensions","Cannot reference another CTE defined earlier in the same WITH list"],"answer":1},{"id":44,"topic":"cte","q":"What is true about a Recursive CTE in SQL?","options":["Iterates from an anchor part to a fixed point for hierarchies and graphs","Always runs forever until the DBA cancels the session manually once NULL handling, collation, and three-valued logic are taken into account","Is exactly equivalent to a global temporary table with no recursion","Cannot combine anchor and recursive parts using UNION ALL syntax"],"answer":0},{"id":45,"topic":"subqueries","q":"Which statement about Correlated subquery is most accurate?","options":["Is always more efficient than an equivalent JOIN for large fact tables","Cannot legally appear inside a WHERE clause in standard SQL grammar","Never references columns from an outer query block by SQL definition","References outer-query columns and is re-evaluated in the outer row context"],"answer":3},{"id":46,"topic":"subqueries","q":"Which statement about EXISTS predicate is most accurate?","options":["Is true when the subquery returns at least one row and stops early when possible","Is always equivalent to IN with a NULL-safe comparison on all columns","Counts how many rows the subquery would return if fully materialized","Requires the subquery SELECT list to contain an aggregate function once NULL handling, collation, and three-valued logic are taken into account"],"answer":0},{"id":47,"topic":"subqueries","q":"Which statement about IN (subquery) is most accurate?","options":["Always correlates to the outer query on every column in the SELECT list","Holds when the left value equals any row returned by the subquery","Is defined to mean the same as an INNER JOIN for every possible query shape","Handles NULL comparisons safely without UNKNOWN outcomes in all cases"],"answer":1},{"id":48,"topic":"dml_ddl","q":"What does INSERT do in SQL?","options":["Adds new rows into an existing table or partition according to provided values","Renames columns in place without rewriting any existing stored rows for most analytical queries, though dialect quirks should always be verified","Alters column data types and constraints on the table in one statement","Removes rows that match a predicate from the target table permanently"],"answer":0},{"id":49,"topic":"dml_ddl","q":"What does UPDATE do in SQL?","options":["Adds new nullable columns to the table definition for every matched row","Requires a JOIN to every referenced table even for single-table updates","Drops the entire database if the WHERE clause matches zero rows once NULL handling, collation, and three-valued logic are taken into account","Changes column values for rows that satisfy the optional WHERE condition"],"answer":3},{"id":50,"topic":"dml_ddl","q":"What does DELETE do in SQL?","options":["Removes rows that match the predicate while leaving table structure intact","Always acquires identical locks and logs identical volume as TRUNCATE","Cannot include a WHERE clause when referential integrity is enabled","Sets default values on columns instead of removing matching rows in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":0},{"id":51,"topic":"dml_ddl","q":"What does TRUNCATE do in SQL?","options":["Logs each removed row individually exactly like DELETE for all engines","Drops the table object and all dependent views in the same command","Quickly removes all rows from a table with dialect-specific logging and lock rules","Removes at most one row unless a TOP or LIMIT modifier is supplied for most analytical queries, though dialect quirks should always be verified"],"answer":2},{"id":52,"topic":"optimization","q":"Tuning drill #52: Why might OR across different indexed columns hurt performance?","options":["OR is ignored by optimizers so indexes are still used exactly like AND when the optimizer sees selective predicates and proper key statistics","OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR predicates are always merged into a single perfect index intersection plan"],"answer":2},{"id":53,"topic":"indexes","q":"Index drill #53: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","They apply only to full table scans and not to selective predicates in typical OLTP and warehouse engines when standard SQL rules apply","They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly"],"answer":3},{"id":54,"topic":"nulls","q":"NULL drill #54: Result of TRUE AND NULL in three-valued logic?","options":["FALSE, which removes the row from the result before aggregates are computed","TRUE, so the row always satisfies conjunctions involving NULL comparisons","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","NULL is forbidden from appearing in boolean expressions in standard SQL"],"answer":2},{"id":55,"topic":"dml_ddl","q":"DDL drill #55: What does CREATE VIEW typically store?","options":["Only the transaction log identifiers for the last bulk load operation in typical OLTP and warehouse engines when standard SQL rules apply","The text of a query definition; stored rows remain in the underlying base tables","Physical copies of all result rows refreshed only when the session ends","Index entries alone with no reference back to the defining SELECT text"],"answer":1},{"id":56,"topic":"set_ops","q":"Set drill #56: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Use identical column aliases in the same order or the statement is rejected"],"answer":0},{"id":57,"topic":"basics","q":"Concept drill #57: Which best describes a relational \"relation\" in SQL theory?","options":["A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples as documented in the SQL standard, aside from vendor-specific extensions","A single physical heap file on disk with no notion of column names or types","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)"],"answer":3},{"id":58,"topic":"joins","q":"Join drill #58: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It requires a USING list for every join and fails if any column shares a name","It refuses to match on column names and instead hashes entire row images only","It forbids referencing more than one table in the same FROM clause entirely"],"answer":0},{"id":59,"topic":"aggregates","q":"Aggregate drill #59: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","They must be grouped or functionally dependent on the grouped keys per SQL rules","HAVING replaces the entire SELECT list and hides non-aggregated columns","Only COUNT(*) may appear in the SELECT list when GROUP BY is present once NULL handling, collation, and three-valued logic are taken into account"],"answer":1},{"id":60,"topic":"window","q":"Window drill #60: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Creates a durable partitioned table object stored on disk for reuse in typical OLTP and warehouse engines when standard SQL rules apply","Eliminates the need for GROUP BY by collapsing all rows into one partition","Sorts every table in the database globally before any WHERE is evaluated"],"answer":0},{"id":61,"topic":"subqueries","q":"Subquery drill #61: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","Only aggregate functions without a GROUP BY inside the subquery body","Exactly two columns that the outer query can unpack into local variables","At most one row and one column of values for each outer row being processed"],"answer":3},{"id":62,"topic":"cte","q":"CTE drill #62: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","No — the standard allows only a single CTE name per SQL statement","Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present"],"answer":0},{"id":63,"topic":"optimization","q":"Tuning drill #63: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR forces a global sort of the entire table before any filter is applied","OR is ignored by optimizers so indexes are still used exactly like AND in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":1},{"id":64,"topic":"indexes","q":"Index drill #64: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates once NULL handling, collation, and three-valued logic are taken into account","They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are never useful and should be avoided in production schema design"],"answer":2},{"id":65,"topic":"nulls","q":"NULL drill #65: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE"],"answer":3},{"id":66,"topic":"dml_ddl","q":"DDL drill #66: What does CREATE VIEW typically store?","options":["Index entries alone with no reference back to the defining SELECT text","Physical copies of all result rows refreshed only when the session ends","The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation once NULL handling, collation, and three-valued logic are taken into account"],"answer":2},{"id":67,"topic":"set_ops","q":"Set drill #67: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match","Contain exactly one projected expression per SELECT in the operation"],"answer":2},{"id":68,"topic":"basics","q":"Concept drill #68: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples for most analytical queries, though dialect quirks should always be verified","A single physical heap file on disk with no notion of column names or types"],"answer":0},{"id":69,"topic":"joins","q":"Join drill #69: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It refuses to match on column names and instead hashes entire row images only","It equates all same-named columns implicitly, which may not be the intended join keys"],"answer":3},{"id":70,"topic":"aggregates","q":"Aggregate drill #70: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","Only COUNT(*) may appear in the SELECT list when GROUP BY is present as documented in the SQL standard, aside from vendor-specific extensions","GROUP BY forbids returning any base columns alongside aggregate expressions","HAVING replaces the entire SELECT list and hides non-aggregated columns"],"answer":0},{"id":71,"topic":"window","q":"Window drill #71: What does PARTITION BY in OVER() do?","options":["Creates a durable partitioned table object stored on disk for reuse once NULL handling, collation, and three-valued logic are taken into account","Splits the row stream so the window function restarts separately in each partition","Sorts every table in the database globally before any WHERE is evaluated","Eliminates the need for GROUP BY by collapsing all rows into one partition"],"answer":1},{"id":72,"topic":"subqueries","q":"Subquery drill #72: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Only aggregate functions without a GROUP BY inside the subquery body","Exactly two columns that the outer query can unpack into local variables","Any number of rows as long as the subquery text fits in a single line"],"answer":0},{"id":73,"topic":"cte","q":"CTE drill #73: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Only when the CTE is declared recursive with an ANCHOR keyword present","Only on very old MySQL releases before common table expressions existed","Yes — later CTEs may reference earlier names in the same WITH clause list"],"answer":3},{"id":74,"topic":"optimization","q":"Tuning drill #74: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR predicates are always merged into a single perfect index intersection plan","OR is ignored by optimizers so indexes are still used exactly like AND once NULL handling, collation, and three-valued logic are taken into account","OR forces a global sort of the entire table before any filter is applied"],"answer":0},{"id":75,"topic":"indexes","q":"Index drill #75: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are legal only on PRIMARY KEY columns and not on secondary keys","They apply only to full table scans and not to selective predicates as documented in the SQL standard, aside from vendor-specific extensions"],"answer":1},{"id":76,"topic":"nulls","q":"NULL drill #76: Result of TRUE AND NULL in three-valued logic?","options":["FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","TRUE, so the row always satisfies conjunctions involving NULL comparisons"],"answer":2},{"id":77,"topic":"dml_ddl","q":"DDL drill #77: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation as documented in the SQL standard, aside from vendor-specific extensions","The text of a query definition; stored rows remain in the underlying base tables"],"answer":3},{"id":78,"topic":"set_ops","q":"Set drill #78: For UNION, column lists must?","options":["Contain exactly one projected expression per SELECT in the operation","Use identical column aliases in the same order or the statement is rejected","Be compatible in arity and types; corresponding names do not have to match","Match primary-key definitions between the two sides before UNION is legal"],"answer":2},{"id":79,"topic":"basics","q":"Concept drill #79: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","Exactly one scalar column that holds nested documents instead of tuples when the optimizer sees selective predicates and proper key statistics","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A list that is always sorted by the primary key values for convenient retrieval"],"answer":2},{"id":80,"topic":"joins","q":"Join drill #80: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys","It refuses to match on column names and instead hashes entire row images only"],"answer":2},{"id":81,"topic":"aggregates","q":"Aggregate drill #81: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["Only COUNT(*) may appear in the SELECT list when GROUP BY is present for most analytical queries, though dialect quirks should always be verified","HAVING replaces the entire SELECT list and hides non-aggregated columns","GROUP BY forbids returning any base columns alongside aggregate expressions","They must be grouped or functionally dependent on the grouped keys per SQL rules"],"answer":3},{"id":82,"topic":"window","q":"Window drill #82: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Eliminates the need for GROUP BY by collapsing all rows into one partition","Sorts every table in the database globally before any WHERE is evaluated","Creates a durable partitioned table object stored on disk for reuse as documented in the SQL standard, aside from vendor-specific extensions"],"answer":0},{"id":83,"topic":"subqueries","q":"Subquery drill #83: Scalar subquery in SELECT must return?","options":["Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed","Any number of rows as long as the subquery text fits in a single line","Exactly two columns that the outer query can unpack into local variables"],"answer":1},{"id":84,"topic":"cte","q":"CTE drill #84: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","No — the standard allows only a single CTE name per SQL statement"],"answer":0},{"id":85,"topic":"optimization","q":"Tuning drill #85: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","OR forces a global sort of the entire table before any filter is applied","OR is ignored by optimizers so indexes are still used exactly like AND as documented in the SQL standard, aside from vendor-specific extensions","The planner may fail to union index-driven paths; rewriting with UNION ALL can help"],"answer":3},{"id":86,"topic":"indexes","q":"Index drill #86: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are never useful and should be avoided in production schema design","They apply only to full table scans and not to selective predicates for most analytical queries, though dialect quirks should always be verified","They are legal only on PRIMARY KEY columns and not on secondary keys"],"answer":0},{"id":87,"topic":"nulls","q":"NULL drill #87: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","NULL is forbidden from appearing in boolean expressions in standard SQL","FALSE, which removes the row from the result before aggregates are computed"],"answer":1},{"id":88,"topic":"dml_ddl","q":"DDL drill #88: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation for most analytical queries, though dialect quirks should always be verified","Index entries alone with no reference back to the defining SELECT text","Physical copies of all result rows refreshed only when the session ends"],"answer":0},{"id":89,"topic":"set_ops","q":"Set drill #89: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match"],"answer":3},{"id":90,"topic":"basics","q":"Concept drill #90: Which best describes a relational \"relation\" in SQL theory?","options":["A list that is always sorted by the primary key values for convenient retrieval","A single physical heap file on disk with no notion of column names or types","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","Exactly one scalar column that holds nested documents instead of tuples in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":2},{"id":91,"topic":"joins","q":"Join drill #91: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys","It requires a USING list for every join and fails if any column shares a name"],"answer":2},{"id":92,"topic":"aggregates","q":"Aggregate drill #92: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["Only COUNT(*) may appear in the SELECT list when GROUP BY is present when the optimizer sees selective predicates and proper key statistics","HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","GROUP BY forbids returning any base columns alongside aggregate expressions"],"answer":2},{"id":93,"topic":"window","q":"Window drill #93: What does PARTITION BY in OVER() do?","options":["Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse for most analytical queries, though dialect quirks should always be verified","Sorts every table in the database globally before any WHERE is evaluated","Splits the row stream so the window function restarts separately in each partition"],"answer":3},{"id":94,"topic":"subqueries","q":"Subquery drill #94: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Exactly two columns that the outer query can unpack into local variables","Any number of rows as long as the subquery text fits in a single line","Only aggregate functions without a GROUP BY inside the subquery body"],"answer":0},{"id":95,"topic":"cte","q":"CTE drill #95: Can multiple CTEs chain in one WITH?","options":["Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list","No — the standard allows only a single CTE name per SQL statement","Only on very old MySQL releases before common table expressions existed"],"answer":1},{"id":96,"topic":"optimization","q":"Tuning drill #96: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR is ignored by optimizers so indexes are still used exactly like AND for most analytical queries, though dialect quirks should always be verified","OR forces a global sort of the entire table before any filter is applied","OR predicates are always merged into a single perfect index intersection plan"],"answer":0},{"id":97,"topic":"indexes","q":"Index drill #97: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates when the optimizer sees selective predicates and proper key statistics","They are legal only on PRIMARY KEY columns and not on secondary keys","They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly"],"answer":3},{"id":98,"topic":"nulls","q":"NULL drill #98: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","TRUE, so the row always satisfies conjunctions involving NULL comparisons","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL"],"answer":0},{"id":99,"topic":"dml_ddl","q":"DDL drill #99: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation when the optimizer sees selective predicates and proper key statistics","Index entries alone with no reference back to the defining SELECT text"],"answer":1},{"id":100,"topic":"set_ops","q":"Set drill #100: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Match primary-key definitions between the two sides before UNION is legal","Contain exactly one projected expression per SELECT in the operation","Use identical column aliases in the same order or the statement is rejected"],"answer":0},{"id":101,"topic":"basics","q":"Concept drill #101: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","Exactly one scalar column that holds nested documents instead of tuples once NULL handling, collation, and three-valued logic are taken into account","A list that is always sorted by the primary key values for convenient retrieval","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)"],"answer":3},{"id":102,"topic":"joins","q":"Join drill #102: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It refuses to match on column names and instead hashes entire row images only","It equates all same-named columns implicitly, which may not be the intended join keys","It forbids referencing more than one table in the same FROM clause entirely"],"answer":2},{"id":103,"topic":"aggregates","q":"Aggregate drill #103: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","Only COUNT(*) may appear in the SELECT list when GROUP BY is present in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":2},{"id":104,"topic":"window","q":"Window drill #104: What does PARTITION BY in OVER() do?","options":["Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse when the optimizer sees selective predicates and proper key statistics","Splits the row stream so the window function restarts separately in each partition","Sorts every table in the database globally before any WHERE is evaluated"],"answer":2},{"id":105,"topic":"subqueries","q":"Subquery drill #105: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed"],"answer":3},{"id":106,"topic":"cte","q":"CTE drill #106: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","Only on very old MySQL releases before common table expressions existed","No — the standard allows only a single CTE name per SQL statement","Only when the CTE is declared recursive with an ANCHOR keyword present"],"answer":0},{"id":107,"topic":"optimization","q":"Tuning drill #107: Why might OR across different indexed columns hurt performance?","options":["OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR predicates are always merged into a single perfect index intersection plan","OR is ignored by optimizers so indexes are still used exactly like AND when the optimizer sees selective predicates and proper key statistics"],"answer":1},{"id":108,"topic":"indexes","q":"Index drill #108: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They apply only to full table scans and not to selective predicates in typical OLTP and warehouse engines when standard SQL rules apply","They are legal only on PRIMARY KEY columns and not on secondary keys","They are never useful and should be avoided in production schema design"],"answer":0},{"id":109,"topic":"nulls","q":"NULL drill #109: Result of TRUE AND NULL in three-valued logic?","options":["FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","TRUE, so the row always satisfies conjunctions involving NULL comparisons","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE"],"answer":3},{"id":110,"topic":"dml_ddl","q":"DDL drill #110: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Index entries alone with no reference back to the defining SELECT text","Physical copies of all result rows refreshed only when the session ends","Only the transaction log identifiers for the last bulk load operation in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":0},{"id":111,"topic":"set_ops","q":"Set drill #111: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Be compatible in arity and types; corresponding names do not have to match","Match primary-key definitions between the two sides before UNION is legal","Contain exactly one projected expression per SELECT in the operation"],"answer":1},{"id":112,"topic":"basics","q":"Concept drill #112: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","Exactly one scalar column that holds nested documents instead of tuples as documented in the SQL standard, aside from vendor-specific extensions","A list that is always sorted by the primary key values for convenient retrieval","A single physical heap file on disk with no notion of column names or types"],"answer":0},{"id":113,"topic":"joins","q":"Join drill #113: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It forbids referencing more than one table in the same FROM clause entirely","It requires a USING list for every join and fails if any column shares a name","It equates all same-named columns implicitly, which may not be the intended join keys"],"answer":3},{"id":114,"topic":"aggregates","q":"Aggregate drill #114: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","GROUP BY forbids returning any base columns alongside aggregate expressions","Only COUNT(*) may appear in the SELECT list when GROUP BY is present once NULL handling, collation, and three-valued logic are taken into account","HAVING replaces the entire SELECT list and hides non-aggregated columns"],"answer":0},{"id":115,"topic":"window","q":"Window drill #115: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Creates a durable partitioned table object stored on disk for reuse in typical OLTP and warehouse engines when standard SQL rules apply","Splits the row stream so the window function restarts separately in each partition","Eliminates the need for GROUP BY by collapsing all rows into one partition"],"answer":2},{"id":116,"topic":"subqueries","q":"Subquery drill #116: Scalar subquery in SELECT must return?","options":["Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed","Any number of rows as long as the subquery text fits in a single line"],"answer":2},{"id":117,"topic":"cte","q":"CTE drill #117: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list"],"answer":3},{"id":118,"topic":"optimization","q":"Tuning drill #118: Why might OR across different indexed columns hurt performance?","options":["OR is ignored by optimizers so indexes are still used exactly like AND in typical OLTP and warehouse engines when standard SQL rules apply","OR predicates are always merged into a single perfect index intersection plan","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR forces a global sort of the entire table before any filter is applied"],"answer":2},{"id":119,"topic":"indexes","q":"Index drill #119: Partial / filtered indexes are useful when?","options":["They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are never useful and should be avoided in production schema design","They apply only to full table scans and not to selective predicates once NULL handling, collation, and three-valued logic are taken into account"],"answer":1},{"id":120,"topic":"nulls","q":"NULL drill #120: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","TRUE, so the row always satisfies conjunctions involving NULL comparisons"],"answer":0},{"id":121,"topic":"dml_ddl","q":"DDL drill #121: What does CREATE VIEW typically store?","options":["Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation once NULL handling, collation, and three-valued logic are taken into account","Physical copies of all result rows refreshed only when the session ends","The text of a query definition; stored rows remain in the underlying base tables"],"answer":3},{"id":122,"topic":"set_ops","q":"Set drill #122: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Contain exactly one projected expression per SELECT in the operation","Use identical column aliases in the same order or the statement is rejected","Match primary-key definitions between the two sides before UNION is legal"],"answer":0},{"id":123,"topic":"basics","q":"Concept drill #123: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","Exactly one scalar column that holds nested documents instead of tuples for most analytical queries, though dialect quirks should always be verified","A list that is always sorted by the primary key values for convenient retrieval"],"answer":1},{"id":124,"topic":"joins","q":"Join drill #124: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It forbids referencing more than one table in the same FROM clause entirely","It requires a USING list for every join and fails if any column shares a name","It refuses to match on column names and instead hashes entire row images only"],"answer":0},{"id":125,"topic":"aggregates","q":"Aggregate drill #125: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","HAVING replaces the entire SELECT list and hides non-aggregated columns","Only COUNT(*) may appear in the SELECT list when GROUP BY is present as documented in the SQL standard, aside from vendor-specific extensions","They must be grouped or functionally dependent on the grouped keys per SQL rules"],"answer":3},{"id":126,"topic":"window","q":"Window drill #126: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Sorts every table in the database globally before any WHERE is evaluated","Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse once NULL handling, collation, and three-valued logic are taken into account"],"answer":0},{"id":127,"topic":"subqueries","q":"Subquery drill #127: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","At most one row and one column of values for each outer row being processed","Only aggregate functions without a GROUP BY inside the subquery body","Exactly two columns that the outer query can unpack into local variables"],"answer":1},{"id":128,"topic":"cte","q":"CTE drill #128: Can multiple CTEs chain in one WITH?","options":["Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list","No — the standard allows only a single CTE name per SQL statement"],"answer":2},{"id":129,"topic":"optimization","q":"Tuning drill #129: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","OR is ignored by optimizers so indexes are still used exactly like AND once NULL handling, collation, and three-valued logic are taken into account","OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help"],"answer":3},{"id":130,"topic":"indexes","q":"Index drill #130: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates as documented in the SQL standard, aside from vendor-specific extensions","They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are legal only on PRIMARY KEY columns and not on secondary keys"],"answer":2},{"id":131,"topic":"nulls","q":"NULL drill #131: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","FALSE, which removes the row from the result before aggregates are computed"],"answer":2},{"id":132,"topic":"dml_ddl","q":"DDL drill #132: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation as documented in the SQL standard, aside from vendor-specific extensions","Physical copies of all result rows refreshed only when the session ends"],"answer":0},{"id":133,"topic":"set_ops","q":"Set drill #133: For UNION, column lists must?","options":["Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Use identical column aliases in the same order or the statement is rejected","Be compatible in arity and types; corresponding names do not have to match"],"answer":3},{"id":134,"topic":"basics","q":"Concept drill #134: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A list that is always sorted by the primary key values for convenient retrieval","A single physical heap file on disk with no notion of column names or types","Exactly one scalar column that holds nested documents instead of tuples when the optimizer sees selective predicates and proper key statistics"],"answer":0},{"id":135,"topic":"joins","q":"Join drill #135: When is a NATURAL JOIN risky?","options":["It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys","It refuses to match on column names and instead hashes entire row images only","It requires a USING list for every join and fails if any column shares a name"],"answer":1},{"id":136,"topic":"aggregates","q":"Aggregate drill #136: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","HAVING replaces the entire SELECT list and hides non-aggregated columns","Only COUNT(*) may appear in the SELECT list when GROUP BY is present for most analytical queries, though dialect quirks should always be verified","GROUP BY forbids returning any base columns alongside aggregate expressions"],"answer":0},{"id":137,"topic":"window","q":"Window drill #137: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Creates a durable partitioned table object stored on disk for reuse as documented in the SQL standard, aside from vendor-specific extensions","Eliminates the need for GROUP BY by collapsing all rows into one partition","Splits the row stream so the window function restarts separately in each partition"],"answer":3},{"id":138,"topic":"subqueries","q":"Subquery drill #138: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Any number of rows as long as the subquery text fits in a single line","Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body"],"answer":0},{"id":139,"topic":"cte","q":"CTE drill #139: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Yes — later CTEs may reference earlier names in the same WITH clause list","Only when the CTE is declared recursive with an ANCHOR keyword present","Only on very old MySQL releases before common table expressions existed"],"answer":1},{"id":140,"topic":"optimization","q":"Tuning drill #140: Why might OR across different indexed columns hurt performance?","options":["OR is ignored by optimizers so indexes are still used exactly like AND as documented in the SQL standard, aside from vendor-specific extensions","OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR predicates are always merged into a single perfect index intersection plan"],"answer":2},{"id":141,"topic":"indexes","q":"Index drill #141: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","They apply only to full table scans and not to selective predicates for most analytical queries, though dialect quirks should always be verified","They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly"],"answer":3},{"id":142,"topic":"nulls","q":"NULL drill #142: Result of TRUE AND NULL in three-valued logic?","options":["FALSE, which removes the row from the result before aggregates are computed","TRUE, so the row always satisfies conjunctions involving NULL comparisons","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","NULL is forbidden from appearing in boolean expressions in standard SQL"],"answer":2},{"id":143,"topic":"dml_ddl","q":"DDL drill #143: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","Only the transaction log identifiers for the last bulk load operation for most analytical queries, though dialect quirks should always be verified","The text of a query definition; stored rows remain in the underlying base tables","Index entries alone with no reference back to the defining SELECT text"],"answer":2},{"id":144,"topic":"set_ops","q":"Set drill #144: For UNION, column lists must?","options":["Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match","Use identical column aliases in the same order or the statement is rejected"],"answer":2},{"id":145,"topic":"basics","q":"Concept drill #145: Which best describes a relational \"relation\" in SQL theory?","options":["A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples in typical OLTP and warehouse engines when standard SQL rules apply","A single physical heap file on disk with no notion of column names or types","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)"],"answer":3},{"id":146,"topic":"joins","q":"Join drill #146: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It requires a USING list for every join and fails if any column shares a name","It refuses to match on column names and instead hashes entire row images only","It forbids referencing more than one table in the same FROM clause entirely"],"answer":0},{"id":147,"topic":"aggregates","q":"Aggregate drill #147: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","GROUP BY forbids returning any base columns alongside aggregate expressions","Only COUNT(*) may appear in the SELECT list when GROUP BY is present when the optimizer sees selective predicates and proper key statistics"],"answer":1},{"id":148,"topic":"window","q":"Window drill #148: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse for most analytical queries, though dialect quirks should always be verified","Sorts every table in the database globally before any WHERE is evaluated"],"answer":0},{"id":149,"topic":"subqueries","q":"Subquery drill #149: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","Only aggregate functions without a GROUP BY inside the subquery body","Exactly two columns that the outer query can unpack into local variables","At most one row and one column of values for each outer row being processed"],"answer":3},{"id":150,"topic":"cte","q":"CTE drill #150: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","No — the standard allows only a single CTE name per SQL statement","Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present"],"answer":0},{"id":151,"topic":"optimization","q":"Tuning drill #151: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR forces a global sort of the entire table before any filter is applied","OR is ignored by optimizers so indexes are still used exactly like AND for most analytical queries, though dialect quirks should always be verified"],"answer":1},{"id":152,"topic":"indexes","q":"Index drill #152: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are legal only on PRIMARY KEY columns and not on secondary keys","They apply only to full table scans and not to selective predicates when the optimizer sees selective predicates and proper key statistics","They are never useful and should be avoided in production schema design"],"answer":0},{"id":153,"topic":"nulls","q":"NULL drill #153: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE"],"answer":3},{"id":154,"topic":"dml_ddl","q":"DDL drill #154: What does CREATE VIEW typically store?","options":["Index entries alone with no reference back to the defining SELECT text","Physical copies of all result rows refreshed only when the session ends","The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation when the optimizer sees selective predicates and proper key statistics"],"answer":2},{"id":155,"topic":"set_ops","q":"Set drill #155: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match","Contain exactly one projected expression per SELECT in the operation"],"answer":2},{"id":156,"topic":"basics","q":"Concept drill #156: Which best describes a relational \"relation\" in SQL theory?","options":["A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples once NULL handling, collation, and three-valued logic are taken into account","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A single physical heap file on disk with no notion of column names or types"],"answer":2},{"id":157,"topic":"joins","q":"Join drill #157: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It refuses to match on column names and instead hashes entire row images only","It equates all same-named columns implicitly, which may not be the intended join keys"],"answer":3},{"id":158,"topic":"aggregates","q":"Aggregate drill #158: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","Only COUNT(*) may appear in the SELECT list when GROUP BY is present in typical OLTP and warehouse engines when standard SQL rules apply","GROUP BY forbids returning any base columns alongside aggregate expressions","HAVING replaces the entire SELECT list and hides non-aggregated columns"],"answer":0},{"id":159,"topic":"window","q":"Window drill #159: What does PARTITION BY in OVER() do?","options":["Creates a durable partitioned table object stored on disk for reuse when the optimizer sees selective predicates and proper key statistics","Splits the row stream so the window function restarts separately in each partition","Sorts every table in the database globally before any WHERE is evaluated","Eliminates the need for GROUP BY by collapsing all rows into one partition"],"answer":1},{"id":160,"topic":"subqueries","q":"Subquery drill #160: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","Any number of rows as long as the subquery text fits in a single line"],"answer":0},{"id":161,"topic":"cte","q":"CTE drill #161: Can multiple CTEs chain in one WITH?","options":["Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","No — the standard allows only a single CTE name per SQL statement","Yes — later CTEs may reference earlier names in the same WITH clause list"],"answer":3},{"id":162,"topic":"optimization","q":"Tuning drill #162: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR predicates are always merged into a single perfect index intersection plan","OR is ignored by optimizers so indexes are still used exactly like AND when the optimizer sees selective predicates and proper key statistics","OR forces a global sort of the entire table before any filter is applied"],"answer":0},{"id":163,"topic":"indexes","q":"Index drill #163: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are legal only on PRIMARY KEY columns and not on secondary keys","They apply only to full table scans and not to selective predicates in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":1},{"id":164,"topic":"nulls","q":"NULL drill #164: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","NULL is forbidden from appearing in boolean expressions in standard SQL","FALSE, which removes the row from the result before aggregates are computed","TRUE, so the row always satisfies conjunctions involving NULL comparisons"],"answer":0},{"id":165,"topic":"dml_ddl","q":"DDL drill #165: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","Only the transaction log identifiers for the last bulk load operation in typical OLTP and warehouse engines when standard SQL rules apply","Index entries alone with no reference back to the defining SELECT text","The text of a query definition; stored rows remain in the underlying base tables"],"answer":3},{"id":166,"topic":"set_ops","q":"Set drill #166: For UNION, column lists must?","options":["Contain exactly one projected expression per SELECT in the operation","Use identical column aliases in the same order or the statement is rejected","Be compatible in arity and types; corresponding names do not have to match","Match primary-key definitions between the two sides before UNION is legal"],"answer":2},{"id":167,"topic":"basics","q":"Concept drill #167: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","Exactly one scalar column that holds nested documents instead of tuples as documented in the SQL standard, aside from vendor-specific extensions","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A list that is always sorted by the primary key values for convenient retrieval"],"answer":2},{"id":168,"topic":"joins","q":"Join drill #168: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys","It refuses to match on column names and instead hashes entire row images only"],"answer":2},{"id":169,"topic":"aggregates","q":"Aggregate drill #169: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","Only COUNT(*) may appear in the SELECT list when GROUP BY is present once NULL handling, collation, and three-valued logic are taken into account","HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules"],"answer":3},{"id":170,"topic":"window","q":"Window drill #170: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Eliminates the need for GROUP BY by collapsing all rows into one partition","Sorts every table in the database globally before any WHERE is evaluated","Creates a durable partitioned table object stored on disk for reuse in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":0},{"id":171,"topic":"subqueries","q":"Subquery drill #171: Scalar subquery in SELECT must return?","options":["Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed","Any number of rows as long as the subquery text fits in a single line","Exactly two columns that the outer query can unpack into local variables"],"answer":1},{"id":172,"topic":"cte","q":"CTE drill #172: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","No — the standard allows only a single CTE name per SQL statement"],"answer":0},{"id":173,"topic":"optimization","q":"Tuning drill #173: Why might OR across different indexed columns hurt performance?","options":["OR is ignored by optimizers so indexes are still used exactly like AND in typical OLTP and warehouse engines when standard SQL rules apply","OR forces a global sort of the entire table before any filter is applied","OR predicates are always merged into a single perfect index intersection plan","The planner may fail to union index-driven paths; rewriting with UNION ALL can help"],"answer":3},{"id":174,"topic":"indexes","q":"Index drill #174: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are never useful and should be avoided in production schema design","They apply only to full table scans and not to selective predicates once NULL handling, collation, and three-valued logic are taken into account","They are legal only on PRIMARY KEY columns and not on secondary keys"],"answer":0},{"id":175,"topic":"nulls","q":"NULL drill #175: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","NULL is forbidden from appearing in boolean expressions in standard SQL","FALSE, which removes the row from the result before aggregates are computed"],"answer":1},{"id":176,"topic":"dml_ddl","q":"DDL drill #176: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation once NULL handling, collation, and three-valued logic are taken into account","Index entries alone with no reference back to the defining SELECT text","Physical copies of all result rows refreshed only when the session ends"],"answer":0},{"id":177,"topic":"set_ops","q":"Set drill #177: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Match primary-key definitions between the two sides before UNION is legal","Contain exactly one projected expression per SELECT in the operation","Be compatible in arity and types; corresponding names do not have to match"],"answer":3},{"id":178,"topic":"basics","q":"Concept drill #178: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A single physical heap file on disk with no notion of column names or types","A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples for most analytical queries, though dialect quirks should always be verified"],"answer":0},{"id":179,"topic":"joins","q":"Join drill #179: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys","It requires a USING list for every join and fails if any column shares a name"],"answer":2},{"id":180,"topic":"aggregates","q":"Aggregate drill #180: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["Only COUNT(*) may appear in the SELECT list when GROUP BY is present as documented in the SQL standard, aside from vendor-specific extensions","HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","GROUP BY forbids returning any base columns alongside aggregate expressions"],"answer":2},{"id":181,"topic":"window","q":"Window drill #181: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse once NULL handling, collation, and three-valued logic are taken into account","Splits the row stream so the window function restarts separately in each partition"],"answer":3},{"id":182,"topic":"subqueries","q":"Subquery drill #182: Scalar subquery in SELECT must return?","options":["Exactly two columns that the outer query can unpack into local variables","Any number of rows as long as the subquery text fits in a single line","At most one row and one column of values for each outer row being processed","Only aggregate functions without a GROUP BY inside the subquery body"],"answer":2},{"id":183,"topic":"cte","q":"CTE drill #183: Can multiple CTEs chain in one WITH?","options":["Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list","No — the standard allows only a single CTE name per SQL statement","Only on very old MySQL releases before common table expressions existed"],"answer":1},{"id":184,"topic":"optimization","q":"Tuning drill #184: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR is ignored by optimizers so indexes are still used exactly like AND once NULL handling, collation, and three-valued logic are taken into account","OR forces a global sort of the entire table before any filter is applied","OR predicates are always merged into a single perfect index intersection plan"],"answer":0},{"id":185,"topic":"indexes","q":"Index drill #185: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates as documented in the SQL standard, aside from vendor-specific extensions","They are legal only on PRIMARY KEY columns and not on secondary keys","They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly"],"answer":3},{"id":186,"topic":"nulls","q":"NULL drill #186: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","FALSE, which removes the row from the result before aggregates are computed","TRUE, so the row always satisfies conjunctions involving NULL comparisons","NULL is forbidden from appearing in boolean expressions in standard SQL"],"answer":0},{"id":187,"topic":"dml_ddl","q":"DDL drill #187: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation as documented in the SQL standard, aside from vendor-specific extensions","Index entries alone with no reference back to the defining SELECT text"],"answer":1},{"id":188,"topic":"set_ops","q":"Set drill #188: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Match primary-key definitions between the two sides before UNION is legal","Contain exactly one projected expression per SELECT in the operation","Use identical column aliases in the same order or the statement is rejected"],"answer":0},{"id":189,"topic":"basics","q":"Concept drill #189: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","Exactly one scalar column that holds nested documents instead of tuples when the optimizer sees selective predicates and proper key statistics","A list that is always sorted by the primary key values for convenient retrieval","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)"],"answer":3},{"id":190,"topic":"joins","q":"Join drill #190: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It refuses to match on column names and instead hashes entire row images only","It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely"],"answer":0},{"id":191,"topic":"aggregates","q":"Aggregate drill #191: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","Only COUNT(*) may appear in the SELECT list when GROUP BY is present for most analytical queries, though dialect quirks should always be verified"],"answer":2},{"id":192,"topic":"window","q":"Window drill #192: What does PARTITION BY in OVER() do?","options":["Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse as documented in the SQL standard, aside from vendor-specific extensions","Splits the row stream so the window function restarts separately in each partition","Sorts every table in the database globally before any WHERE is evaluated"],"answer":2},{"id":193,"topic":"subqueries","q":"Subquery drill #193: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed"],"answer":3},{"id":194,"topic":"cte","q":"CTE drill #194: Can multiple CTEs chain in one WITH?","options":["Only on very old MySQL releases before common table expressions existed","No — the standard allows only a single CTE name per SQL statement","Yes — later CTEs may reference earlier names in the same WITH clause list","Only when the CTE is declared recursive with an ANCHOR keyword present"],"answer":2},{"id":195,"topic":"optimization","q":"Tuning drill #195: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR is ignored by optimizers so indexes are still used exactly like AND as documented in the SQL standard, aside from vendor-specific extensions"],"answer":2},{"id":196,"topic":"indexes","q":"Index drill #196: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They apply only to full table scans and not to selective predicates for most analytical queries, though dialect quirks should always be verified","They are legal only on PRIMARY KEY columns and not on secondary keys","They are never useful and should be avoided in production schema design"],"answer":0},{"id":197,"topic":"nulls","q":"NULL drill #197: Result of TRUE AND NULL in three-valued logic?","options":["FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","TRUE, so the row always satisfies conjunctions involving NULL comparisons","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE"],"answer":3},{"id":198,"topic":"dml_ddl","q":"DDL drill #198: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Index entries alone with no reference back to the defining SELECT text","Physical copies of all result rows refreshed only when the session ends","Only the transaction log identifiers for the last bulk load operation for most analytical queries, though dialect quirks should always be verified"],"answer":0},{"id":199,"topic":"set_ops","q":"Set drill #199: For UNION, column lists must?","options":["Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match","Use identical column aliases in the same order or the statement is rejected","Contain exactly one projected expression per SELECT in the operation"],"answer":1},{"id":200,"topic":"basics","q":"Concept drill #200: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","Exactly one scalar column that holds nested documents instead of tuples in typical OLTP and warehouse engines when standard SQL rules apply","A list that is always sorted by the primary key values for convenient retrieval","A single physical heap file on disk with no notion of column names or types"],"answer":0},{"id":201,"topic":"joins","q":"Join drill #201: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It forbids referencing more than one table in the same FROM clause entirely","It requires a USING list for every join and fails if any column shares a name","It equates all same-named columns implicitly, which may not be the intended join keys"],"answer":3},{"id":202,"topic":"aggregates","q":"Aggregate drill #202: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","GROUP BY forbids returning any base columns alongside aggregate expressions","Only COUNT(*) may appear in the SELECT list when GROUP BY is present when the optimizer sees selective predicates and proper key statistics","HAVING replaces the entire SELECT list and hides non-aggregated columns"],"answer":0},{"id":203,"topic":"window","q":"Window drill #203: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Splits the row stream so the window function restarts separately in each partition","Creates a durable partitioned table object stored on disk for reuse for most analytical queries, though dialect quirks should always be verified","Eliminates the need for GROUP BY by collapsing all rows into one partition"],"answer":1},{"id":204,"topic":"subqueries","q":"Subquery drill #204: Scalar subquery in SELECT must return?","options":["Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed","Any number of rows as long as the subquery text fits in a single line"],"answer":2},{"id":205,"topic":"cte","q":"CTE drill #205: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list"],"answer":3},{"id":206,"topic":"optimization","q":"Tuning drill #206: Why might OR across different indexed columns hurt performance?","options":["OR is ignored by optimizers so indexes are still used exactly like AND for most analytical queries, though dialect quirks should always be verified","OR predicates are always merged into a single perfect index intersection plan","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR forces a global sort of the entire table before any filter is applied"],"answer":2},{"id":207,"topic":"indexes","q":"Index drill #207: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They apply only to full table scans and not to selective predicates when the optimizer sees selective predicates and proper key statistics"],"answer":2},{"id":208,"topic":"nulls","q":"NULL drill #208: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","TRUE, so the row always satisfies conjunctions involving NULL comparisons"],"answer":0},{"id":209,"topic":"dml_ddl","q":"DDL drill #209: What does CREATE VIEW typically store?","options":["Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation when the optimizer sees selective predicates and proper key statistics","Physical copies of all result rows refreshed only when the session ends","The text of a query definition; stored rows remain in the underlying base tables"],"answer":3},{"id":210,"topic":"set_ops","q":"Set drill #210: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Contain exactly one projected expression per SELECT in the operation","Use identical column aliases in the same order or the statement is rejected","Match primary-key definitions between the two sides before UNION is legal"],"answer":0},{"id":211,"topic":"basics","q":"Concept drill #211: Which best describes a relational \"relation\" in SQL theory?","options":["Exactly one scalar column that holds nested documents instead of tuples once NULL handling, collation, and three-valued logic are taken into account","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A single physical heap file on disk with no notion of column names or types","A list that is always sorted by the primary key values for convenient retrieval"],"answer":1},{"id":212,"topic":"joins","q":"Join drill #212: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It refuses to match on column names and instead hashes entire row images only"],"answer":0},{"id":213,"topic":"aggregates","q":"Aggregate drill #213: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","HAVING replaces the entire SELECT list and hides non-aggregated columns","Only COUNT(*) may appear in the SELECT list when GROUP BY is present in typical OLTP and warehouse engines when standard SQL rules apply","They must be grouped or functionally dependent on the grouped keys per SQL rules"],"answer":3},{"id":214,"topic":"window","q":"Window drill #214: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Sorts every table in the database globally before any WHERE is evaluated","Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse when the optimizer sees selective predicates and proper key statistics"],"answer":0},{"id":215,"topic":"subqueries","q":"Subquery drill #215: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","At most one row and one column of values for each outer row being processed","Only aggregate functions without a GROUP BY inside the subquery body","Exactly two columns that the outer query can unpack into local variables"],"answer":1},{"id":216,"topic":"cte","q":"CTE drill #216: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","Only when the CTE is declared recursive with an ANCHOR keyword present","Only on very old MySQL releases before common table expressions existed","No — the standard allows only a single CTE name per SQL statement"],"answer":0},{"id":217,"topic":"optimization","q":"Tuning drill #217: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","OR is ignored by optimizers so indexes are still used exactly like AND when the optimizer sees selective predicates and proper key statistics","OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help"],"answer":3},{"id":218,"topic":"indexes","q":"Index drill #218: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates in typical OLTP and warehouse engines when standard SQL rules apply","They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are legal only on PRIMARY KEY columns and not on secondary keys"],"answer":2},{"id":219,"topic":"nulls","q":"NULL drill #219: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","FALSE, which removes the row from the result before aggregates are computed"],"answer":2},{"id":220,"topic":"dml_ddl","q":"DDL drill #220: What does CREATE VIEW typically store?","options":["Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation in typical OLTP and warehouse engines when standard SQL rules apply","The text of a query definition; stored rows remain in the underlying base tables","Physical copies of all result rows refreshed only when the session ends"],"answer":2},{"id":221,"topic":"set_ops","q":"Set drill #221: For UNION, column lists must?","options":["Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Use identical column aliases in the same order or the statement is rejected","Be compatible in arity and types; corresponding names do not have to match"],"answer":3},{"id":222,"topic":"basics","q":"Concept drill #222: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A list that is always sorted by the primary key values for convenient retrieval","A single physical heap file on disk with no notion of column names or types","Exactly one scalar column that holds nested documents instead of tuples as documented in the SQL standard, aside from vendor-specific extensions"],"answer":0},{"id":223,"topic":"joins","q":"Join drill #223: When is a NATURAL JOIN risky?","options":["It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys","It refuses to match on column names and instead hashes entire row images only","It requires a USING list for every join and fails if any column shares a name"],"answer":1},{"id":224,"topic":"aggregates","q":"Aggregate drill #224: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","Only COUNT(*) may appear in the SELECT list when GROUP BY is present once NULL handling, collation, and three-valued logic are taken into account","HAVING replaces the entire SELECT list and hides non-aggregated columns","GROUP BY forbids returning any base columns alongside aggregate expressions"],"answer":0},{"id":225,"topic":"window","q":"Window drill #225: What does PARTITION BY in OVER() do?","options":["Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse in typical OLTP and warehouse engines when standard SQL rules apply","Sorts every table in the database globally before any WHERE is evaluated","Splits the row stream so the window function restarts separately in each partition"],"answer":3},{"id":226,"topic":"subqueries","q":"Subquery drill #226: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Any number of rows as long as the subquery text fits in a single line","Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body"],"answer":0},{"id":227,"topic":"cte","q":"CTE drill #227: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Yes — later CTEs may reference earlier names in the same WITH clause list","Only when the CTE is declared recursive with an ANCHOR keyword present","Only on very old MySQL releases before common table expressions existed"],"answer":1},{"id":228,"topic":"optimization","q":"Tuning drill #228: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR forces a global sort of the entire table before any filter is applied","OR is ignored by optimizers so indexes are still used exactly like AND in typical OLTP and warehouse engines when standard SQL rules apply","OR predicates are always merged into a single perfect index intersection plan"],"answer":0},{"id":229,"topic":"indexes","q":"Index drill #229: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","They are legal only on PRIMARY KEY columns and not on secondary keys","They apply only to full table scans and not to selective predicates once NULL handling, collation, and three-valued logic are taken into account","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly"],"answer":3},{"id":230,"topic":"nulls","q":"NULL drill #230: Result of TRUE AND NULL in three-valued logic?","options":["FALSE, which removes the row from the result before aggregates are computed","TRUE, so the row always satisfies conjunctions involving NULL comparisons","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","NULL is forbidden from appearing in boolean expressions in standard SQL"],"answer":2},{"id":231,"topic":"dml_ddl","q":"DDL drill #231: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","Only the transaction log identifiers for the last bulk load operation once NULL handling, collation, and three-valued logic are taken into account","The text of a query definition; stored rows remain in the underlying base tables","Index entries alone with no reference back to the defining SELECT text"],"answer":2},{"id":232,"topic":"set_ops","q":"Set drill #232: For UNION, column lists must?","options":["Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match","Use identical column aliases in the same order or the statement is rejected"],"answer":2},{"id":233,"topic":"basics","q":"Concept drill #233: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples for most analytical queries, though dialect quirks should always be verified","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)"],"answer":3},{"id":234,"topic":"joins","q":"Join drill #234: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It requires a USING list for every join and fails if any column shares a name","It refuses to match on column names and instead hashes entire row images only","It forbids referencing more than one table in the same FROM clause entirely"],"answer":0},{"id":235,"topic":"aggregates","q":"Aggregate drill #235: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","GROUP BY forbids returning any base columns alongside aggregate expressions","Only COUNT(*) may appear in the SELECT list when GROUP BY is present as documented in the SQL standard, aside from vendor-specific extensions"],"answer":1},{"id":236,"topic":"window","q":"Window drill #236: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse once NULL handling, collation, and three-valued logic are taken into account","Sorts every table in the database globally before any WHERE is evaluated"],"answer":0},{"id":237,"topic":"subqueries","q":"Subquery drill #237: Scalar subquery in SELECT must return?","options":["Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","Any number of rows as long as the subquery text fits in a single line","At most one row and one column of values for each outer row being processed"],"answer":3},{"id":238,"topic":"cte","q":"CTE drill #238: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","No — the standard allows only a single CTE name per SQL statement","Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present"],"answer":0},{"id":239,"topic":"optimization","q":"Tuning drill #239: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR forces a global sort of the entire table before any filter is applied","OR is ignored by optimizers so indexes are still used exactly like AND once NULL handling, collation, and three-valued logic are taken into account"],"answer":1},{"id":240,"topic":"indexes","q":"Index drill #240: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are legal only on PRIMARY KEY columns and not on secondary keys","They apply only to full table scans and not to selective predicates as documented in the SQL standard, aside from vendor-specific extensions","They are never useful and should be avoided in production schema design"],"answer":0},{"id":241,"topic":"nulls","q":"NULL drill #241: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","NULL is forbidden from appearing in boolean expressions in standard SQL","FALSE, which removes the row from the result before aggregates are computed","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE"],"answer":3},{"id":242,"topic":"dml_ddl","q":"DDL drill #242: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Physical copies of all result rows refreshed only when the session ends","Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation as documented in the SQL standard, aside from vendor-specific extensions"],"answer":0},{"id":243,"topic":"set_ops","q":"Set drill #243: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match","Contain exactly one projected expression per SELECT in the operation"],"answer":2},{"id":244,"topic":"basics","q":"Concept drill #244: Which best describes a relational \"relation\" in SQL theory?","options":["A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples when the optimizer sees selective predicates and proper key statistics","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A single physical heap file on disk with no notion of column names or types"],"answer":2},{"id":245,"topic":"joins","q":"Join drill #245: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys"],"answer":3},{"id":246,"topic":"aggregates","q":"Aggregate drill #246: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["Only COUNT(*) may appear in the SELECT list when GROUP BY is present for most analytical queries, though dialect quirks should always be verified","GROUP BY forbids returning any base columns alongside aggregate expressions","They must be grouped or functionally dependent on the grouped keys per SQL rules","HAVING replaces the entire SELECT list and hides non-aggregated columns"],"answer":2},{"id":247,"topic":"window","q":"Window drill #247: What does PARTITION BY in OVER() do?","options":["Creates a durable partitioned table object stored on disk for reuse as documented in the SQL standard, aside from vendor-specific extensions","Splits the row stream so the window function restarts separately in each partition","Sorts every table in the database globally before any WHERE is evaluated","Eliminates the need for GROUP BY by collapsing all rows into one partition"],"answer":1},{"id":248,"topic":"subqueries","q":"Subquery drill #248: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","Any number of rows as long as the subquery text fits in a single line"],"answer":0},{"id":249,"topic":"cte","q":"CTE drill #249: Can multiple CTEs chain in one WITH?","options":["Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","No — the standard allows only a single CTE name per SQL statement","Yes — later CTEs may reference earlier names in the same WITH clause list"],"answer":3},{"id":250,"topic":"optimization","q":"Tuning drill #250: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR is ignored by optimizers so indexes are still used exactly like AND as documented in the SQL standard, aside from vendor-specific extensions","OR predicates are always merged into a single perfect index intersection plan","OR forces a global sort of the entire table before any filter is applied"],"answer":0},{"id":251,"topic":"indexes","q":"Index drill #251: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are legal only on PRIMARY KEY columns and not on secondary keys","They apply only to full table scans and not to selective predicates for most analytical queries, though dialect quirks should always be verified"],"answer":1},{"id":252,"topic":"nulls","q":"NULL drill #252: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","NULL is forbidden from appearing in boolean expressions in standard SQL","FALSE, which removes the row from the result before aggregates are computed","TRUE, so the row always satisfies conjunctions involving NULL comparisons"],"answer":0},{"id":253,"topic":"dml_ddl","q":"DDL drill #253: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","Only the transaction log identifiers for the last bulk load operation for most analytical queries, though dialect quirks should always be verified","Index entries alone with no reference back to the defining SELECT text","The text of a query definition; stored rows remain in the underlying base tables"],"answer":3},{"id":254,"topic":"set_ops","q":"Set drill #254: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Use identical column aliases in the same order or the statement is rejected","Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal"],"answer":0},{"id":255,"topic":"basics","q":"Concept drill #255: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","Exactly one scalar column that holds nested documents instead of tuples in typical OLTP and warehouse engines when standard SQL rules apply","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A list that is always sorted by the primary key values for convenient retrieval"],"answer":2},{"id":256,"topic":"joins","q":"Join drill #256: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys","It refuses to match on column names and instead hashes entire row images only"],"answer":2},{"id":257,"topic":"aggregates","q":"Aggregate drill #257: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","Only COUNT(*) may appear in the SELECT list when GROUP BY is present when the optimizer sees selective predicates and proper key statistics","HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules"],"answer":3},{"id":258,"topic":"window","q":"Window drill #258: What does PARTITION BY in OVER() do?","options":["Eliminates the need for GROUP BY by collapsing all rows into one partition","Sorts every table in the database globally before any WHERE is evaluated","Splits the row stream so the window function restarts separately in each partition","Creates a durable partitioned table object stored on disk for reuse for most analytical queries, though dialect quirks should always be verified"],"answer":2},{"id":259,"topic":"subqueries","q":"Subquery drill #259: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed","Exactly two columns that the outer query can unpack into local variables"],"answer":2},{"id":260,"topic":"cte","q":"CTE drill #260: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","No — the standard allows only a single CTE name per SQL statement"],"answer":0},{"id":261,"topic":"optimization","q":"Tuning drill #261: Why might OR across different indexed columns hurt performance?","options":["OR is ignored by optimizers so indexes are still used exactly like AND for most analytical queries, though dialect quirks should always be verified","OR forces a global sort of the entire table before any filter is applied","OR predicates are always merged into a single perfect index intersection plan","The planner may fail to union index-driven paths; rewriting with UNION ALL can help"],"answer":3},{"id":262,"topic":"indexes","q":"Index drill #262: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They apply only to full table scans and not to selective predicates when the optimizer sees selective predicates and proper key statistics","They are never useful and should be avoided in production schema design","They are legal only on PRIMARY KEY columns and not on secondary keys"],"answer":0},{"id":263,"topic":"nulls","q":"NULL drill #263: Result of TRUE AND NULL in three-valued logic?","options":["NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","TRUE, so the row always satisfies conjunctions involving NULL comparisons","FALSE, which removes the row from the result before aggregates are computed"],"answer":1},{"id":264,"topic":"dml_ddl","q":"DDL drill #264: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation when the optimizer sees selective predicates and proper key statistics","Index entries alone with no reference back to the defining SELECT text","Physical copies of all result rows refreshed only when the session ends"],"answer":0},{"id":265,"topic":"set_ops","q":"Set drill #265: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Match primary-key definitions between the two sides before UNION is legal","Contain exactly one projected expression per SELECT in the operation","Be compatible in arity and types; corresponding names do not have to match"],"answer":3},{"id":266,"topic":"basics","q":"Concept drill #266: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A single physical heap file on disk with no notion of column names or types","A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples once NULL handling, collation, and three-valued logic are taken into account"],"answer":0},{"id":267,"topic":"joins","q":"Join drill #267: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It equates all same-named columns implicitly, which may not be the intended join keys","It forbids referencing more than one table in the same FROM clause entirely","It requires a USING list for every join and fails if any column shares a name"],"answer":1},{"id":268,"topic":"aggregates","q":"Aggregate drill #268: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["Only COUNT(*) may appear in the SELECT list when GROUP BY is present in typical OLTP and warehouse engines when standard SQL rules apply","HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","GROUP BY forbids returning any base columns alongside aggregate expressions"],"answer":2},{"id":269,"topic":"window","q":"Window drill #269: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse when the optimizer sees selective predicates and proper key statistics","Splits the row stream so the window function restarts separately in each partition"],"answer":3},{"id":270,"topic":"subqueries","q":"Subquery drill #270: Scalar subquery in SELECT must return?","options":["Exactly two columns that the outer query can unpack into local variables","Any number of rows as long as the subquery text fits in a single line","At most one row and one column of values for each outer row being processed","Only aggregate functions without a GROUP BY inside the subquery body"],"answer":2},{"id":271,"topic":"cte","q":"CTE drill #271: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list","Only on very old MySQL releases before common table expressions existed"],"answer":2},{"id":272,"topic":"optimization","q":"Tuning drill #272: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR is ignored by optimizers so indexes are still used exactly like AND when the optimizer sees selective predicates and proper key statistics","OR forces a global sort of the entire table before any filter is applied","OR predicates are always merged into a single perfect index intersection plan"],"answer":0},{"id":273,"topic":"indexes","q":"Index drill #273: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates in typical OLTP and warehouse engines when standard SQL rules apply","They are legal only on PRIMARY KEY columns and not on secondary keys","They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly"],"answer":3},{"id":274,"topic":"nulls","q":"NULL drill #274: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","FALSE, which removes the row from the result before aggregates are computed","TRUE, so the row always satisfies conjunctions involving NULL comparisons","NULL is forbidden from appearing in boolean expressions in standard SQL"],"answer":0},{"id":275,"topic":"dml_ddl","q":"DDL drill #275: What does CREATE VIEW typically store?","options":["Only the transaction log identifiers for the last bulk load operation in typical OLTP and warehouse engines when standard SQL rules apply","The text of a query definition; stored rows remain in the underlying base tables","Physical copies of all result rows refreshed only when the session ends","Index entries alone with no reference back to the defining SELECT text"],"answer":1},{"id":276,"topic":"set_ops","q":"Set drill #276: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Use identical column aliases in the same order or the statement is rejected"],"answer":0},{"id":277,"topic":"basics","q":"Concept drill #277: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","Exactly one scalar column that holds nested documents instead of tuples as documented in the SQL standard, aside from vendor-specific extensions","A list that is always sorted by the primary key values for convenient retrieval","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)"],"answer":3},{"id":278,"topic":"joins","q":"Join drill #278: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It refuses to match on column names and instead hashes entire row images only","It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely"],"answer":0},{"id":279,"topic":"aggregates","q":"Aggregate drill #279: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","They must be grouped or functionally dependent on the grouped keys per SQL rules","HAVING replaces the entire SELECT list and hides non-aggregated columns","Only COUNT(*) may appear in the SELECT list when GROUP BY is present once NULL handling, collation, and three-valued logic are taken into account"],"answer":1},{"id":280,"topic":"window","q":"Window drill #280: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Creates a durable partitioned table object stored on disk for reuse in typical OLTP and warehouse engines when standard SQL rules apply","Eliminates the need for GROUP BY by collapsing all rows into one partition","Sorts every table in the database globally before any WHERE is evaluated"],"answer":0},{"id":281,"topic":"subqueries","q":"Subquery drill #281: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed"],"answer":3},{"id":282,"topic":"cte","q":"CTE drill #282: Can multiple CTEs chain in one WITH?","options":["Only on very old MySQL releases before common table expressions existed","No — the standard allows only a single CTE name per SQL statement","Yes — later CTEs may reference earlier names in the same WITH clause list","Only when the CTE is declared recursive with an ANCHOR keyword present"],"answer":2},{"id":283,"topic":"optimization","q":"Tuning drill #283: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR is ignored by optimizers so indexes are still used exactly like AND in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":2},{"id":284,"topic":"indexes","q":"Index drill #284: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates once NULL handling, collation, and three-valued logic are taken into account","They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are never useful and should be avoided in production schema design"],"answer":2},{"id":285,"topic":"nulls","q":"NULL drill #285: Result of TRUE AND NULL in three-valued logic?","options":["FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","TRUE, so the row always satisfies conjunctions involving NULL comparisons","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE"],"answer":3},{"id":286,"topic":"dml_ddl","q":"DDL drill #286: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Index entries alone with no reference back to the defining SELECT text","Physical copies of all result rows refreshed only when the session ends","Only the transaction log identifiers for the last bulk load operation once NULL handling, collation, and three-valued logic are taken into account"],"answer":0},{"id":287,"topic":"set_ops","q":"Set drill #287: For UNION, column lists must?","options":["Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match","Use identical column aliases in the same order or the statement is rejected","Contain exactly one projected expression per SELECT in the operation"],"answer":1},{"id":288,"topic":"basics","q":"Concept drill #288: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples for most analytical queries, though dialect quirks should always be verified","A single physical heap file on disk with no notion of column names or types"],"answer":0},{"id":289,"topic":"joins","q":"Join drill #289: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It forbids referencing more than one table in the same FROM clause entirely","It requires a USING list for every join and fails if any column shares a name","It equates all same-named columns implicitly, which may not be the intended join keys"],"answer":3},{"id":290,"topic":"aggregates","q":"Aggregate drill #290: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","GROUP BY forbids returning any base columns alongside aggregate expressions","Only COUNT(*) may appear in the SELECT list when GROUP BY is present as documented in the SQL standard, aside from vendor-specific extensions","HAVING replaces the entire SELECT list and hides non-aggregated columns"],"answer":0},{"id":291,"topic":"window","q":"Window drill #291: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Splits the row stream so the window function restarts separately in each partition","Creates a durable partitioned table object stored on disk for reuse once NULL handling, collation, and three-valued logic are taken into account","Eliminates the need for GROUP BY by collapsing all rows into one partition"],"answer":1},{"id":292,"topic":"subqueries","q":"Subquery drill #292: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Only aggregate functions without a GROUP BY inside the subquery body","Exactly two columns that the outer query can unpack into local variables","Any number of rows as long as the subquery text fits in a single line"],"answer":0},{"id":293,"topic":"cte","q":"CTE drill #293: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Only when the CTE is declared recursive with an ANCHOR keyword present","Only on very old MySQL releases before common table expressions existed","Yes — later CTEs may reference earlier names in the same WITH clause list"],"answer":3},{"id":294,"topic":"optimization","q":"Tuning drill #294: Why might OR across different indexed columns hurt performance?","options":["OR is ignored by optimizers so indexes are still used exactly like AND once NULL handling, collation, and three-valued logic are taken into account","OR predicates are always merged into a single perfect index intersection plan","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR forces a global sort of the entire table before any filter is applied"],"answer":2},{"id":295,"topic":"indexes","q":"Index drill #295: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They apply only to full table scans and not to selective predicates as documented in the SQL standard, aside from vendor-specific extensions"],"answer":2},{"id":296,"topic":"nulls","q":"NULL drill #296: Result of TRUE AND NULL in three-valued logic?","options":["FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","TRUE, so the row always satisfies conjunctions involving NULL comparisons"],"answer":2},{"id":297,"topic":"dml_ddl","q":"DDL drill #297: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation as documented in the SQL standard, aside from vendor-specific extensions","The text of a query definition; stored rows remain in the underlying base tables"],"answer":3},{"id":298,"topic":"set_ops","q":"Set drill #298: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Contain exactly one projected expression per SELECT in the operation","Use identical column aliases in the same order or the statement is rejected","Match primary-key definitions between the two sides before UNION is legal"],"answer":0},{"id":299,"topic":"basics","q":"Concept drill #299: Which best describes a relational \"relation\" in SQL theory?","options":["Exactly one scalar column that holds nested documents instead of tuples when the optimizer sees selective predicates and proper key statistics","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A single physical heap file on disk with no notion of column names or types","A list that is always sorted by the primary key values for convenient retrieval"],"answer":1},{"id":300,"topic":"joins","q":"Join drill #300: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It refuses to match on column names and instead hashes entire row images only"],"answer":0},{"id":301,"topic":"aggregates","q":"Aggregate drill #301: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["Only COUNT(*) may appear in the SELECT list when GROUP BY is present for most analytical queries, though dialect quirks should always be verified","HAVING replaces the entire SELECT list and hides non-aggregated columns","GROUP BY forbids returning any base columns alongside aggregate expressions","They must be grouped or functionally dependent on the grouped keys per SQL rules"],"answer":3},{"id":302,"topic":"window","q":"Window drill #302: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Sorts every table in the database globally before any WHERE is evaluated","Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse as documented in the SQL standard, aside from vendor-specific extensions"],"answer":0},{"id":303,"topic":"subqueries","q":"Subquery drill #303: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","At most one row and one column of values for each outer row being processed","Only aggregate functions without a GROUP BY inside the subquery body","Exactly two columns that the outer query can unpack into local variables"],"answer":1},{"id":304,"topic":"cte","q":"CTE drill #304: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","Only when the CTE is declared recursive with an ANCHOR keyword present","Only on very old MySQL releases before common table expressions existed","No — the standard allows only a single CTE name per SQL statement"],"answer":0},{"id":305,"topic":"optimization","q":"Tuning drill #305: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","OR forces a global sort of the entire table before any filter is applied","OR is ignored by optimizers so indexes are still used exactly like AND as documented in the SQL standard, aside from vendor-specific extensions","The planner may fail to union index-driven paths; rewriting with UNION ALL can help"],"answer":3},{"id":306,"topic":"indexes","q":"Index drill #306: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates for most analytical queries, though dialect quirks should always be verified","They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are legal only on PRIMARY KEY columns and not on secondary keys"],"answer":2},{"id":307,"topic":"nulls","q":"NULL drill #307: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","FALSE, which removes the row from the result before aggregates are computed"],"answer":2},{"id":308,"topic":"dml_ddl","q":"DDL drill #308: What does CREATE VIEW typically store?","options":["Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation for most analytical queries, though dialect quirks should always be verified","The text of a query definition; stored rows remain in the underlying base tables","Physical copies of all result rows refreshed only when the session ends"],"answer":2},{"id":309,"topic":"set_ops","q":"Set drill #309: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match"],"answer":3},{"id":310,"topic":"basics","q":"Concept drill #310: Which best describes a relational \"relation\" in SQL theory?","options":["A list that is always sorted by the primary key values for convenient retrieval","A single physical heap file on disk with no notion of column names or types","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","Exactly one scalar column that holds nested documents instead of tuples in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":2},{"id":311,"topic":"joins","q":"Join drill #311: When is a NATURAL JOIN risky?","options":["It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys","It refuses to match on column names and instead hashes entire row images only","It requires a USING list for every join and fails if any column shares a name"],"answer":1},{"id":312,"topic":"aggregates","q":"Aggregate drill #312: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","Only COUNT(*) may appear in the SELECT list when GROUP BY is present when the optimizer sees selective predicates and proper key statistics","HAVING replaces the entire SELECT list and hides non-aggregated columns","GROUP BY forbids returning any base columns alongside aggregate expressions"],"answer":0},{"id":313,"topic":"window","q":"Window drill #313: What does PARTITION BY in OVER() do?","options":["Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse for most analytical queries, though dialect quirks should always be verified","Sorts every table in the database globally before any WHERE is evaluated","Splits the row stream so the window function restarts separately in each partition"],"answer":3},{"id":314,"topic":"subqueries","q":"Subquery drill #314: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Exactly two columns that the outer query can unpack into local variables","Any number of rows as long as the subquery text fits in a single line","Only aggregate functions without a GROUP BY inside the subquery body"],"answer":0},{"id":315,"topic":"cte","q":"CTE drill #315: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Yes — later CTEs may reference earlier names in the same WITH clause list","Only when the CTE is declared recursive with an ANCHOR keyword present","Only on very old MySQL releases before common table expressions existed"],"answer":1},{"id":316,"topic":"optimization","q":"Tuning drill #316: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR forces a global sort of the entire table before any filter is applied","OR is ignored by optimizers so indexes are still used exactly like AND for most analytical queries, though dialect quirks should always be verified","OR predicates are always merged into a single perfect index intersection plan"],"answer":0},{"id":317,"topic":"indexes","q":"Index drill #317: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","They are legal only on PRIMARY KEY columns and not on secondary keys","They apply only to full table scans and not to selective predicates when the optimizer sees selective predicates and proper key statistics","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly"],"answer":3},{"id":318,"topic":"nulls","q":"NULL drill #318: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","TRUE, so the row always satisfies conjunctions involving NULL comparisons","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL"],"answer":0},{"id":319,"topic":"dml_ddl","q":"DDL drill #319: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","Only the transaction log identifiers for the last bulk load operation when the optimizer sees selective predicates and proper key statistics","The text of a query definition; stored rows remain in the underlying base tables","Index entries alone with no reference back to the defining SELECT text"],"answer":2},{"id":320,"topic":"set_ops","q":"Set drill #320: For UNION, column lists must?","options":["Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match","Use identical column aliases in the same order or the statement is rejected"],"answer":2},{"id":321,"topic":"basics","q":"Concept drill #321: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples once NULL handling, collation, and three-valued logic are taken into account","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)"],"answer":3},{"id":322,"topic":"joins","q":"Join drill #322: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It refuses to match on column names and instead hashes entire row images only","It equates all same-named columns implicitly, which may not be the intended join keys","It forbids referencing more than one table in the same FROM clause entirely"],"answer":2},{"id":323,"topic":"aggregates","q":"Aggregate drill #323: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","GROUP BY forbids returning any base columns alongside aggregate expressions","Only COUNT(*) may appear in the SELECT list when GROUP BY is present in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":1},{"id":324,"topic":"window","q":"Window drill #324: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse when the optimizer sees selective predicates and proper key statistics","Sorts every table in the database globally before any WHERE is evaluated"],"answer":0},{"id":325,"topic":"subqueries","q":"Subquery drill #325: Scalar subquery in SELECT must return?","options":["Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","Any number of rows as long as the subquery text fits in a single line","At most one row and one column of values for each outer row being processed"],"answer":3},{"id":326,"topic":"cte","q":"CTE drill #326: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","Only on very old MySQL releases before common table expressions existed","No — the standard allows only a single CTE name per SQL statement","Only when the CTE is declared recursive with an ANCHOR keyword present"],"answer":0},{"id":327,"topic":"optimization","q":"Tuning drill #327: Why might OR across different indexed columns hurt performance?","options":["OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR predicates are always merged into a single perfect index intersection plan","OR is ignored by optimizers so indexes are still used exactly like AND when the optimizer sees selective predicates and proper key statistics"],"answer":1},{"id":328,"topic":"indexes","q":"Index drill #328: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are legal only on PRIMARY KEY columns and not on secondary keys","They apply only to full table scans and not to selective predicates in typical OLTP and warehouse engines when standard SQL rules apply","They are never useful and should be avoided in production schema design"],"answer":0},{"id":329,"topic":"nulls","q":"NULL drill #329: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","NULL is forbidden from appearing in boolean expressions in standard SQL","FALSE, which removes the row from the result before aggregates are computed","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE"],"answer":3},{"id":330,"topic":"dml_ddl","q":"DDL drill #330: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Physical copies of all result rows refreshed only when the session ends","Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":0},{"id":331,"topic":"set_ops","q":"Set drill #331: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Be compatible in arity and types; corresponding names do not have to match","Match primary-key definitions between the two sides before UNION is legal","Contain exactly one projected expression per SELECT in the operation"],"answer":1},{"id":332,"topic":"basics","q":"Concept drill #332: Which best describes a relational \"relation\" in SQL theory?","options":["A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples as documented in the SQL standard, aside from vendor-specific extensions","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A single physical heap file on disk with no notion of column names or types"],"answer":2},{"id":333,"topic":"joins","q":"Join drill #333: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys"],"answer":3},{"id":334,"topic":"aggregates","q":"Aggregate drill #334: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["Only COUNT(*) may appear in the SELECT list when GROUP BY is present once NULL handling, collation, and three-valued logic are taken into account","GROUP BY forbids returning any base columns alongside aggregate expressions","They must be grouped or functionally dependent on the grouped keys per SQL rules","HAVING replaces the entire SELECT list and hides non-aggregated columns"],"answer":2},{"id":335,"topic":"window","q":"Window drill #335: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Creates a durable partitioned table object stored on disk for reuse in typical OLTP and warehouse engines when standard SQL rules apply","Splits the row stream so the window function restarts separately in each partition","Eliminates the need for GROUP BY by collapsing all rows into one partition"],"answer":2},{"id":336,"topic":"subqueries","q":"Subquery drill #336: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","Any number of rows as long as the subquery text fits in a single line"],"answer":0},{"id":337,"topic":"cte","q":"CTE drill #337: Can multiple CTEs chain in one WITH?","options":["Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","No — the standard allows only a single CTE name per SQL statement","Yes — later CTEs may reference earlier names in the same WITH clause list"],"answer":3},{"id":338,"topic":"optimization","q":"Tuning drill #338: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR is ignored by optimizers so indexes are still used exactly like AND in typical OLTP and warehouse engines when standard SQL rules apply","OR predicates are always merged into a single perfect index intersection plan","OR forces a global sort of the entire table before any filter is applied"],"answer":0},{"id":339,"topic":"indexes","q":"Index drill #339: Partial / filtered indexes are useful when?","options":["They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are never useful and should be avoided in production schema design","They apply only to full table scans and not to selective predicates once NULL handling, collation, and three-valued logic are taken into account"],"answer":1},{"id":340,"topic":"nulls","q":"NULL drill #340: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","TRUE, so the row always satisfies conjunctions involving NULL comparisons"],"answer":0},{"id":341,"topic":"dml_ddl","q":"DDL drill #341: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","Only the transaction log identifiers for the last bulk load operation once NULL handling, collation, and three-valued logic are taken into account","Index entries alone with no reference back to the defining SELECT text","The text of a query definition; stored rows remain in the underlying base tables"],"answer":3},{"id":342,"topic":"set_ops","q":"Set drill #342: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Use identical column aliases in the same order or the statement is rejected","Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal"],"answer":0},{"id":343,"topic":"basics","q":"Concept drill #343: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","Exactly one scalar column that holds nested documents instead of tuples for most analytical queries, though dialect quirks should always be verified","A list that is always sorted by the primary key values for convenient retrieval"],"answer":1},{"id":344,"topic":"joins","q":"Join drill #344: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It forbids referencing more than one table in the same FROM clause entirely","It requires a USING list for every join and fails if any column shares a name","It refuses to match on column names and instead hashes entire row images only"],"answer":0},{"id":345,"topic":"aggregates","q":"Aggregate drill #345: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","Only COUNT(*) may appear in the SELECT list when GROUP BY is present as documented in the SQL standard, aside from vendor-specific extensions","HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules"],"answer":3},{"id":346,"topic":"window","q":"Window drill #346: What does PARTITION BY in OVER() do?","options":["Eliminates the need for GROUP BY by collapsing all rows into one partition","Sorts every table in the database globally before any WHERE is evaluated","Splits the row stream so the window function restarts separately in each partition","Creates a durable partitioned table object stored on disk for reuse once NULL handling, collation, and three-valued logic are taken into account"],"answer":2},{"id":347,"topic":"subqueries","q":"Subquery drill #347: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed","Exactly two columns that the outer query can unpack into local variables"],"answer":2},{"id":348,"topic":"cte","q":"CTE drill #348: Can multiple CTEs chain in one WITH?","options":["Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list","No — the standard allows only a single CTE name per SQL statement"],"answer":2},{"id":349,"topic":"optimization","q":"Tuning drill #349: Why might OR across different indexed columns hurt performance?","options":["OR is ignored by optimizers so indexes are still used exactly like AND once NULL handling, collation, and three-valued logic are taken into account","OR forces a global sort of the entire table before any filter is applied","OR predicates are always merged into a single perfect index intersection plan","The planner may fail to union index-driven paths; rewriting with UNION ALL can help"],"answer":3},{"id":350,"topic":"indexes","q":"Index drill #350: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They apply only to full table scans and not to selective predicates as documented in the SQL standard, aside from vendor-specific extensions","They are never useful and should be avoided in production schema design","They are legal only on PRIMARY KEY columns and not on secondary keys"],"answer":0},{"id":351,"topic":"nulls","q":"NULL drill #351: Result of TRUE AND NULL in three-valued logic?","options":["NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","TRUE, so the row always satisfies conjunctions involving NULL comparisons","FALSE, which removes the row from the result before aggregates are computed"],"answer":1},{"id":352,"topic":"dml_ddl","q":"DDL drill #352: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation as documented in the SQL standard, aside from vendor-specific extensions","Physical copies of all result rows refreshed only when the session ends"],"answer":0},{"id":353,"topic":"set_ops","q":"Set drill #353: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Match primary-key definitions between the two sides before UNION is legal","Contain exactly one projected expression per SELECT in the operation","Be compatible in arity and types; corresponding names do not have to match"],"answer":3},{"id":354,"topic":"basics","q":"Concept drill #354: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A single physical heap file on disk with no notion of column names or types","A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples when the optimizer sees selective predicates and proper key statistics"],"answer":0},{"id":355,"topic":"joins","q":"Join drill #355: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It equates all same-named columns implicitly, which may not be the intended join keys","It forbids referencing more than one table in the same FROM clause entirely","It requires a USING list for every join and fails if any column shares a name"],"answer":1},{"id":356,"topic":"aggregates","q":"Aggregate drill #356: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","HAVING replaces the entire SELECT list and hides non-aggregated columns","Only COUNT(*) may appear in the SELECT list when GROUP BY is present for most analytical queries, though dialect quirks should always be verified","GROUP BY forbids returning any base columns alongside aggregate expressions"],"answer":0},{"id":357,"topic":"window","q":"Window drill #357: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Creates a durable partitioned table object stored on disk for reuse as documented in the SQL standard, aside from vendor-specific extensions","Eliminates the need for GROUP BY by collapsing all rows into one partition","Splits the row stream so the window function restarts separately in each partition"],"answer":3},{"id":358,"topic":"subqueries","q":"Subquery drill #358: Scalar subquery in SELECT must return?","options":["Exactly two columns that the outer query can unpack into local variables","Any number of rows as long as the subquery text fits in a single line","At most one row and one column of values for each outer row being processed","Only aggregate functions without a GROUP BY inside the subquery body"],"answer":2},{"id":359,"topic":"cte","q":"CTE drill #359: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list","Only on very old MySQL releases before common table expressions existed"],"answer":2},{"id":360,"topic":"optimization","q":"Tuning drill #360: Why might OR across different indexed columns hurt performance?","options":["OR is ignored by optimizers so indexes are still used exactly like AND as documented in the SQL standard, aside from vendor-specific extensions","OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR predicates are always merged into a single perfect index intersection plan"],"answer":2},{"id":361,"topic":"indexes","q":"Index drill #361: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","They apply only to full table scans and not to selective predicates for most analytical queries, though dialect quirks should always be verified","They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly"],"answer":3},{"id":362,"topic":"nulls","q":"NULL drill #362: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","FALSE, which removes the row from the result before aggregates are computed","TRUE, so the row always satisfies conjunctions involving NULL comparisons","NULL is forbidden from appearing in boolean expressions in standard SQL"],"answer":0},{"id":363,"topic":"dml_ddl","q":"DDL drill #363: What does CREATE VIEW typically store?","options":["Only the transaction log identifiers for the last bulk load operation for most analytical queries, though dialect quirks should always be verified","The text of a query definition; stored rows remain in the underlying base tables","Physical copies of all result rows refreshed only when the session ends","Index entries alone with no reference back to the defining SELECT text"],"answer":1},{"id":364,"topic":"set_ops","q":"Set drill #364: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Use identical column aliases in the same order or the statement is rejected"],"answer":0},{"id":365,"topic":"basics","q":"Concept drill #365: Which best describes a relational \"relation\" in SQL theory?","options":["A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples in typical OLTP and warehouse engines when standard SQL rules apply","A single physical heap file on disk with no notion of column names or types","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)"],"answer":3},{"id":366,"topic":"joins","q":"Join drill #366: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It refuses to match on column names and instead hashes entire row images only","It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely"],"answer":0},{"id":367,"topic":"aggregates","q":"Aggregate drill #367: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","They must be grouped or functionally dependent on the grouped keys per SQL rules","HAVING replaces the entire SELECT list and hides non-aggregated columns","Only COUNT(*) may appear in the SELECT list when GROUP BY is present when the optimizer sees selective predicates and proper key statistics"],"answer":1},{"id":368,"topic":"window","q":"Window drill #368: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Creates a durable partitioned table object stored on disk for reuse for most analytical queries, though dialect quirks should always be verified","Eliminates the need for GROUP BY by collapsing all rows into one partition","Sorts every table in the database globally before any WHERE is evaluated"],"answer":0},{"id":369,"topic":"subqueries","q":"Subquery drill #369: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","Only aggregate functions without a GROUP BY inside the subquery body","Exactly two columns that the outer query can unpack into local variables","At most one row and one column of values for each outer row being processed"],"answer":3},{"id":370,"topic":"cte","q":"CTE drill #370: Can multiple CTEs chain in one WITH?","options":["Only on very old MySQL releases before common table expressions existed","No — the standard allows only a single CTE name per SQL statement","Yes — later CTEs may reference earlier names in the same WITH clause list","Only when the CTE is declared recursive with an ANCHOR keyword present"],"answer":2},{"id":371,"topic":"optimization","q":"Tuning drill #371: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR is ignored by optimizers so indexes are still used exactly like AND for most analytical queries, though dialect quirks should always be verified"],"answer":2},{"id":372,"topic":"indexes","q":"Index drill #372: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates when the optimizer sees selective predicates and proper key statistics","They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are never useful and should be avoided in production schema design"],"answer":2},{"id":373,"topic":"nulls","q":"NULL drill #373: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE"],"answer":3},{"id":374,"topic":"dml_ddl","q":"DDL drill #374: What does CREATE VIEW typically store?","options":["Index entries alone with no reference back to the defining SELECT text","Physical copies of all result rows refreshed only when the session ends","The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation when the optimizer sees selective predicates and proper key statistics"],"answer":2},{"id":375,"topic":"set_ops","q":"Set drill #375: For UNION, column lists must?","options":["Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match","Use identical column aliases in the same order or the statement is rejected","Contain exactly one projected expression per SELECT in the operation"],"answer":1},{"id":376,"topic":"basics","q":"Concept drill #376: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples once NULL handling, collation, and three-valued logic are taken into account","A single physical heap file on disk with no notion of column names or types"],"answer":0},{"id":377,"topic":"joins","q":"Join drill #377: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It refuses to match on column names and instead hashes entire row images only","It equates all same-named columns implicitly, which may not be the intended join keys"],"answer":3},{"id":378,"topic":"aggregates","q":"Aggregate drill #378: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","Only COUNT(*) may appear in the SELECT list when GROUP BY is present in typical OLTP and warehouse engines when standard SQL rules apply","GROUP BY forbids returning any base columns alongside aggregate expressions","HAVING replaces the entire SELECT list and hides non-aggregated columns"],"answer":0},{"id":379,"topic":"window","q":"Window drill #379: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Splits the row stream so the window function restarts separately in each partition","Creates a durable partitioned table object stored on disk for reuse when the optimizer sees selective predicates and proper key statistics","Eliminates the need for GROUP BY by collapsing all rows into one partition"],"answer":1},{"id":380,"topic":"subqueries","q":"Subquery drill #380: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Only aggregate functions without a GROUP BY inside the subquery body","Exactly two columns that the outer query can unpack into local variables","Any number of rows as long as the subquery text fits in a single line"],"answer":0},{"id":381,"topic":"cte","q":"CTE drill #381: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Only when the CTE is declared recursive with an ANCHOR keyword present","Only on very old MySQL releases before common table expressions existed","Yes — later CTEs may reference earlier names in the same WITH clause list"],"answer":3},{"id":382,"topic":"optimization","q":"Tuning drill #382: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR predicates are always merged into a single perfect index intersection plan","OR is ignored by optimizers so indexes are still used exactly like AND when the optimizer sees selective predicates and proper key statistics","OR forces a global sort of the entire table before any filter is applied"],"answer":0},{"id":383,"topic":"indexes","q":"Index drill #383: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They apply only to full table scans and not to selective predicates in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":2},{"id":384,"topic":"nulls","q":"NULL drill #384: Result of TRUE AND NULL in three-valued logic?","options":["FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","TRUE, so the row always satisfies conjunctions involving NULL comparisons"],"answer":2},{"id":385,"topic":"dml_ddl","q":"DDL drill #385: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation in typical OLTP and warehouse engines when standard SQL rules apply","The text of a query definition; stored rows remain in the underlying base tables"],"answer":3},{"id":386,"topic":"set_ops","q":"Set drill #386: For UNION, column lists must?","options":["Contain exactly one projected expression per SELECT in the operation","Use identical column aliases in the same order or the statement is rejected","Be compatible in arity and types; corresponding names do not have to match","Match primary-key definitions between the two sides before UNION is legal"],"answer":2},{"id":387,"topic":"basics","q":"Concept drill #387: Which best describes a relational \"relation\" in SQL theory?","options":["Exactly one scalar column that holds nested documents instead of tuples as documented in the SQL standard, aside from vendor-specific extensions","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A single physical heap file on disk with no notion of column names or types","A list that is always sorted by the primary key values for convenient retrieval"],"answer":1},{"id":388,"topic":"joins","q":"Join drill #388: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It refuses to match on column names and instead hashes entire row images only"],"answer":0},{"id":389,"topic":"aggregates","q":"Aggregate drill #389: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["Only COUNT(*) may appear in the SELECT list when GROUP BY is present once NULL handling, collation, and three-valued logic are taken into account","HAVING replaces the entire SELECT list and hides non-aggregated columns","GROUP BY forbids returning any base columns alongside aggregate expressions","They must be grouped or functionally dependent on the grouped keys per SQL rules"],"answer":3},{"id":390,"topic":"window","q":"Window drill #390: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Eliminates the need for GROUP BY by collapsing all rows into one partition","Sorts every table in the database globally before any WHERE is evaluated","Creates a durable partitioned table object stored on disk for reuse in typical OLTP and warehouse engines when standard SQL rules apply"],"answer":0},{"id":391,"topic":"subqueries","q":"Subquery drill #391: Scalar subquery in SELECT must return?","options":["Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed","Any number of rows as long as the subquery text fits in a single line","Exactly two columns that the outer query can unpack into local variables"],"answer":1},{"id":392,"topic":"cte","q":"CTE drill #392: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","Only when the CTE is declared recursive with an ANCHOR keyword present","Only on very old MySQL releases before common table expressions existed","No — the standard allows only a single CTE name per SQL statement"],"answer":0},{"id":393,"topic":"optimization","q":"Tuning drill #393: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","OR forces a global sort of the entire table before any filter is applied","OR is ignored by optimizers so indexes are still used exactly like AND in typical OLTP and warehouse engines when standard SQL rules apply","The planner may fail to union index-driven paths; rewriting with UNION ALL can help"],"answer":3},{"id":394,"topic":"indexes","q":"Index drill #394: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are never useful and should be avoided in production schema design","They apply only to full table scans and not to selective predicates once NULL handling, collation, and three-valued logic are taken into account","They are legal only on PRIMARY KEY columns and not on secondary keys"],"answer":0},{"id":395,"topic":"nulls","q":"NULL drill #395: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","NULL is forbidden from appearing in boolean expressions in standard SQL","FALSE, which removes the row from the result before aggregates are computed"],"answer":1},{"id":396,"topic":"dml_ddl","q":"DDL drill #396: What does CREATE VIEW typically store?","options":["Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation once NULL handling, collation, and three-valued logic are taken into account","The text of a query definition; stored rows remain in the underlying base tables","Physical copies of all result rows refreshed only when the session ends"],"answer":2},{"id":397,"topic":"set_ops","q":"Set drill #397: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match"],"answer":3},{"id":398,"topic":"basics","q":"Concept drill #398: Which best describes a relational \"relation\" in SQL theory?","options":["A list that is always sorted by the primary key values for convenient retrieval","A single physical heap file on disk with no notion of column names or types","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","Exactly one scalar column that holds nested documents instead of tuples for most analytical queries, though dialect quirks should always be verified"],"answer":2},{"id":399,"topic":"joins","q":"Join drill #399: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys","It requires a USING list for every join and fails if any column shares a name"],"answer":2},{"id":400,"topic":"aggregates","q":"Aggregate drill #400: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","Only COUNT(*) may appear in the SELECT list when GROUP BY is present as documented in the SQL standard, aside from vendor-specific extensions","HAVING replaces the entire SELECT list and hides non-aggregated columns","GROUP BY forbids returning any base columns alongside aggregate expressions"],"answer":0},{"id":401,"topic":"window","q":"Window drill #401: What does PARTITION BY in OVER() do?","options":["Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse once NULL handling, collation, and three-valued logic are taken into account","Sorts every table in the database globally before any WHERE is evaluated","Splits the row stream so the window function restarts separately in each partition"],"answer":3},{"id":402,"topic":"subqueries","q":"Subquery drill #402: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Exactly two columns that the outer query can unpack into local variables","Any number of rows as long as the subquery text fits in a single line","Only aggregate functions without a GROUP BY inside the subquery body"],"answer":0},{"id":403,"topic":"cte","q":"CTE drill #403: Can multiple CTEs chain in one WITH?","options":["Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list","No — the standard allows only a single CTE name per SQL statement","Only on very old MySQL releases before common table expressions existed"],"answer":1},{"id":404,"topic":"optimization","q":"Tuning drill #404: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR forces a global sort of the entire table before any filter is applied","OR is ignored by optimizers so indexes are still used exactly like AND once NULL handling, collation, and three-valued logic are taken into account","OR predicates are always merged into a single perfect index intersection plan"],"answer":0},{"id":405,"topic":"indexes","q":"Index drill #405: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","They are legal only on PRIMARY KEY columns and not on secondary keys","They apply only to full table scans and not to selective predicates as documented in the SQL standard, aside from vendor-specific extensions","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly"],"answer":3},{"id":406,"topic":"nulls","q":"NULL drill #406: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","TRUE, so the row always satisfies conjunctions involving NULL comparisons","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL"],"answer":0},{"id":407,"topic":"dml_ddl","q":"DDL drill #407: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation as documented in the SQL standard, aside from vendor-specific extensions","Index entries alone with no reference back to the defining SELECT text"],"answer":1},{"id":408,"topic":"set_ops","q":"Set drill #408: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Match primary-key definitions between the two sides before UNION is legal","Contain exactly one projected expression per SELECT in the operation","Use identical column aliases in the same order or the statement is rejected"],"answer":0},{"id":409,"topic":"basics","q":"Concept drill #409: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples when the optimizer sees selective predicates and proper key statistics","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)"],"answer":3},{"id":410,"topic":"joins","q":"Join drill #410: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It refuses to match on column names and instead hashes entire row images only","It equates all same-named columns implicitly, which may not be the intended join keys","It forbids referencing more than one table in the same FROM clause entirely"],"answer":2},{"id":411,"topic":"aggregates","q":"Aggregate drill #411: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","Only COUNT(*) may appear in the SELECT list when GROUP BY is present for most analytical queries, though dialect quirks should always be verified"],"answer":2},{"id":412,"topic":"window","q":"Window drill #412: What does PARTITION BY in OVER() do?","options":["Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse as documented in the SQL standard, aside from vendor-specific extensions","Splits the row stream so the window function restarts separately in each partition","Sorts every table in the database globally before any WHERE is evaluated"],"answer":2},{"id":413,"topic":"subqueries","q":"Subquery drill #413: Scalar subquery in SELECT must return?","options":["Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","Any number of rows as long as the subquery text fits in a single line","At most one row and one column of values for each outer row being processed"],"answer":3},{"id":414,"topic":"cte","q":"CTE drill #414: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","Only on very old MySQL releases before common table expressions existed","No — the standard allows only a single CTE name per SQL statement","Only when the CTE is declared recursive with an ANCHOR keyword present"],"answer":0},{"id":415,"topic":"optimization","q":"Tuning drill #415: Why might OR across different indexed columns hurt performance?","options":["OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR predicates are always merged into a single perfect index intersection plan","OR is ignored by optimizers so indexes are still used exactly like AND as documented in the SQL standard, aside from vendor-specific extensions"],"answer":1},{"id":416,"topic":"indexes","q":"Index drill #416: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They apply only to full table scans and not to selective predicates for most analytical queries, though dialect quirks should always be verified","They are legal only on PRIMARY KEY columns and not on secondary keys","They are never useful and should be avoided in production schema design"],"answer":0},{"id":417,"topic":"nulls","q":"NULL drill #417: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","NULL is forbidden from appearing in boolean expressions in standard SQL","FALSE, which removes the row from the result before aggregates are computed","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE"],"answer":3},{"id":418,"topic":"dml_ddl","q":"DDL drill #418: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Physical copies of all result rows refreshed only when the session ends","Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation for most analytical queries, though dialect quirks should always be verified"],"answer":0},{"id":419,"topic":"set_ops","q":"Set drill #419: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Be compatible in arity and types; corresponding names do not have to match","Match primary-key definitions between the two sides before UNION is legal","Contain exactly one projected expression per SELECT in the operation"],"answer":1},{"id":420,"topic":"basics","q":"Concept drill #420: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","Exactly one scalar column that holds nested documents instead of tuples in typical OLTP and warehouse engines when standard SQL rules apply","A list that is always sorted by the primary key values for convenient retrieval","A single physical heap file on disk with no notion of column names or types"],"answer":0},{"id":421,"topic":"joins","q":"Join drill #421: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys"],"answer":3},{"id":422,"topic":"aggregates","q":"Aggregate drill #422: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["Only COUNT(*) may appear in the SELECT list when GROUP BY is present when the optimizer sees selective predicates and proper key statistics","GROUP BY forbids returning any base columns alongside aggregate expressions","They must be grouped or functionally dependent on the grouped keys per SQL rules","HAVING replaces the entire SELECT list and hides non-aggregated columns"],"answer":2},{"id":423,"topic":"window","q":"Window drill #423: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Creates a durable partitioned table object stored on disk for reuse for most analytical queries, though dialect quirks should always be verified","Splits the row stream so the window function restarts separately in each partition","Eliminates the need for GROUP BY by collapsing all rows into one partition"],"answer":2},{"id":424,"topic":"subqueries","q":"Subquery drill #424: Scalar subquery in SELECT must return?","options":["Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed","Any number of rows as long as the subquery text fits in a single line"],"answer":2},{"id":425,"topic":"cte","q":"CTE drill #425: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list"],"answer":3},{"id":426,"topic":"optimization","q":"Tuning drill #426: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR is ignored by optimizers so indexes are still used exactly like AND for most analytical queries, though dialect quirks should always be verified","OR predicates are always merged into a single perfect index intersection plan","OR forces a global sort of the entire table before any filter is applied"],"answer":0},{"id":427,"topic":"indexes","q":"Index drill #427: Partial / filtered indexes are useful when?","options":["They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are never useful and should be avoided in production schema design","They apply only to full table scans and not to selective predicates when the optimizer sees selective predicates and proper key statistics"],"answer":1},{"id":428,"topic":"nulls","q":"NULL drill #428: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","TRUE, so the row always satisfies conjunctions involving NULL comparisons"],"answer":0},{"id":429,"topic":"dml_ddl","q":"DDL drill #429: What does CREATE VIEW typically store?","options":["Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation when the optimizer sees selective predicates and proper key statistics","Physical copies of all result rows refreshed only when the session ends","The text of a query definition; stored rows remain in the underlying base tables"],"answer":3},{"id":430,"topic":"set_ops","q":"Set drill #430: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Use identical column aliases in the same order or the statement is rejected","Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal"],"answer":0},{"id":431,"topic":"basics","q":"Concept drill #431: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","Exactly one scalar column that holds nested documents instead of tuples once NULL handling, collation, and three-valued logic are taken into account","A list that is always sorted by the primary key values for convenient retrieval"],"answer":1},{"id":432,"topic":"joins","q":"Join drill #432: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It forbids referencing more than one table in the same FROM clause entirely","It requires a USING list for every join and fails if any column shares a name","It refuses to match on column names and instead hashes entire row images only"],"answer":0},{"id":433,"topic":"aggregates","q":"Aggregate drill #433: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","HAVING replaces the entire SELECT list and hides non-aggregated columns","Only COUNT(*) may appear in the SELECT list when GROUP BY is present in typical OLTP and warehouse engines when standard SQL rules apply","They must be grouped or functionally dependent on the grouped keys per SQL rules"],"answer":3},{"id":434,"topic":"window","q":"Window drill #434: What does PARTITION BY in OVER() do?","options":["Eliminates the need for GROUP BY by collapsing all rows into one partition","Sorts every table in the database globally before any WHERE is evaluated","Splits the row stream so the window function restarts separately in each partition","Creates a durable partitioned table object stored on disk for reuse when the optimizer sees selective predicates and proper key statistics"],"answer":2},{"id":435,"topic":"subqueries","q":"Subquery drill #435: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed","Exactly two columns that the outer query can unpack into local variables"],"answer":2},{"id":436,"topic":"cte","q":"CTE drill #436: Can multiple CTEs chain in one WITH?","options":["Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list","No — the standard allows only a single CTE name per SQL statement"],"answer":2},{"id":437,"topic":"optimization","q":"Tuning drill #437: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","OR is ignored by optimizers so indexes are still used exactly like AND when the optimizer sees selective predicates and proper key statistics","OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help"],"answer":3},{"id":438,"topic":"indexes","q":"Index drill #438: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates in typical OLTP and warehouse engines when standard SQL rules apply","They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are legal only on PRIMARY KEY columns and not on secondary keys"],"answer":2},{"id":439,"topic":"nulls","q":"NULL drill #439: Result of TRUE AND NULL in three-valued logic?","options":["NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","TRUE, so the row always satisfies conjunctions involving NULL comparisons","FALSE, which removes the row from the result before aggregates are computed"],"answer":1},{"id":440,"topic":"dml_ddl","q":"DDL drill #440: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation in typical OLTP and warehouse engines when standard SQL rules apply","Physical copies of all result rows refreshed only when the session ends"],"answer":0},{"id":441,"topic":"set_ops","q":"Set drill #441: For UNION, column lists must?","options":["Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Use identical column aliases in the same order or the statement is rejected","Be compatible in arity and types; corresponding names do not have to match"],"answer":3},{"id":442,"topic":"basics","q":"Concept drill #442: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A list that is always sorted by the primary key values for convenient retrieval","A single physical heap file on disk with no notion of column names or types","Exactly one scalar column that holds nested documents instead of tuples as documented in the SQL standard, aside from vendor-specific extensions"],"answer":0},{"id":443,"topic":"joins","q":"Join drill #443: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It equates all same-named columns implicitly, which may not be the intended join keys","It forbids referencing more than one table in the same FROM clause entirely","It requires a USING list for every join and fails if any column shares a name"],"answer":1},{"id":444,"topic":"aggregates","q":"Aggregate drill #444: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","HAVING replaces the entire SELECT list and hides non-aggregated columns","Only COUNT(*) may appear in the SELECT list when GROUP BY is present once NULL handling, collation, and three-valued logic are taken into account","GROUP BY forbids returning any base columns alongside aggregate expressions"],"answer":0},{"id":445,"topic":"window","q":"Window drill #445: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Creates a durable partitioned table object stored on disk for reuse in typical OLTP and warehouse engines when standard SQL rules apply","Eliminates the need for GROUP BY by collapsing all rows into one partition","Splits the row stream so the window function restarts separately in each partition"],"answer":3},{"id":446,"topic":"subqueries","q":"Subquery drill #446: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Any number of rows as long as the subquery text fits in a single line","Exactly two columns that the outer query can unpack into local variables","Only aggregate functions without a GROUP BY inside the subquery body"],"answer":0},{"id":447,"topic":"cte","q":"CTE drill #447: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list","Only on very old MySQL releases before common table expressions existed"],"answer":2},{"id":448,"topic":"optimization","q":"Tuning drill #448: Why might OR across different indexed columns hurt performance?","options":["OR is ignored by optimizers so indexes are still used exactly like AND in typical OLTP and warehouse engines when standard SQL rules apply","OR forces a global sort of the entire table before any filter is applied","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR predicates are always merged into a single perfect index intersection plan"],"answer":2},{"id":449,"topic":"indexes","q":"Index drill #449: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","They apply only to full table scans and not to selective predicates once NULL handling, collation, and three-valued logic are taken into account","They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly"],"answer":3},{"id":450,"topic":"nulls","q":"NULL drill #450: Result of TRUE AND NULL in three-valued logic?","options":["FALSE, which removes the row from the result before aggregates are computed","TRUE, so the row always satisfies conjunctions involving NULL comparisons","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","NULL is forbidden from appearing in boolean expressions in standard SQL"],"answer":2},{"id":451,"topic":"dml_ddl","q":"DDL drill #451: What does CREATE VIEW typically store?","options":["Only the transaction log identifiers for the last bulk load operation once NULL handling, collation, and three-valued logic are taken into account","The text of a query definition; stored rows remain in the underlying base tables","Physical copies of all result rows refreshed only when the session ends","Index entries alone with no reference back to the defining SELECT text"],"answer":1},{"id":452,"topic":"set_ops","q":"Set drill #452: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Use identical column aliases in the same order or the statement is rejected"],"answer":0},{"id":453,"topic":"basics","q":"Concept drill #453: Which best describes a relational \"relation\" in SQL theory?","options":["A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples for most analytical queries, though dialect quirks should always be verified","A single physical heap file on disk with no notion of column names or types","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)"],"answer":3},{"id":454,"topic":"joins","q":"Join drill #454: When is a NATURAL JOIN risky?","options":["It equates all same-named columns implicitly, which may not be the intended join keys","It requires a USING list for every join and fails if any column shares a name","It refuses to match on column names and instead hashes entire row images only","It forbids referencing more than one table in the same FROM clause entirely"],"answer":0},{"id":455,"topic":"aggregates","q":"Aggregate drill #455: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","GROUP BY forbids returning any base columns alongside aggregate expressions","Only COUNT(*) may appear in the SELECT list when GROUP BY is present as documented in the SQL standard, aside from vendor-specific extensions"],"answer":1},{"id":456,"topic":"window","q":"Window drill #456: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Creates a durable partitioned table object stored on disk for reuse once NULL handling, collation, and three-valued logic are taken into account","Eliminates the need for GROUP BY by collapsing all rows into one partition","Sorts every table in the database globally before any WHERE is evaluated"],"answer":0},{"id":457,"topic":"subqueries","q":"Subquery drill #457: Scalar subquery in SELECT must return?","options":["Any number of rows as long as the subquery text fits in a single line","Only aggregate functions without a GROUP BY inside the subquery body","Exactly two columns that the outer query can unpack into local variables","At most one row and one column of values for each outer row being processed"],"answer":3},{"id":458,"topic":"cte","q":"CTE drill #458: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","No — the standard allows only a single CTE name per SQL statement","Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present"],"answer":0},{"id":459,"topic":"optimization","q":"Tuning drill #459: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR forces a global sort of the entire table before any filter is applied","OR is ignored by optimizers so indexes are still used exactly like AND once NULL handling, collation, and three-valued logic are taken into account"],"answer":1},{"id":460,"topic":"indexes","q":"Index drill #460: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates as documented in the SQL standard, aside from vendor-specific extensions","They are legal only on PRIMARY KEY columns and not on secondary keys","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are never useful and should be avoided in production schema design"],"answer":2},{"id":461,"topic":"nulls","q":"NULL drill #461: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE"],"answer":3},{"id":462,"topic":"dml_ddl","q":"DDL drill #462: What does CREATE VIEW typically store?","options":["Index entries alone with no reference back to the defining SELECT text","Physical copies of all result rows refreshed only when the session ends","The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation as documented in the SQL standard, aside from vendor-specific extensions"],"answer":2},{"id":463,"topic":"set_ops","q":"Set drill #463: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match","Contain exactly one projected expression per SELECT in the operation"],"answer":2},{"id":464,"topic":"basics","q":"Concept drill #464: Which best describes a relational \"relation\" in SQL theory?","options":["An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A list that is always sorted by the primary key values for convenient retrieval","Exactly one scalar column that holds nested documents instead of tuples when the optimizer sees selective predicates and proper key statistics","A single physical heap file on disk with no notion of column names or types"],"answer":0},{"id":465,"topic":"joins","q":"Join drill #465: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It refuses to match on column names and instead hashes entire row images only","It equates all same-named columns implicitly, which may not be the intended join keys"],"answer":3},{"id":466,"topic":"aggregates","q":"Aggregate drill #466: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["They must be grouped or functionally dependent on the grouped keys per SQL rules","Only COUNT(*) may appear in the SELECT list when GROUP BY is present for most analytical queries, though dialect quirks should always be verified","GROUP BY forbids returning any base columns alongside aggregate expressions","HAVING replaces the entire SELECT list and hides non-aggregated columns"],"answer":0},{"id":467,"topic":"window","q":"Window drill #467: What does PARTITION BY in OVER() do?","options":["Creates a durable partitioned table object stored on disk for reuse as documented in the SQL standard, aside from vendor-specific extensions","Splits the row stream so the window function restarts separately in each partition","Sorts every table in the database globally before any WHERE is evaluated","Eliminates the need for GROUP BY by collapsing all rows into one partition"],"answer":1},{"id":468,"topic":"subqueries","q":"Subquery drill #468: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Only aggregate functions without a GROUP BY inside the subquery body","Exactly two columns that the outer query can unpack into local variables","Any number of rows as long as the subquery text fits in a single line"],"answer":0},{"id":469,"topic":"cte","q":"CTE drill #469: Can multiple CTEs chain in one WITH?","options":["No — the standard allows only a single CTE name per SQL statement","Only when the CTE is declared recursive with an ANCHOR keyword present","Only on very old MySQL releases before common table expressions existed","Yes — later CTEs may reference earlier names in the same WITH clause list"],"answer":3},{"id":470,"topic":"optimization","q":"Tuning drill #470: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR predicates are always merged into a single perfect index intersection plan","OR is ignored by optimizers so indexes are still used exactly like AND as documented in the SQL standard, aside from vendor-specific extensions","OR forces a global sort of the entire table before any filter is applied"],"answer":0},{"id":471,"topic":"indexes","q":"Index drill #471: Partial / filtered indexes are useful when?","options":["They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are legal only on PRIMARY KEY columns and not on secondary keys","They apply only to full table scans and not to selective predicates for most analytical queries, though dialect quirks should always be verified"],"answer":1},{"id":472,"topic":"nulls","q":"NULL drill #472: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","NULL is forbidden from appearing in boolean expressions in standard SQL","FALSE, which removes the row from the result before aggregates are computed","TRUE, so the row always satisfies conjunctions involving NULL comparisons"],"answer":0},{"id":473,"topic":"dml_ddl","q":"DDL drill #473: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","Index entries alone with no reference back to the defining SELECT text","Only the transaction log identifiers for the last bulk load operation for most analytical queries, though dialect quirks should always be verified","The text of a query definition; stored rows remain in the underlying base tables"],"answer":3},{"id":474,"topic":"set_ops","q":"Set drill #474: For UNION, column lists must?","options":["Contain exactly one projected expression per SELECT in the operation","Use identical column aliases in the same order or the statement is rejected","Be compatible in arity and types; corresponding names do not have to match","Match primary-key definitions between the two sides before UNION is legal"],"answer":2},{"id":475,"topic":"basics","q":"Concept drill #475: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","Exactly one scalar column that holds nested documents instead of tuples in typical OLTP and warehouse engines when standard SQL rules apply","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","A list that is always sorted by the primary key values for convenient retrieval"],"answer":2},{"id":476,"topic":"joins","q":"Join drill #476: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys","It refuses to match on column names and instead hashes entire row images only"],"answer":2},{"id":477,"topic":"aggregates","q":"Aggregate drill #477: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["Only COUNT(*) may appear in the SELECT list when GROUP BY is present when the optimizer sees selective predicates and proper key statistics","HAVING replaces the entire SELECT list and hides non-aggregated columns","GROUP BY forbids returning any base columns alongside aggregate expressions","They must be grouped or functionally dependent on the grouped keys per SQL rules"],"answer":3},{"id":478,"topic":"window","q":"Window drill #478: What does PARTITION BY in OVER() do?","options":["Splits the row stream so the window function restarts separately in each partition","Eliminates the need for GROUP BY by collapsing all rows into one partition","Sorts every table in the database globally before any WHERE is evaluated","Creates a durable partitioned table object stored on disk for reuse for most analytical queries, though dialect quirks should always be verified"],"answer":0},{"id":479,"topic":"subqueries","q":"Subquery drill #479: Scalar subquery in SELECT must return?","options":["Only aggregate functions without a GROUP BY inside the subquery body","At most one row and one column of values for each outer row being processed","Any number of rows as long as the subquery text fits in a single line","Exactly two columns that the outer query can unpack into local variables"],"answer":1},{"id":480,"topic":"cte","q":"CTE drill #480: Can multiple CTEs chain in one WITH?","options":["Yes — later CTEs may reference earlier names in the same WITH clause list","Only on very old MySQL releases before common table expressions existed","Only when the CTE is declared recursive with an ANCHOR keyword present","No — the standard allows only a single CTE name per SQL statement"],"answer":0},{"id":481,"topic":"optimization","q":"Tuning drill #481: Why might OR across different indexed columns hurt performance?","options":["OR predicates are always merged into a single perfect index intersection plan","OR forces a global sort of the entire table before any filter is applied","OR is ignored by optimizers so indexes are still used exactly like AND for most analytical queries, though dialect quirks should always be verified","The planner may fail to union index-driven paths; rewriting with UNION ALL can help"],"answer":3},{"id":482,"topic":"indexes","q":"Index drill #482: Partial / filtered indexes are useful when?","options":["A predicate isolates a hot fraction of rows so the index stays small and cache-friendly","They are never useful and should be avoided in production schema design","They apply only to full table scans and not to selective predicates when the optimizer sees selective predicates and proper key statistics","They are legal only on PRIMARY KEY columns and not on secondary keys"],"answer":0},{"id":483,"topic":"nulls","q":"NULL drill #483: Result of TRUE AND NULL in three-valued logic?","options":["TRUE, so the row always satisfies conjunctions involving NULL comparisons","UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","NULL is forbidden from appearing in boolean expressions in standard SQL","FALSE, which removes the row from the result before aggregates are computed"],"answer":1},{"id":484,"topic":"dml_ddl","q":"DDL drill #484: What does CREATE VIEW typically store?","options":["The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation when the optimizer sees selective predicates and proper key statistics","Index entries alone with no reference back to the defining SELECT text","Physical copies of all result rows refreshed only when the session ends"],"answer":0},{"id":485,"topic":"set_ops","q":"Set drill #485: For UNION, column lists must?","options":["Use identical column aliases in the same order or the statement is rejected","Contain exactly one projected expression per SELECT in the operation","Match primary-key definitions between the two sides before UNION is legal","Be compatible in arity and types; corresponding names do not have to match"],"answer":3},{"id":486,"topic":"basics","q":"Concept drill #486: Which best describes a relational \"relation\" in SQL theory?","options":["A list that is always sorted by the primary key values for convenient retrieval","A single physical heap file on disk with no notion of column names or types","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)","Exactly one scalar column that holds nested documents instead of tuples once NULL handling, collation, and three-valued logic are taken into account"],"answer":2},{"id":487,"topic":"joins","q":"Join drill #487: When is a NATURAL JOIN risky?","options":["It refuses to match on column names and instead hashes entire row images only","It forbids referencing more than one table in the same FROM clause entirely","It equates all same-named columns implicitly, which may not be the intended join keys","It requires a USING list for every join and fails if any column shares a name"],"answer":2},{"id":488,"topic":"aggregates","q":"Aggregate drill #488: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["Only COUNT(*) may appear in the SELECT list when GROUP BY is present in typical OLTP and warehouse engines when standard SQL rules apply","HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","GROUP BY forbids returning any base columns alongside aggregate expressions"],"answer":2},{"id":489,"topic":"window","q":"Window drill #489: What does PARTITION BY in OVER() do?","options":["Sorts every table in the database globally before any WHERE is evaluated","Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse when the optimizer sees selective predicates and proper key statistics","Splits the row stream so the window function restarts separately in each partition"],"answer":3},{"id":490,"topic":"subqueries","q":"Subquery drill #490: Scalar subquery in SELECT must return?","options":["At most one row and one column of values for each outer row being processed","Exactly two columns that the outer query can unpack into local variables","Any number of rows as long as the subquery text fits in a single line","Only aggregate functions without a GROUP BY inside the subquery body"],"answer":0},{"id":491,"topic":"cte","q":"CTE drill #491: Can multiple CTEs chain in one WITH?","options":["Only when the CTE is declared recursive with an ANCHOR keyword present","Yes — later CTEs may reference earlier names in the same WITH clause list","No — the standard allows only a single CTE name per SQL statement","Only on very old MySQL releases before common table expressions existed"],"answer":1},{"id":492,"topic":"optimization","q":"Tuning drill #492: Why might OR across different indexed columns hurt performance?","options":["The planner may fail to union index-driven paths; rewriting with UNION ALL can help","OR is ignored by optimizers so indexes are still used exactly like AND when the optimizer sees selective predicates and proper key statistics","OR forces a global sort of the entire table before any filter is applied","OR predicates are always merged into a single perfect index intersection plan"],"answer":0},{"id":493,"topic":"indexes","q":"Index drill #493: Partial / filtered indexes are useful when?","options":["They apply only to full table scans and not to selective predicates in typical OLTP and warehouse engines when standard SQL rules apply","They are legal only on PRIMARY KEY columns and not on secondary keys","They are never useful and should be avoided in production schema design","A predicate isolates a hot fraction of rows so the index stays small and cache-friendly"],"answer":3},{"id":494,"topic":"nulls","q":"NULL drill #494: Result of TRUE AND NULL in three-valued logic?","options":["UNKNOWN — the row does not pass a WHERE clause that needs pure TRUE","TRUE, so the row always satisfies conjunctions involving NULL comparisons","FALSE, which removes the row from the result before aggregates are computed","NULL is forbidden from appearing in boolean expressions in standard SQL"],"answer":0},{"id":495,"topic":"dml_ddl","q":"DDL drill #495: What does CREATE VIEW typically store?","options":["Physical copies of all result rows refreshed only when the session ends","The text of a query definition; stored rows remain in the underlying base tables","Only the transaction log identifiers for the last bulk load operation in typical OLTP and warehouse engines when standard SQL rules apply","Index entries alone with no reference back to the defining SELECT text"],"answer":1},{"id":496,"topic":"set_ops","q":"Set drill #496: For UNION, column lists must?","options":["Be compatible in arity and types; corresponding names do not have to match","Match primary-key definitions between the two sides before UNION is legal","Contain exactly one projected expression per SELECT in the operation","Use identical column aliases in the same order or the statement is rejected"],"answer":0},{"id":497,"topic":"basics","q":"Concept drill #497: Which best describes a relational \"relation\" in SQL theory?","options":["A single physical heap file on disk with no notion of column names or types","Exactly one scalar column that holds nested documents instead of tuples as documented in the SQL standard, aside from vendor-specific extensions","A list that is always sorted by the primary key values for convenient retrieval","An unordered set of tuples (rows) sharing a fixed set of named attributes (columns)"],"answer":3},{"id":498,"topic":"joins","q":"Join drill #498: When is a NATURAL JOIN risky?","options":["It requires a USING list for every join and fails if any column shares a name","It refuses to match on column names and instead hashes entire row images only","It equates all same-named columns implicitly, which may not be the intended join keys","It forbids referencing more than one table in the same FROM clause entirely"],"answer":2},{"id":499,"topic":"aggregates","q":"Aggregate drill #499: Why can SELECT non-aggregated columns be invalid with GROUP BY?","options":["GROUP BY forbids returning any base columns alongside aggregate expressions","HAVING replaces the entire SELECT list and hides non-aggregated columns","They must be grouped or functionally dependent on the grouped keys per SQL rules","Only COUNT(*) may appear in the SELECT list when GROUP BY is present once NULL handling, collation, and three-valued logic are taken into account"],"answer":2},{"id":500,"topic":"window","q":"Window drill #500: What does PARTITION BY in OVER() do?","options":["Eliminates the need for GROUP BY by collapsing all rows into one partition","Creates a durable partitioned table object stored on disk for reuse in typical OLTP and warehouse engines when standard SQL rules apply","Splits the row stream so the window function restarts separately in each partition","Sorts every table in the database globally before any WHERE is evaluated"],"answer":2}]}